# Ready-to-Paste Next Session Start — V1.1.0 — PR #113

Give this file alone to the next developer in the first interaction.

Do not also paste the full handoff, bootstrap JSON, history, roadmap, startup pack or other large project files unless the fallback conditions below occur. The successor should retrieve the compact context system and any deeper evidence itself from GitHub.

Continue the FIFA 17 Career Mode Showdown project from the newest live source state.

## GitHub route rule

Use the connected GitHub app/tool first for repository reads, live-main verification, PRs, CI evidence, branches, commits, issues, reviews and supported writes.

The repository-owned rootless `gh` CLI bootstrap is a separate fallback utility, not the connected GitHub app and not a startup prerequisite merely because it exists. Use it only when a concrete required operation cannot be performed through the connected GitHub tool and genuinely needs local CLI capability. Do not repeatedly retry it. After two materially similar route failures, obey the route circuit breaker. Never copy connector credentials into `gh`.

This owner instruction is newer than predecessor wording that made the CLI bootstrap mandatory in every fresh Work environment. Reconcile the older process docs/contracts naturally inside the first substantive engineering PR, not through a documentation-only sidequest.

## Adaptive lean startup

Do not preload the full project history.

From transition branch `agent/post-pr113-app-check-handoff`, retrieve only:

1. `SESSION_BOOTSTRAP.json`
2. `SESSION_CONTEXT_MODEL.json`
3. `SESSION_CONTEXT_GRAPH.json`

Then verify live GitHub `main`, newer relevant merged/open PRs, current candidate CI if any, package/runtime identity and `WORK_ENVIRONMENT_STATUS.json`.

The context system preserves the full project record in repository sources but ranks what should actually enter the new session. It uses current-task relevance, source authority, live delta, dependency distance, security/recovery criticality, contradiction value, learned utility, staleness and context cost.

Mandatory security/recovery/current-authority locks always bypass ranking.

If live `main` still matches the capsule and no newer current-authority work supersedes it, load only the highest-ranked task packet under the compact context budget and immediately continue the real next prerequisite.

If live `main` differs, inspect only the delta from the recorded boundary to live main first. Give intervening changed files highest delta priority, reconstruct the changed current lane, then load only the newly relevant task packet.

Use the complete successor handoff/history only as Tier 3 deep fallback when the compact context plus live delta cannot resolve a contradiction, a historical contract failure requires provenance, security/recovery/versioning semantics are ambiguous, the owner requests full reconstruction, or WEC/interruption recovery requires it.

Do not train or introduce a heavyweight neural model/vector database at startup. The current system intentionally uses an ML-ready hybrid of information retrieval, dependency graph ranking, deterministic safety priors and conservative online utility learning. A small supervised ranking model may be evaluated only after enough labeled sessions exist and only if held-out startup tests prove lower context cost without more missed-critical-context incidents.

## Expected substantive lane

At this checkpoint the expected next lane is Firebase App Check with reCAPTCHA Enterprise registration/integration proof, subject to fresh live-source and current official Firebase/Google Cloud verification.

Keep App Check enforcement off until controlled client integration and metrics prove legitimate traffic works. Do not start Stage 3 pairing before genuine Stage 2 production/operational activation is complete.

Private Remote Joining remains the highest long-term priority, dependency-gated and stability-first. Do not create documentation/history/process sidequests when substantive prerequisite work is available.

## When the owner should send more files

Do not ask the owner for a second upload by default. Retrieve referenced files directly from GitHub.

Request/use the startup pack or full handoff only if:

1. the connected GitHub tool cannot access the repository or transition branch;
2. the compact context files are missing/corrupt;
3. live source materially contradicts the compact state and the delta cannot resolve the current lane;
4. a deep historical security/recovery/versioning rationale is genuinely required and cannot be recovered from current source;
5. interruption recovery or WEC handoff requires the complete record.

## WEF 100 packaging rule

At every future `Handoff proximity: 100%` checkpoint, generate and make available to the owner:

1. the complete successor handoff at repository root;
2. its byte-identical mirror under `project-documents/handoffs/`;
3. a new versioned `START_NEXT_SESSION_...md` root file;
4. its byte-identical mirror under `project-documents/session-starts/`;
5. refreshed `SESSION_BOOTSTRAP.json`, `SESSION_CONTEXT_MODEL.json` and `SESSION_CONTEXT_GRAPH.json` when current-state or learned evidence changed.

Starter versioning is independent from application version: PATCH for an ordinary new WEF checkpoint using the same startup protocol, MINOR for a backward-compatible material protocol improvement, MAJOR for a breaking startup-contract redesign.

The owner-facing rule remains simple: give only the newest versioned `START_NEXT_SESSION_...md` file to the next developer first.

Because this adaptive context system was created on the transition branch after PR #113, carry it naturally into the first substantive App Check PR. Do not open a documentation-only PR solely to publish it.
