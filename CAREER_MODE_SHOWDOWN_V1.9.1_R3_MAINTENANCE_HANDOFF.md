# Career Mode Showdown v1.9.1-r3 Maintenance Release Record

Status: RELEASE CANDIDATE / NOT PRODUCTION-PROVEN
Application version: `v1.9.1`
Runtime revision: `1.9.1-r3`
Previous known-good whole shell: `1.9.1-r2`
Remote Joining readiness: `100/100` under frozen model `RJR-1`
Shared Showdown Journey readiness: `0/100` under fixed model `SSJR-1.1`

## Purpose

This runtime-only release candidate introduces the smallest paired-first production Shared Showdown Setup milestone for exactly two private managers. It advances the private Remote Joining journey so manager/profile inputs and the pre-draw Save Library shell are followed by exact Connected Rivalry pairing, an exact ACTIVE private session, authoritative league and club selection, authoritative `1 / 3 / 5 / 10` season length, and dual manager confirmation in that order.

The shared-mode marker is non-secret and persists with the active pre-draw Save Library shell. It survives a fresh tab/browser session instead of relying only on `sessionStorage`. Capture-phase guards block the actual bound local league/club controls while the shared setup is pending, including when a modified client removes `disabled` attributes. Candidate protocol/catalog/adapter modules remain non-production authorities used only behind the separately reviewed production wrapper.

## Candidate evidence

PR #203 remains open. This maintenance record is candidate-era authority only and must not be rewritten as deployed proof before the final exact PR head passes all 15 permanent workflow families, review threads are resolved, expected-head merge protection succeeds, all post-merge main workflows pass, the zero-billing Firestore Rules publisher performs exact provider readback, and the deployed `1.9.1-r3` whole shell is independently verified.

The permanent Stage 3 family now includes the sessionStorage-cleared reload/browser bypass proof and the generated production Rules emulator proof. The generated Firestore authority must equal the reviewed Spark base plus only the two bounded Shared Setup fragment splices. Source, tests, CI, review, merge, deployment, Rules publication, documentation, WEC and this maintenance record earn zero SSJR credit by themselves.

## Installed-app whole-shell boundary

`1.9.1-r3` is a new whole-shell identity because executable browser behavior changed. The Service Worker shell caches the complete SSJR runtime dependency chain so an already-installed controlled PWA does not serve an old shell while denying new versioned Shared Setup dependencies. `1.9.1-r2` remains the immediate independently production-proven recovery target. Never construct a mixed-version rollback.

The Installable Offline App remains protected. The v1.3.0 Recovery & Device Resilience baseline remains protected. Local Profiles and the Save Library remain the canonical local identity/save chain.

## Locked safety boundary

Firebase remains Spark and billing remains permanently forbidden. Billing must never be activated. Do not attach Cloud Billing, enable Blaze, add a payment method, activate Cloud Run or Cloud Functions, purchase credits, or select any billing-required provider path. Firestore browser persistence remains memory-only. App Check enforcement remains OFF. Google Auth remains popup-only `browserSessionPersistence` with no additional scopes.

Private pairing, Connected Rivalry and Private Remote Joining remain exact non-enumerable capabilities for exactly two private managers with no public discovery, listing, lobby, matchmaking, community, rankings or global leaderboards. Canonical local storage remains exactly `careerModeShowdown.saveLibrary`, `careerModeShowdown.legacyShowdowns`, and `careerModeShowdown.preferences`.

Candidate A remains non-mutating. Candidate B remains read-only. Candidate C remains the sole destructive remote-to-local gameplay Apply authority with transaction-owned mutation and strict exact raw-snapshot rollback. Shared Setup itself does not destructively apply remote gameplay state to canonical local saves. Provider or Spark quota failure must fail closed while local play remains available.

## Shared Setup authority boundary

The production wrapper requires the exact connected Google account, registered browser/device, attached Connected Rivalry and exact unexpired ACTIVE private session before any authoritative shared league or club operation. The provider uses immutable repository-owned league/club catalog authority, CAS revisions and fresh idempotency operation IDs. A fresh ACTIVE private session for the same rivalry resumes existing setup and never redraws it.

The generated production Firestore Rules remain deny-by-default, no-list and no-delete. They require the exact two-manager pairing, active account/device/session authority, host-only setup open, sequential setup transitions, lengths limited to 1/3/5/10, preserved history prefix, and dual confirmations. The reviewed Spark base remains unchanged; the bounded Shared Setup authority is generated additively and reproven against the adversarial emulator before production authentication and publication.

## Evidence truth and next boundary

RJR-1 remains frozen `100/100`; its consumed physical two-device/two-network acceptance is not repeatable or recreditable absent a proven regression. SSJR-1.1 remains `0/100`. The first SSJR score movement still requires the fixed production two-account evidence layer; publication mechanics cannot substitute for that evidence.

This candidate deliberately stops before transfer/results/scoring/history transport. After r3 is production-proven, the next bounded evidence action is the genuine production two-account paired-first Shared Setup journey if it can be executed without violating the consumed RJR evidence rule. Later Shared Journey milestones remain necessary for genuine SSJR100.