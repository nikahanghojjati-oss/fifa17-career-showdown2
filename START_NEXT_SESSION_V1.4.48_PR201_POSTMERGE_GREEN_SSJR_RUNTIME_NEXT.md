# Start next session v1.4.48 — PR201 provider candidate published, paired-first production runtime next

SLE = Smart Lean Efficient. Treat this starter as orientation only. Current source, later owner instructions and live GitHub/provider/deployment evidence win.

Repository: `nikahanghojjati-oss/fifa17-career-showdown2`
Public site: https://nikahanghojjati-oss.github.io/fifa17-career-showdown2/

Read `AGENTS.md`, `SESSION_BOOTSTRAP.json`, `00_CURRENT_HANDOFF.md`, `NEXT_TASK.md`, `WORK_ENVIRONMENT_STATUS.json`, `SHARED_SHOWDOWN_JOURNEY_MODEL.json`, and `SHARED_SHOWDOWN_JOURNEY_READINESS.json` first. Expand to `SUCCESSOR_HANDOFF_PR201_POSTMERGE_GREEN_SSJR_RUNTIME_NEXT_SLE_2026-09-06.md` only as needed.

## Verified boundary

PR #201 final exact head: `2c99e0746d6a3c5df946aa96f2c010da208bf70f`.
PR #201 exact tree / squash-merge tree: `87ddbde2550facccdfba0ec02a3a625ea81d1a08`.
PR #201 squash merge / live-main publication boundary: `0cf810f99460bc66d41b4a036d42befb5bb5ef39`.

The exact PR head passed all 15 permanent pull-request workflow families. `Validate Stage 3 Private Pairing` included the new Shared Showdown Setup Spark candidate emulator step and both provider emulator suites passed. The emulator proved paired ACTIVE-session authority, exact-two-manager gating, CAS/idempotency, immutable canonical league/club selection, direct modified-client denial, exact-session substitution denial, same-rivalry fresh-session continuity, dual-manager identical confirmation, inactive/revoked identity denial, and zero canonical local-save mutation.

PR201 fixed two adversarial issues before publication: caller-controlled catalog bias and cross-manager idempotent replay. It also corrected exact live session-ID propagation and reduced candidate Rules evaluation cost below Firestore's valid-write expression ceiling without dropping the protected Shared Setup authority facts. Production Shared Setup remains intentionally NOT activated by PR201.

Production application/runtime remains `v1.9.1 / 1.9.1-r2`; previous whole-shell recovery remains `1.9.1-r1`. RJR-1 remains COMPLETE/FROZEN at `100/100`. The previously accepted RJR physical proof used two physical devices and two independent networks and is consumed unless a proven regression invalidates it.

## SSJR authority

Fixed model: `SSJR-1.1`.
Current readiness: `0/100`.
Estimated focused sessions to genuine SSJR100: `~6–11`.

PR201 adds real deterministic/provider-enforcement candidate evidence, but candidate code, emulator proof, PR, CI, merge, deployment mechanics, WEC, SLE/SNS and documentation do not satisfy the required production-two-account layer. Therefore the score remains 0/100.

The locked journey order remains:

Profiles/pre-draw Save shell → exact Connected Rivalry pairing → exact ACTIVE session → shared setup open → one authoritative league draw → two distinct permanent same-league clubs → 1/3/5/10 season length → both managers confirm identical setup → career-start acknowledgements → transfer/results/season commit → authoritative scoring/history/progression → recovery/conflict safety → final reconciliation → CLOSED / no resurrection.

Pairing and exact ACTIVE session must exist before any league or club selection in both provider authority and final UI. A fresh ACTIVE session for the same rivalry resumes the existing Shared Setup and never resets the competition.

## IMMEDIATE NEXT TASK AFTER FULL STUDY

Initialize a fresh unique WEC from current live `main`; validate/archive closing WEC `we-2026-09-05-ssjr-provider-adapter-a48` and never inherit its HANDOFF decision.

Then execute the first score-enabling runtime milestone: wire the PR201 Shared Setup provider candidate into the production browser journey only after exact Connected Rivalry pairing and an exact ACTIVE session, and prepare the smallest safe Spark-only production Rules promotion for the exact Shared Setup path.

Ordered gates:

1. Reverify PR201 post-merge/main evidence, production `v1.9.1 / 1.9.1-r2`, RJR100 and SSJR0.
2. Study `js/sparkSharedShowdownSetup.js`, `js/sharedShowdownCatalog.js`, `firestore.shared-setup-candidate.rules`, the current auth/session/runtime adapters and the main menu/setup screens.
3. Integrate a paired-first Shared Setup entry so fresh managers cannot spin/select league or clubs before Connected Rivalry + ACTIVE. Existing local-only play must remain safe and usable outside protected remote mutations.
4. Promote only the proven Shared Setup Rules delta into the zero-billing production Rules source after exact emulator/contract review; do not weaken existing production Rules.
5. Automate browser/provider tests for both managers reaching the same empty setup, one immutable league outcome, distinct same-league permanent clubs, supported season length, dual confirmation, reload/reconnect/fresh-session resume, stale/replay/race negatives and modified-client bypass attempts.
6. Publish with exact-head CI/review/expected-head gates and obtain production two-account evidence before awarding any SSJR capability.

Do not broaden this milestone into transfer/results/scoring transport until the shared-entry/shared-setup production evidence boundary is proven. Do not ask the owner to repeat the consumed generic RJR physical acceptance.

## Permanent locks

Billing must never be activated. Firebase remains Spark. Never link Cloud Billing, enable Blaze, add a payment method, use purchased credits, Cloud Run, Cloud Functions or another billing-required service. App Check enforcement remains OFF. Firestore browser persistence remains memory-only. Google Auth remains popup-only `browserSessionPersistence` with no extra scopes.

Canonical localStorage remains exactly `careerModeShowdown.saveLibrary`, `careerModeShowdown.legacyShowdowns`, `careerModeShowdown.preferences`. Candidate A is non-mutating; Candidate B is read-only; Candidate C is the sole destructive remote-to-local gameplay Apply authority with transaction-owned strict exact raw-snapshot rollback. Exactly two private managers. No public discovery/listing/lobby/matchmaking/community/rankings/global leaderboards. Never durably retain raw private capabilities or raw account/device/rivalry/session authority IDs.

## Mandatory reporting

Every substantive current and future project session uses exactly:

```text
Handoff proximity: X%
Shared Showdown Journey readiness: Y/100
Estimated focused sessions to genuine SSJR100: ~N–M
Current lane: ...
Concrete dependency completed: ...
Next unlock: ...
Blocker: ...
Sidequest check: NONE
```

At Handoff proximity 100%, recursively generate the complete SLE package, archive/finalize the current WEC, refresh current starter/handoff/bootstrap/context pointers, give the owner one short repository-first successor prompt, and stop before the next substantial milestone.
