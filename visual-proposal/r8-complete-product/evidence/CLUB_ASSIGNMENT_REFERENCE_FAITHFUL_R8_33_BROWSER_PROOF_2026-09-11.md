# Club Assignment — R8.33 Reference-Faithful Browser Proof

Status: `ASSEMBLED_PROPOSAL / BROWSER-GEOMETRY PASS / OWNER VISUAL REVIEW PENDING / NOT PRODUCTION`

## Scope

This proof covers the R8.33 owner-reference-faithful Club Assignment presentation consumer:

`prototypes/04e-club-assignment-reference-faithful-r8-33.html`

It is a visual/proposal consumer only. The production reveal and persistence authority remains the existing Club Assignment runtime.

## Owner target represented

The assembled R8.33 screen moves the browser implementation materially closer to the supplied Club Assignment reference:

- CM17 black/gold top shell;
- gold brush-like `CLUB ASSIGNMENT` title;
- league-confirmed / two-packs-ready status line;
- 01 DRAW / 02 PACK 1 / 03 PACK 2 / 04 VS / 05 LOCK progress rail;
- Manager 1 Daniel left and Manager 2 Nik right on wide desktop;
- handwritten gold manager labels and traits;
- two real DOM black/gold pack surfaces;
- central gold rivalry `VS`;
- lower locked-club rivalry deck;
- dominant gold Open/Confirm action plus dark Back action;
- procedural stadium, crowd, floodlight, banner and pitch atmosphere;
- compact CM17 footer identity.

No whole-screen generated image is used as implementation.

## Responsive browser matrix

Local Chromium was run against the assembled consumer at:

| Viewport | Motion | Horizontal overflow | Pack/action overlap | Character treatment |
| --- | --- | --- | --- | --- |
| 1366×768 | normal | PASS — none | PASS — disjoint | Daniel/Nik cinematic layers visible |
| 1440×900 | normal | PASS — none | PASS — disjoint | Daniel/Nik cinematic layers visible |
| 940×700 | reduced | PASS — none | PASS — disjoint | large character art removed |
| 390×844 DPR2 | normal | PASS — none | PASS — disjoint | character-free mobile composition |

No page-script errors were observed in this matrix.

## Production-main reconciliation

Production `main` independently resolved to:

`35843a13fd1ecfa3e8f1c91ef4c09a06584ba029` / runtime `1.9.1-r18`.

Compared with the prior r17 visual anchor `e624d19e04c0ca56f33fa7f0d25fdcc42eb99eda`, main is two commits ahead. The r18 work is Terminal Close / deployment-guard work. Current `index.html` still exposes the same Home, League Wheel and Club Assignment DOM roles needed by the visual proposal, including the existing five-option League Wheel and the current Club Assignment reveal shell/progress/pack/confirmation controls.

R8.33 does not change production `main`.

## State-authority boundary

R8.33 `04e` is the owner-reference-faithful presentation target. It does **not** replace the existing six-state Club Assignment authority already proven by the predecessor consumers:

`ready -> opening -> manager-one -> manager-two -> versus -> confirmation`

The senior production integration must map the R8.33 visual shell onto those current live states rather than ship the proposal's small demonstration clock as a second reveal engine.

## Deliberate remaining fidelity limitation

The current reusable character masters are the approved/verified Daniel pointing pose and Nik thinking pose. They reproduce the scale, lighting, manager placement and rivalry framing, but they do not yet literally grip the DOM pack surfaces the way the owner reference depicts.

That difference is visible and intentionally recorded. If owner review requires literal pack-holding contact, the next step is a **bounded isolated character-source ticket** for each manager, composited back into this exact DOM geometry. The webpage itself must not be regenerated as a picture.

## Verdict

R8.33 Club Assignment is ready for owner visual review as an `ASSEMBLED_PROPOSAL`.

Geometry/responsive gate: **PASS**.

Production integration authorization: **NOT GRANTED**.

Owner visual approval: **OPEN**.
