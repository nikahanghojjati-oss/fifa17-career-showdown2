# START HERE — GPT-5.6 Sol Visual Master Control

This file is intentionally short.

Before any new visual generation or proposal implementation in the Showdown visual track:

1. Read `evidence/SOL_VISUAL_ORCHESTRATION_MASTER_GUARD_2026-09-10.md`.
2. Independently resolve current production `main` and the current visual branch head.
3. Preserve proposal containment under `visual-proposal/r8-complete-product/` unless the owner explicitly authorizes production implementation.
4. Treat reference uploads as evidence, not automatic image-generation triggers.
5. Sol must explain the exact asset/goal before invoking image generation.
6. Keep Manager 1 = Daniel and Manager 2 = Nik.
7. For Club Assignment, preserve the Mode B2 protected live-pack aperture. Do not let character hands overlap the authoritative pack.
8. For club identities, read `evidence/CLUB_IDENTITY_V2_1_SAFE_CONTEXT_ARCHITECTURE_2026-09-10.md` and use deterministic SVG descriptors. Do not AI-generate the 98 production badges as raster art.
9. Keep Club Identity research/history/location data out of runtime state, Firebase, Firestore, Auth, Shared Journey, saves and provider payloads.
10. Preserve the permanent zero-dollar/Spark architecture and do not enable billing.
11. Generate character/cinematic art only after the relevant DOM/prototype geometry and asset ticket are READY.
12. When context pressure threatens reliable continuity, stop new generation and create a safe-transfer handoff with exact heads, commits, open risks and next action.

Compact operating loop:

`RESOLVE -> EXPLAIN -> BUILD/GENERATE -> QA -> RECORD -> NEXT`

Image generation is a subordinate renderer. GPT-5.6 Sol remains the workflow operator.
