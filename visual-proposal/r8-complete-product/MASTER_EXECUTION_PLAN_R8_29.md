# R8.29 Visual Master Execution Plan

Status: ACTIVE MASTER PLAN / PROPOSAL ONLY / NOT PRODUCTION

## Non-negotiable operating authority

GPT-5.6 Sol remains the operator. Image generation is a subordinate renderer. Reference uploads are evidence, never automatic generation triggers. Production is not modified by this plan.

Permanent invariants: Manager 1 = Daniel; Manager 2 = Nik; Firebase Spark only; Billing OFF; no paid fallback; no public discovery/community/rankings; proposal work remains under `visual-proposal/r8-complete-product/` until explicit implementation authority.

## Current production reconciliation

Current production main observed during this session:

`23bba67ed7bf9cc38cb7dc9106e3f64a17a6b55f` / runtime `1.9.1-r17`.

r17 adds Final Reconciliation as a read-only completed-Showdown projection under existing Season Review ownership. It creates no new write authority. The visual proposal must reconcile that panel before final owner review.

## Master dependency order

1. Club Identity V2.1 authoring catalog for all 98 supported 2016-17 clubs. `TECHNICAL AUTHORING COMPLETE`.
2. Aggregate descriptor/schema/uniqueness/no-network/no-Firebase/no-persistence validation. `R1 PASS RECORDED / R2 EXECUTION OPEN IN FILE-CAPABLE ENVIRONMENT`.
3. Deterministic SVG renderer and 98-club perceptual review. `R1 FULL SHEET COMPLETE / SEVEN R2 PRIORITY OVERRIDES LOCAL-QA PASS / FULL 98 R2 SHEET OPEN`.
4. Freeze Club Assignment Mode B2 live-pack aperture and safe character geometry with production-derived DOM/CSS placeholders. `PROPOSAL GEOMETRY LOCKED AT >=1320PX TARGET / SENIOR BROWSER PROOF OPEN`.
5. Resume character system in dependency order: resolve A03/A04 authority if still open, then A05/A06 from measured B2 geometry, A07/A08 focused, A09/A10 victory, A11/A12 setback. One manager/role per uncertain generation lane.
6. Integrate character roles into exact screen contracts only where product-fit passes; utility/recovery/settings/rules stay character-light or character-free.
7. Reconcile all production-visible states through r17, including Final Reconciliation inside Season Review.
8. Finish Audius zero-dollar/device/rights proof or reject it cleanly; no autoplay and no paid fallback.
9. Refresh routed and non-route prototypes/screens in product journey order; preserve existing DOM/state authority.
10. Run structural/browser/visual QA, responsive checks, reduced motion, accessibility, no-overflow and asset provenance checks.
11. Produce complete final screenshot/contact-sheet/media package for explicit owner approval.
12. Only after explicit owner approval seal the senior-developer implementation handoff.

## Club Identity V2.1 gate

Research/history/location data is authoring-only. Runtime receives only compact deterministic visual descriptors derived from the canonical club name.

The badge path must require:

- zero Firestore reads/writes;
- zero Firebase Storage reads/writes;
- zero Auth dependency;
- zero Cloud Functions;
- zero external image/API requests;
- zero runtime hotlinks;
- zero save/schema migration;
- zero billing dependency.

The renderer uses original SVG geometry, club-associated colors and broad factual/history/place cues. It must not trace official crest geometry, wordmarks, civic heraldry, sponsor/league marks, or distinctive protected emblem arrangements.

Current state:

- 98/98 club research/history/place direction complete;
- 98/98 base descriptor drafts complete;
- runtime descriptor contract authored;
- deterministic SVG reference renderer authored;
- aggregate validator authored;
- R1 full 98-club contact sheet produced;
- seven priority R2 corrections authored for Athletic Club, Sporting Gijón, Bayern Munich, Inter Milan, Napoli, Nice and Paris Saint-Germain;
- seven R2 priority identities locally reconstructed and perceptually reviewed: PASS, with Inter and PSG retained on the final small-size watch list;
- no R3 justified without new defect evidence;
- full 98-club R2 contact sheet remains open;
- owner approval remains open;
- production implementation remains blocked.

Evidence:

`evidence/CLUB_IDENTITY_V2_1_R1_R2_PERCEPTUAL_QA_2026-09-10.md`

The external HTML renderer is currently exhausted/over its included allowance with overages disabled. The zero-dollar rule prohibits enabling billing merely to rerender the sheet. Continue technical/local proof and defer the consolidated external R2 sheet until an included route exists.

## Club Assignment Mode B2 gate

The authoritative `.clubPackStage` / `.clubPackDoor` / `.clubCardFace` remains the only pack/reveal object.

The r17 production-derived short-desktop geometry at 1366x768 supports a safer exterior-flank composition:

- centered shell approximately 900px;
- reveal area approximately 820px;
- each reveal card approximately 372x220px;
- effective pack stage approximately 356x180px;
- protected pack aperture uses a target 14px safety gutter;
- character and hand regions stay wholly outside the protected aperture;
- hand may terminate only at an external static rail or use an open gesture.

Revised responsive rule:

- `>=1320px`: Mode B2 may be enabled after browser proof;
- `1180-1319px`: character-free by default, Mode A only if separately proven;
- `<=1179px`: character-free;
- reduced motion: character/frame decoration removed;
- mobile: character-free and existing stacked live-pack layout preserved.

Proposal prototype now includes bounding-box non-intersection and horizontal-overflow checks:

`prototypes/04b-club-assignment-mode-b2-structural-prototype.html`

Evidence:

`evidence/CLUB_ASSIGNMENT_B2_R17_GEOMETRY_PROOF_2026-09-10.md`

If the senior runtime/browser proof fails, Mode A or character-free presentation wins automatically. The product must never move merely to rescue artwork.

## Character lane next action

Before any A05/A06 generation, Sol must resolve the final asset ticket from the B2 geometry:

- exact manager;
- exact side/flank;
- upper-body crop envelope;
- hand termination / static-rail relationship;
- transparent safe region toward the live pack;
- identity reference hierarchy;
- excluded references;
- no generated pack object;
- no baked club/manager/CM17 text;
- face-first QA threshold.

No additional user reference photos are currently required. Ask only if a specific identity/angle deficiency appears during a controlled single-role attempt.

## r17 reconciliation lane

Before final screenshot review, represent the actual r17 Final Reconciliation panel under existing Season Review ownership. Preserve its read-only nature, exact manager/profile/save/rivalry binding, Candidate-C separation and zero-billing boundary. Do not create a new route.

## Media lane

Audius Showdown Radio remains the locked default proposal direction: one native HTML `<audio>` authority, no autoplay, stream assignment only after deliberate Play, no YouTube music fallback and no paid fallback.

Real iPhone Safari + Chromebook playback and final per-track rights/taste proof remain open. Do not fabricate device evidence.

## Transition trigger

Do not transition merely because a milestone finishes. Transition when context pressure, drift risk, tool instability, or accumulated unresolved authority would make continued work less reliable.

Before transition:

1. independently re-resolve production main and visual branch head;
2. verify proposal containment;
3. record all completed work, exact commits, rejected directions and open risks;
4. identify the exact next action rather than a vague roadmap;
5. create one safe-transfer handoff document;
6. tell the owner that transition is recommended.

Compact operating loop remains:

`RESOLVE -> EXPLAIN -> BUILD/GENERATE -> QA -> RECORD -> NEXT`.
