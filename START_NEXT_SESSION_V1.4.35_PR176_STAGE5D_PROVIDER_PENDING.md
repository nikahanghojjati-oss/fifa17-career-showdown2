# START NEXT SESSION — v1.4.35 / PR #176 / RJR87 / Stage 5D Provider Pending

SLE = Smart Lean Efficient. Work Environment Continuity (WEC) remains mandatory.

This starter is orientation only. Current source, live GitHub/provider/deployment evidence, `REMOTE_JOINING_READINESS.json`, `NEXT_TASK.md`, `PROJECT_STATE.md`, the successor's fresh WEC and later owner instructions win.

## Verified boundary

Repository: `nikahanghojjati-oss/fifa17-career-showdown2`.

Public site: `https://nikahanghojjati-oss.github.io/fifa17-career-showdown2/`.

PR #176 `Promote minimum production private-session Rules source` is merged. Source branch: `rjr/stage5d-minimum-production-session-rules-2026-09-01`. Starting main: `e0445ebf214b9385667187e0e580bba497d8f039`. Exact final reviewed head: `4c1e9be8e0af26e277ed9fd1ae0545ec065173ff`. Expected-head squash merge/live-main checkpoint: `a4489fe7d812144deb3f747019eb162628480dac`. Merge tree: `e7083c2cda0e737f9d1c5654ca663df6ddf3408a`.

All 14 permanent exact-head workflow families passed on the final unchanged PR head. Exactly 15 normal post-merge/Pages workflow runs then completed successfully on the merge, with zero failures and zero runs left in progress.

Production remains `v1.8.1 / 1.8.1-r5`, DEPLOYED / PRODUCTION-PROVEN. PR #176 did not load the private-session adapter, expose host/join UX or change runtime assets.

Repository `firestore.spark.rules` is now the reviewed Stage 5D source blob `363af783d7e5436fdfaa3766d4aa413fc9952a08`.

Provider truth remains deliberately separate. The last independently Firebase-provider-proven production Rules blob is still `2b7c0b166ae0aae7ab7a3ce84725b21091262484`. The new repository source has NOT yet been independently proven provider-live.

Remote Joining readiness remains `87/100` under fixed RJR-1. Source promotion, CI, review, merge, Pages and provider-publication mechanics receive zero capability credit by themselves.

## Stage 5D result

The merged production Rules source promotes the already-proven Stage 5C standard-auth private-session boundary into `firestore.spark.rules` while preserving surrounding production protections.

The exact session boundary keeps ordinary Firebase `request.auth.uid` account authentication, opaque exact capability paths, no list/discovery, exactly two entitled rivalry accounts, active account and rivalry rechecks, host-only open, peer-only join, immutable membership and authority, monotonic CAS, bounded expiry/close/revoke, terminal no-resurrection, account-bound mutation metadata, active registered-device mutation metadata, physical delete denial and fail-closed provider behavior.

Registered device IDs remain account-owned mutation metadata. They are not authentication, cryptographic browser possession or physical-device proof.

PR #174 Stage 5B device credential candidate is preserved dormant research: it proved a non-extractable P-256 browser credential with per-sign-in `device_id` custom-token claims, Chromium/browser execution, Auth-plus-Firestore emulator acceptance, simultaneous same-uid device isolation and atomic credential revocation. That consumed emulator evidence remains historical proof and must not be repeated merely for confidence.

The Stage 5B non-extractable P-256 custom-token research remains preserved but is not the production critical path because its trusted Cloud Run issuer would require billing. The production critical path remains standard Google Authentication plus direct exact-path Firestore Rules on Firebase Spark.

## Provider publication boundary

Repository source promotion is not Firebase provider publication evidence.

The only intended provider mutation is the already-reviewed zero-billing Spark-compatible Rules deployment:

`firebase deploy --only firestore:rules --project fifa17-career-showdown-prod --config firebase.production.rules.json`

Execute that command only from an authenticated Firebase control-plane route already able to access the production project while remaining on Spark. After deployment, independently verify that the live provider Rules source is exact blob `363af783d7e5436fdfaa3766d4aa413fc9952a08` before updating any provider proof or manifest truth.

If no authenticated provider route exists, preserve the blocker honestly. Do not fabricate provider state and do not activate billing to obtain access.

The closing environment found no Firebase or Google Cloud connector/plugin, no repository Firebase Rules deployment workflow, no `FIREBASE_TOKEN`, no `GOOGLE_APPLICATION_CREDENTIALS`, no workload-identity-provider configuration and no GitHub integration route capable of exposing repository secret APIs. This is a control-plane access boundary, not an engineering or owner-authorization blocker.

## Controlling owner authorization and permanent locks

Read `00_OWNER_ZERO_BILLING_REMOTE_JOINING_AUTHORIZATION.md`, `ZERO_BILLING_REMOTE_JOINING_ARCHITECTURE_DECISION_2026-08-31.md` and `00_OWNER_STANDING_MERGE_DEPLOY_AUTHORIZATION.md`.

The owner authorizes every remaining nonbilling Remote Joining engineering, IAM, provider, authentication-policy, Security Rules, runtime, deployment, testing, evidence, merge and publication decision needed after the required tests and gates pass. Standing merge and deploy authorization remains active; do not ask again for already-granted nonbilling authority.

Billing must never be activated. Never link Cloud Billing, enable Blaze, add a payment method, activate Cloud Run, buy review credits or activate another service whose use requires billing even if it advertises a free allowance. Firebase must remain Spark.

App Check enforcement remains OFF. Firestore persistence remains memory-only. Google Auth remains popup-only `browserSessionPersistence` with no extra scopes.

Exactly two private managers remain mandatory. Session listing, public discovery, lobbies, community, matchmaking, global rankings and leaderboards remain prohibited.

Canonical localStorage remains exactly `careerModeShowdown.saveLibrary`, `careerModeShowdown.legacyShowdowns`, and `careerModeShowdown.preferences`; `activeShowdown` is not canonical storage. Candidate A remains non-mutating export, Candidate B remains read-only import analysis, and Candidate C remains the sole destructive remote-to-local Apply authority with strict transaction-owned raw-snapshot rollback. The protected historical rivalry must not be used for destructive testing.

Spark quota or provider failure must fail closed while local-first play remains available and must never trigger a plan upgrade or charge.

## WEC boundary

Closing environment: `we-2026-09-01-stage5d-minimum-production-session-rules`.

Its independently observed starting main was `e0445ebf214b9385667187e0e580bba497d8f039`. Its bounded product work is complete: PR #176 source proof, exact-head CI, zero-billing review fallback, expected-head merge and all 15 post-merge/Pages gates are complete.

Its final decision is `HANDOFF_AT_CHECKPOINT` only after the recursive SLE package is mirrored, current pointers are refreshed, applicable SLE/WEC contracts pass, the transition package is published and the final WEC seal is the last intended branch mutation.

The successor must validate and archive this WEC, initialize a fresh unique WEC from independently observed live main with every per-environment counter reset, run `npm run work:assess`, and obey its own assessment. Never inherit this environment's transition decision or counters.

## IMMEDIATE NEXT TASK AFTER FULL STUDY

1. Independently verify current `main`, PR #176 closure, exact final head `4c1e9be8e0af26e277ed9fd1ae0545ec065173ff`, squash merge `a4489fe7d812144deb3f747019eb162628480dac`, merge tree `e7083c2cda0e737f9d1c5654ca663df6ddf3408a`, all 15 post-merge/Pages successes, public runtime `1.8.1-r5`, fixed RJR87, repository Rules blob `363af783d7e5436fdfaa3766d4aa413fc9952a08` and last provider-proven Rules blob `2b7c0b166ae0aae7ab7a3ce84725b21091262484`.

2. Validate/archive closing WEC `we-2026-09-01-stage5d-minimum-production-session-rules`; initialize a fresh unique successor WEC with reset counters and independent assessment.

3. If an authenticated zero-billing Firebase production route exists, publish only the exact reviewed Stage 5D Rules with the command above and independently prove provider source identity equals `363af783d7e5436fdfaa3766d4aa413fc9952a08`. Keep Firebase Spark, App Check enforcement OFF and Firestore memory-only.

4. If authenticated provider access is unavailable, record the exact access blocker and stop that operation. Never activate billing, buy credits or fabricate publication.

5. Only after provider-live Rules proof begin the separate minimum runtime host/join UX milestone. Then pursue provider-live two-account host/join, real two-device/two-network reconnect/token/adverse-network hardening and final stable release acceptance until fixed RJR genuinely reaches 100.

Do not repeat consumed pairing, Candidate C, replay, adverse-provider, token-lifecycle, structural-abuse, sustained-rate-limit, rollback/restoration, earlier provider-Rules or provider-abuse proof merely for confidence.

## Recursive SLE and reporting

The normal owner entrypoint is one short repository-first next-developer prompt generated with `npm run work:next-prompt`. It must point to this starter and the mirrored deep handoff.

Every substantive owner update uses exactly:

Handoff proximity: X%
Remote Joining readiness: ~Y%
Estimated focused sessions to genuine RJR100: ~N–M
Current lane: ...
Concrete dependency completed: ...
Next unlock: ...
Blocker: ...
Sidequest check: ...

At Handoff proximity 100%, complete the byte-identical mirrored SLE handoff and versioned starter, refresh `SESSION_BOOTSTRAP.json` and progressive context, run applicable SLE/WEC contracts and `npm run work:next-prompt`, make the final WEC seal the last intended branch mutation, publish and verify the transition, provide the short repository-first prompt and stop before another substantial milestone.

Every successor recursively preserves SLE, WEC, fixed RJR evidence discipline, zero-billing authority, standing gated merge/deploy authorization, the repository-first prompt delivery rule and this exact reporting format.
