# FIFA 17 Career Mode Showdown — PR #125 Spark Rules Source Reseal — SLE Successor Handoff

SLE = Smart Lean Efficient.

This is the complete deep-reference successor handoff for the Work Environment transition after a fresh successor independently revalidated PR #125 through the production Spark Rules publication boundary. Treat this file as orientation only. Current source, live GitHub state, current Firebase/provider state, later owner instructions, security/recovery authority and the successor's own freshly initialized WEC always win.

Repository: `nikahanghojjati-oss/fifa17-career-showdown2`
Public site: `https://nikahanghojjati-oss.github.io/fifa17-career-showdown2/`
Closing environment: `we-2026-08-21-pr125-spark-rules-publication`
Closing branch: `agent/spark-production-account-runtime`
Current PR: #125 `Ship Spark private connected account runtime`
Live main at closure: `82413e36cd70bb10e332cb2aaa137ad350f2d241` from merged PR #124
Original PR #125 product/source-validation seal: `d83a33066b271d7d89bf932f1066d9e1369b3f6d`
Fresh-successor exact source revalidation head: `45e6d51ea5636c4fdf0c76eefe32f9b8908463d6`

## Transition decision

The closing environment's deterministic WEC decision is `HANDOFF_AT_CHECKPOINT` after the fresh-successor exact-head source revalidation checkpoint completed. This decision applies only to the closing environment and must not be inherited as the successor's own decision.

The closing environment must stop before production Firebase Rules publication, merge/deploy or another substantial milestone. The next successor must independently verify live state, recognize this predecessor closure as historical evidence, initialize a fresh unique WEC with reset per-environment counters and the newly observed live-main SHA, run its own assessment, and obey its own result before provider publication.

Usage allowance is unavailable and must remain `null` / `unavailable` unless an approved source reports it. Never estimate or fabricate usage.

The final strengthened checkpoint model is based on observable evidence only: high context complexity, very-high project complexity, zero compactions, two materially completed phases, four dense evidence events, two tool-routing errors, two corrected continuity-record failures, zero repeated mistakes, zero stale-fact corrections, zero unresolved failures, and a distinct provider-publication milestone next. With completed SLE packaging, the deterministic model requires handoff at this checkpoint.

## What this fresh environment actually did

The fresh successor did not inherit the predecessor's transition decision. It independently reconstructed live state and initialized `we-2026-08-21-pr125-spark-rules-publication` with reset counters.

At entry, live `main` remained `82413e36cd70bb10e332cb2aaa137ad350f2d241`, while PR #125 had moved from original source seal `d83a33066b271d7d89bf932f1066d9e1369b3f6d` to SLE transition-packaging head `9da981f88ccfe091832ed54965e1a95f4836ff9c`.

The successor compared that delta and verified it contained only SLE/WEC/session packaging and the SLE packaging contract. No runtime, product, Firebase Rules or security implementation file changed after the original product/source seal.

The successor then created its own WEC. Two process-record defects were exposed by exact-head CI and corrected transparently:

1. The first fresh WEC wording failed the interruption-resilience contract because it did not express the predecessor `HANDOFF_NOW` decision using the repository's required inherited-predecessor historical marker.
2. After that correction, the Stage 2I continuity contract failed because `continuity.currentTask` omitted the literal `connected-account` label required to preserve the active PR #125 private account boundary.

Both failures were WEC wording only. They did not change product/runtime/security/Firebase Rules behavior. The final corrected WEC-only head is `45e6d51ea5636c4fdf0c76eefe32f9b8908463d6`.

A fallback local Git clone attempt could not resolve `github.com` from the sandbox, so the successor correctly stayed connector-first instead of repeatedly retrying a broken route. During SLE packaging, the first attempt to create this new handoff used the existing-file update action and was rejected because a new-file create action was required; the route was immediately corrected. These are recorded as two tool-routing errors, not product defects.

## Current production truth

Current production application: `v1.4.0 — Product Deepening`.
Current production Installable Offline App runtime: `1.4.0-r2`.
PR #125 candidate immediate whole-shell recovery target: `1.4.0-r2`.
Older fallback knowledge: `1.4.0-r1`.

The production Firebase App Check chain through PRs #115–#119 is DONE / MERGED / PROVEN. Permanent production proof is `Validate Stability Lane` #1230, run ID `32439162225`, on exact production head `3d2ebefec683e0b3bf6b2beac08d54f1c3d9e516`. Its `stability-contracts`, `chromium-stability` and `deployed-site-smoke` jobs succeeded.

Legitimate production App Check token traffic is proven. App Check enforcement remains `OFF`.

The deployed `1.4.0-r2` browser intentionally initializes only Firebase App and Firebase App Check. Production browser Authentication, Firestore, Storage and Functions remain uninitialized. Application-client Firestore create/update/delete remains `deny-all` under the currently published production rules.

## PR #125 candidate truth

Candidate application/runtime: `v1.5.0 / 1.5.0-r1`.
Status: RELEASE CANDIDATE / NOT PRODUCTION-PROVEN.

The candidate remains zero-billing and local-first. It may lazily initialize Firebase Authentication plus memory-only Cloud Firestore only after explicit Connected Account demand.

The authorized provider path remains:

- Google `signInWithPopup()` only;
- explicit `browserSessionPersistence` before sign-in;
- Firebase `uid` as the sole application `accountId` source;
- no additional Google OAuth scopes;
- no provider credential extraction;
- no redirect sign-in;
- no Storage or Functions client;
- no Blaze/billing, Cloud Run, Cloud Functions or trusted backend credential path;
- local Saves/gameplay/recovery continue working signed out, offline, popup-cancelled or Firebase-unavailable.

The exact reviewed production-activation rules candidate remains `firestore.spark.rules`.

It permits one new application-client write shape only: authenticated strict revision-0 self-account create at `accounts/{request.auth.uid}`. Account list/update/delete plus device, profile-link, security-event, invite, rivalry, session, idempotency and gameplay mutations remain denied.

`firestore.spark.rules` is NOT yet published to production. Therefore `v1.5.0 / 1.5.0-r1` is NOT production-proven and PR #125 must not merge yet.

## Fresh-successor exact-head revalidation proof

The original immutable product/source seal remains `d83a33066b271d7d89bf932f1066d9e1369b3f6d`, where all 13 normal pull-request workflow families had already succeeded.

The fresh successor revalidated the later process-only branch delta and froze exact corrected head `45e6d51ea5636c4fdf0c76eefe32f9b8908463d6`.

All 13 normal pull-request workflow families succeeded on that exact unchanged head. Key current runs:

- `Validate Static App` #1580 / run ID `32511770971`: SUCCESS. The full repository contract suite and permanent workflow topology passed, including the repaired fresh-successor WEC, Stage 2I account boundary, production Firebase authority, recovery, SLE and RJR contracts.
- `Validate Stability Lane` #1334 / run ID `32511771122`: `stability-contracts` SUCCESS, trusted runtime activation container build SUCCESS, Firebase Admin import smoke SUCCESS and `chromium-stability` SUCCESS. `deployed-site-smoke` was correctly skipped on the pull request and is not production proof.
- `Validate Candidate C Atomic Restore` #968 / run ID `32511770837`: restore contracts SUCCESS, authoritative browser restore/recovery audit SUCCESS and evidence upload SUCCESS.
- `Validate Candidate B Import Analysis` #1020: SUCCESS.
- Settings Workstream #1461: SUCCESS.
- V1 Visual Immersion #1409: SUCCESS.
- Home Bootstrap #1440: SUCCESS.
- Transfer Workstream #1480: SUCCESS.
- Season Review #1431: SUCCESS.
- Statistics Workstream #1448: SUCCESS.
- Final Polish #1414: SUCCESS.
- League Confirmation #1421: SUCCESS.
- Licensed Football Visuals #1381: SUCCESS.

At the fresh source revalidation freeze, PR #125 was open, non-draft and mergeable. Submitted reviews: zero. Inline review threads: zero. Live main was still `82413e36cd70bb10e332cb2aaa137ad350f2d241`.

Do not treat pull-request source CI as deployed production proof.

## Why the closing head will move again after the green revalidation head

SLE packaging itself creates process-only transition mutations after `45e6d51ea5636c4fdf0c76eefe32f9b8908463d6`: this new full handoff, its byte-identical project mirror, the new starter and mirror, refreshed `SESSION_BOOTSTRAP.json`, materially refreshed `SESSION_CONTEXT_GRAPH.json`, the SLE packaging contract pointer update, and the final transition-prepared WEC seal.

Those packaging commits do not change the v1.5 runtime, Firebase client behavior, `firestore.spark.rules`, security authority or production state. A successor must still independently inspect the final PR head and applicable workflows before provider publication.

Do not reopen already solved historical-contract work without a concrete current failure.

## Provider-publication route status

This closing environment exhausted its currently supported direct publication discovery before crossing the WEC boundary:

- the repository production Firebase alias exists as `fifa17-career-showdown-prod`;
- `firebase.json` still points its default Firestore deploy configuration at `firestore.rules`, the currently published deny-all source, not the separate reviewed `firestore.spark.rules` candidate;
- `package.json` exposes no production Firebase Rules deployment script;
- the repository's existing workflows expose no dedicated Firebase Rules publication workflow;
- the connected GitHub tool does not expose an authenticated Firebase Console/provider write operation;
- plugin discovery exposed no Firebase publication integration available to this environment.

These observations do not authorize a manual hand edit or a repository configuration shortcut. The next successor must recheck direct provider routes because tool capabilities can change. If no authenticated direct route is available, guide the owner through the exact Firebase Console publication of the repository's byte-for-byte `firestore.spark.rules` and require observable provider proof.

Do not publish `firestore.rules`; it is the old deny-all source. Do not modify `firestore.spark.rules` in the Console. Publish its exact repository content only.

## Remote Joining readiness

Machine-readable authority: `REMOTE_JOINING_READINESS.json`.
Model: `RJR-1` fixed 100-point denominator.
Current score: `61/100`.

No readiness points were added for the fresh WEC, source revalidation, green CI, SLE packaging or unpublished Spark Rules. Those are process/source evidence, not a newly proven production capability.

Do not change RJR until genuine new production evidence closes a fixed RJR-1 capability gap.

## Permanent security, privacy, storage and recovery locks

Private Remote Joining remains `PRIORITIZED LONG-TERM / DEPENDENCY-GATED / NOT YET IMPLEMENTATION-AUTHORIZED`.

Dependency order remains:

Cloud / synchronization readiness
→ private account / authentication / authorization and production operational trust
→ paired-device / private-session capability
→ Connected Rivalry
→ Private Remote Joining
→ end-to-end hardening / stable release.

Stage 3 Registered Devices / Private Pairing remains blocked. Connected Rivalry and actual Remote Joining remain downstream.

Public community features and global leaderboard/rankings remain ELIMINATED. Public discovery, public profiles, public matchmaking, public invitation directories and public lobbies remain prohibited.

Production App Check enforcement remains OFF. App Check remains attestation only; it grants no account identity, application authorization, device identity, pairing authority, rivalry/session entitlement, gameplay authority, shared mutation authority or IAM authority.

Stage 2H reviewed IAM remains exactly:

`firebaseauth.users.get`
`datastore.databases.get`
`datastore.entities.get`
`datastore.entities.create`

That trusted-runtime IAM remains unactivated and must not be broadened or activated inside PR #125.

Canonical browser storage remains exactly:

`careerModeShowdown.saveLibrary`
`careerModeShowdown.legacyShowdowns`
`careerModeShowdown.preferences`

`activeShowdown` is not canonical.

Candidate A remains non-mutating export. Candidate B remains read-only import analysis. Candidate C remains the sole destructive import Apply authority with strict raw snapshot authority, transaction-owned mutation, immutable confirmed intent, stale-state guards, ownership-scoped reverse rollback, anti-clobber behavior and exact recovery verification.

## Standing owner publication authorization

`00_OWNER_STANDING_MERGE_DEPLOY_AUTHORIZATION.md` remains active, with provenance under `authority-history/OWNER_STANDING_MERGE_DEPLOY_AUTHORIZATION_2026-08-20.md`.

Standing owner authorization permits merge/deploy without another repeated approval prompt only after every required test and current mandatory publication gate passes on the intended immutable head. A later explicit owner instruction can revoke or narrow this.

For PR #125, the production Spark Rules publication/verification gate is still incomplete. Therefore merge is not yet authorized by the gate conditions even though source CI is green.

## IMMEDIATE NEXT TASK AFTER FULL STUDY

The fresh successor must do this in order:

1. Use the newest `START_NEXT_SESSION_V1.4.2_PR125.md` as the owner entrypoint. Retrieve `SESSION_BOOTSTRAP.json`, verify live GitHub state and initialize a fresh successor WEC. Do not inherit this predecessor's `HANDOFF_AT_CHECKPOINT` decision.
2. Independently verify live `main`, PR #125 final head/state, changed files, current workflow results, submitted reviews, inline review threads and mergeability. Classify the post-`45e6d51ea5636c4fdf0c76eefe32f9b8908463d6` delta as SLE transition packaging only before provider publication. Re-run/follow applicable exact-head gates if current evidence requires it.
3. Recheck for a supported authenticated Firebase publication route. If one is available, publish EXACTLY the reviewed `firestore.spark.rules` to production project `fifa17-career-showdown-prod` without hand edits. If no direct route exists, give the owner one precise Firebase Console action and require observable proof.
4. Verify the production Rules boundary: authenticated self-account revision-0 create at `accounts/{request.auth.uid}` is the only newly permitted application-client write; every account list/update/delete and downstream device/profile/security-event/invite/rivalry/session/idempotency/gameplay mutation remains denied.
5. After Rules proof, re-freeze PR #125 exact head, workflows, reviews, threads and mergeability. Only after all required gates pass may standing owner authorization be used to merge/deploy without another redundant approval.
6. Obtain post-deployment production proof for `v1.5.0 / 1.5.0-r1`: deployed identity, local-first behavior, Connected Account flow, Google popup/session persistence, memory-only Firestore, strict self-account bootstrap, denied downstream writes, recovery/offline invariants and App Check state.
7. Only after genuine production proof may `1.5.0-r1` be promoted from RELEASE CANDIDATE to production-proven and RJR be reconsidered from fixed-model evidence.
8. Reassess the fresh WEC before any separate next prerequisite. If it permits continuation, select only the smallest remaining dependency-gated Stage 2 prerequisite justified by production evidence.

Do NOT enable App Check enforcement. Do NOT activate or broaden paid trusted Cloud Run/IAM. Do NOT begin Registered Devices, pairing, Connected Rivalry or actual Remote Joining UX in this lane.

## Continuity and history hazard

`WORK_ENVIRONMENT_HISTORY.md` is append-only. The connected GitHub route used here still exposes no safe append primitive for that large file. Missing archival entries must not be repaired by risking a destructive full-file rewrite. Preserve the known gap until a safe append-capable or separately verified complete-file route exists.

## Mandatory owner-facing progress format

Every substantive project response must end with exactly these seven lines, in this order:

`Handoff proximity: X%`
`Remote Joining readiness: Y/100`
`Current lane: ...`
`Concrete dependency completed: ...`
`Next unlock: ...`
`Blocker: ...`
`Sidequest check: ...`

Unknown usage must never be fabricated.

At `Handoff proximity: 100%`, finish only the current safe bounded checkpoint, complete SLE packaging and stop before another substantial milestone. WEC can require an earlier or stricter transition.

## Mandatory recursive SLE rule

SLE = Smart Lean Efficient.

Every successor must carry this rule forward. At every future handoff boundary, a plain chat-only successor prompt is not a complete project handoff. The closing environment must create a root `SUCCESSOR_HANDOFF_..._SLE_...md`, a byte-identical project mirror, a new versioned root `START_NEXT_SESSION_...md`, a byte-identical session-start mirror, refresh `SESSION_BOOTSTRAP.json` and materially changed progressive-context pointers, validate the package, make the transition-prepared WEC seal the last intended branch mutation when required, give the owner the newest starter, and stop before the next substantial milestone.

Current source, live GitHub/provider state and the fresh successor's own verification always win.
