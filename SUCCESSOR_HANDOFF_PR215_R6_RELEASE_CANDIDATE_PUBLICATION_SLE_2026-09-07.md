# SUCCESSOR HANDOFF — PR215 R6 RELEASE-CANDIDATE PUBLICATION — SLE — 2026-09-07

SLE = Smart Lean Efficient. This is the complete deep-reference handoff for the environment that reached Session Handoff Proximity 100 while PR215 remained an unmerged bounded r6 publication correction. It is orientation, not permission to trust stale facts. Current source, live GitHub/provider/deployment evidence, later owner instructions and the successor’s freshly initialized WEC always win.

## 1. Repository and product identity

Repository: `nikahanghojjati-oss/fifa17-career-showdown2`
Public site: `https://nikahanghojjati-oss.github.io/fifa17-career-showdown2/`
Application version: `v1.9.1`
Current source/runtime candidate: `1.9.1-r6`
Recorded production authority at the closing checkpoint: `1.9.1-r5` until the coherent r6 publication boundary is fully proven.

Billing must remain permanently OFF. Firebase must remain Spark. No action in this handoff authorizes Blaze, a Cloud Billing account, Cloud Run, Cloud Functions or paid infrastructure.

## 2. What is DONE / MERGED / PROVEN

### RJR

RJR-1 is COMPLETE/FROZEN `100/100`. Its physical Remote Joining acceptance is consumed and must not be repeated or re-credited absent a proven regression. The accepted physical proof used two physical devices on two independent networks. Evidence/continuity/publication work receives zero duplicate RJR credit.

### Historical Shared Setup authority

PR #203 is immutable production-proven `v1.9.1 / 1.9.1-r3` paired-first Shared Setup baseline authority.
PR #205 published the strict Shared Setup two-account evidence validator.
PR #207 published the privacy-safe stdin evidence recorder.
PR #209 published the bounded exact canonical-storage observer.
PR #210 made that observer deployable/cache-safe on Pages without changing the historical r3 runtime authority.

The production Rules historical authority remains ruleset `73b4435e-85a8-49f9-92ef-8ffe3ce0f91c`, generated-source blob `5bcde9297f6b2927a2184605192ab5b6cd46fb29`, unless live verification proves a later authorized replacement.

### PR214 / r6

PR #214 completed its exact-head publication gate and was expected-head squash merged. Recorded merged main SHA: `420d0dd21480c660d4fe139ba011eca1ccb987a9`.

On that exact main boundary:
- GitHub Pages build/deploy succeeded for the r6 static runtime.
- Release Integration Burn-In passed the complete browser journey.
- Browser evidence included the paired-first Shared Setup presentation, full League Wheel and Club Pack journey.
- No runtime/provider gameplay regression was found in the PR215 debugging described below.

Do not reinterpret successful Pages deployment alone as final production authorization. The post-merge Stability lane still exposed a repository contract boundary and production authority remains conservative until the complete r6 convergence gate is clean.

## 3. What is OPEN / UNMERGED / NOT YET PROVEN

Open bounded correction: PR #215, branch `fix/pr214-postmerge-dual-screen-contract`, title `Fix post merge dual-screen MDP contract wording`.

Pre-SLE substantive head: `da099afa12b5b760bf85728857685a7913d25ba3`. The SLE packaging commits necessarily advance the PR head after that point. The successor must independently resolve the exact live head and must never merge using the recorded pre-SLE SHA.

PR215 has no inline review threads at the last verified check. Reverify this live.

The closing environment intentionally did NOT merge PR215 because Session Handoff Proximity entered the mandatory SLE packaging band. The atomic safe checkpoint is therefore an unmerged release-candidate publication correction with a deterministic next assertion.

## 4. PR215 failure/correction history — do not repeat this debugging

PR215 originated from post-merge Stability failures after PR214.

1. Dual-full-screen contract wording
   - Machine-readable MDP rule expressed the correct concept but did not literally say `both managers` in the required exit-rule string.
   - Correction strengthened `SHARED_SHOWDOWN_DUAL_SCREEN_EXPERIENCE.json`; the test was not weakened.

2. Historical provenance in `00_DEVELOPER_START_HERE.md`
   - Currentization for PR214 accidentally omitted immutable PR203/205/207/209/210 markers.
   - Correction restored the historical chain while preserving current r6/PR215 truth.

3. WEC stale failure counters
   - WEC still reported unresolved failures after the underlying defects had already been diagnosed/corrected.
   - Counters were synchronized without erasing evidence notes.

4. GitHub CLI bootstrap continuity clause
   - Three shared contract workflows advanced to `github-cli-bootstrap-contracts.cjs` because the shortened developer entrypoint had dropped the literal bootstrap/recovery instruction.
   - Head `da099afa...` restored connected-GitHub-first routing, repository bootstrap only when native `gh` is genuinely needed, official release checksum verification, `gh auth status`, and prohibition on extracting/repurposing connector credentials.

5. Current proven remaining assertion
   - On fresh exact-head CI after `da099afa...`, the same three shared repository-contract workflow families advanced further.
   - `tests/contracts/production-authorization-acceptance-contracts.cjs` stopped at: source may differ from production authority only for an explicit active release-candidate publication WEC.
   - The contract requires an active WEC with integrity valid, assessment `CONTINUE`, non-main branch, and `currentTask` matching `/release candidate|publish|converg/i` while source and production runtime differ.
   - Closing WEC is active/CONTINUE on PR215 and source is r6 while production authority is r5, but its older `currentTask` said `Finish PR215 post-merge r6 release correction...`; that wording did not match the required publication/convergence marker.
   - Diagnosis: continuity metadata drift only. Do not weaken the production-authorization test. The live WEC/SLE sealing update should explicitly say `release-candidate publication` or `converge`.

At the last pre-package CI observation, other completed workflow families were green and the specialized Stage 5F/runtime setup steps in the three red families were green; all three stopped in the shared complete repository-contract layer. The successor must still verify the new live package head rather than infer green from this history.

## 5. Milestone Delivery Progress — MDP-1

Current MDP: `39.00/100`.

Permanent reporting rule: at every meaningful engineering checkpoint report exactly `MDP task delta: +X.XX (AA.AA -> BB.BB)`. Important work that does not complete a lifecycle stage receives `+0.00`; never award points for effort alone.

The next legitimate unlock is combined `+2.50`, moving MDP `39.00 -> 41.50`, only if all four Shared Setup capabilities complete their final product-integration stage through a coherent r6 release boundary:
- `setup-league`
- `setup-clubs`
- `setup-length`
- `setup-confirmation`

PR215 contract correction, SLE packaging, CI mechanics, merge mechanics and documentation do not independently earn those points. They remove the publication gate that currently prevents product-integration proof.

MDP and SSJR are separate. MDP never grants SSJR credit.

## 6. Shared Showdown Journey Readiness — SSJR-1.1

Current SSJR: `0/100`.
Estimated focused sessions to genuine SSJR100: `~5-10` until evidence changes the model.

Only fixed evidence-backed whole capabilities earn SSJR credit. Source, design, PRs, CI, reviews, merges, deployment, recorder/validator/observer tooling, MDP and SLE/SNS receive zero SSJR credit without genuine production two-account evidence.

Do not ask the owner to perform two-account acceptance until r6 is independently proven through PR215 merge and post-merge release gates.

When testing is legitimately unlocked, use two legitimate private manager accounts on two genuinely distinct registered browsers/devices. A second tab/window sharing one browser identity is insufficient. Owner hardware historically available: Chromebook plus iPhone Safari; use two independent networks when required by the acceptance contract.

Evidence must remain privacy-safe. Never ask the owner to paste raw account/device/rivalry/session/pairing IDs, capabilities or canonical storage bytes.

Recorder command: `npm run record:ssjr-production-shared-setup`
Validator command: `npm run validate:ssjr-production-shared-setup -- <player-one-evidence.json> <player-two-evidence.json>`

## 7. SSJR-DUAL-FULL-SCREEN-1 — permanent owner rule

Both legitimate managers must individually experience every canonical Shared Showdown gameplay screen on their own device from shared entry through terminal completion.

This applies to the whole gameplay journey, not only League Wheel and Club Packs. The repository-owned mapping covers all 20 SSJR capabilities, including later career-start, Transfer Challenge, results, scoring, history/trophies, season transitions, reconciliation and terminal completion.

Role-specific controls may differ. For example, one manager may own an action while the peer observes. That does not permit the observing peer to skip the screen. Both devices must render the complete canonical gameplay stage.

Late, reloaded, reconnected or temporarily offline peers must replay missed canonical screens in order before their local experience may advance beyond them. A toast, status badge, summary, redirect or background synchronization is not a full-screen witness.

Every future Shared Journey screen automatically inherits this invariant. MDP lifecycle completion is blocked for any user-facing capability whose required dual-full-screen experience is not implemented and regression-covered.

## 8. Security / privacy / storage / IAM locks

These are permanent unless a later explicit owner instruction changes them safely:

- Billing permanently OFF; Firebase Spark only.
- App Check enforcement OFF.
- Firestore browser persistence memory-only.
- Google Auth popup-only `browserSessionPersistence`; no extra scopes.
- Exactly two private managers.
- Pairing and exact ACTIVE private session before any league or club authority.
- Canonical localStorage exactly three keys: `careerModeShowdown.saveLibrary`, `careerModeShowdown.legacyShowdowns`, `careerModeShowdown.preferences`.
- Candidate C is the sole destructive remote-to-local gameplay Apply authority.
- Candidate C rollback remains transaction-owned strict exact raw-snapshot rollback.
- No public discovery, listing, lobby, matchmaking, community, rankings or global leaderboards.
- Never durably retain raw private capabilities or raw account/device/rivalry/session/pairing IDs.
- Local-only saves remain local by default; cloud/remote is by explicit need.
- Do not initialize paid provider components.

## 9. GitHub/tool routing

Use the connected GitHub app/connector first for repository reads/writes, PRs, CI, reviews and supported deployment evidence.

The repository-owned rootless native CLI bootstrap is fallback only: `npm run work:gh:bootstrap`. Use it only when a concrete operation genuinely needs native `gh` and the connected GitHub route cannot do it. The bootstrap installs the official release with checksum verification into ignored environment-local tooling; verify with `gh auth status`. Never extract, copy, repurpose or inject connector credentials into `gh`.

If a route materially fails twice, obey the route circuit breaker rather than circling.

## 10. Work Environment Continuity and SHP-2

Closing environment: `we-2026-09-07-pr214-mdp1-a58`.
Direct predecessor recorded by that WEC: `we-2026-09-07-pr214-session-proximity-v2-a57` through repository-owned archive `WORK_ENVIRONMENT_ARCHIVE/we-2026-09-07-pr214-session-proximity-v2-a57.json`.

The successor must validate/archive the inherited WEC, then initialize a fresh unique successor WEC. Do not inherit the predecessor transition decision. Reset Session Handoff Proximity to `0%` for the new session.

SHP-2 is session pressure only, calculated from observable proxies including message/tool volume, long evidence reads, truncations, compactions, edited files, commits, CI/debug cycles, deployment workload, unresolved states and safe checkpoint risk. HTR-1 is separate repository transfer readiness and never supplies the SHP percentage.

The closing session experienced multiple long/truncated evidence reads, multiple bounded CI debug loops and multiple context compactions. That real pressure triggered SLE generation instead of forcing another merge/debug cycle.

Every future environment inherits the owner rule to report SHP frequently at meaningful checkpoints without making reporting itself the work. At 95-99 generate SNS/SLE immediately. At 100 the verified package must already exist and the closing session stops before a new substantial milestone.

## 11. Owner standing authorization

The owner has standing authorization for merge/deploy work through project completion after all current mandatory validation gates are actually satisfied. Do not repeatedly ask for permission to merge a clean PR. This authorization never permits weakening gates or enabling billing.

Automate every reproducible protocol, Rules, browser, storage, recorder, validator, adverse-network and deployment test. Ask the owner only for irreducible physical/two-account actions that cannot be automated.

## 12. IMMEDIATE NEXT TASK AFTER FULL STUDY

1. Retrieve `START_NEXT_SESSION_V1.4.56_PR215_R6_RELEASE_CANDIDATE_PUBLICATION_NEXT.md`, `SESSION_BOOTSTRAP.json`, `WORK_ENVIRONMENT_STATUS.json`, `NEXT_TASK.md`, `PROJECT_STATE.md`, `MILESTONE_DELIVERY_PROGRESS.json`, `SHARED_SHOWDOWN_DUAL_SCREEN_EXPERIENCE.json`, `REMOTE_JOINING_READINESS.json`, and `SHARED_SHOWDOWN_JOURNEY_READINESS.json`. Use this full handoff only as deep fallback/context.
2. Independently verify current `main`, current PR #215 head and tree, PR state/mergeability, review threads, all exact-head workflow families, package/runtime identity, Pages/deployment evidence and current production authority. Current live evidence overrides this file.
3. Validate/archive inherited WEC a58 and initialize a fresh unique successor WEC with reset observations/counters and no inherited transition decision.
4. Keep the active task explicitly framed as `Converge PR215 r6 release-candidate publication`. While source r6 differs from production r5, preserve active lifecycle + `CONTINUE` until the production-authorization contract permits the candidate boundary. Do not weaken that contract.
5. Inspect fresh exact-head CI generated by the SLE/package head. If the known production-authorization assertion is resolved, continue to the next objective assertion only if one exists. If any new red appears, diagnose it from exact logs; do not combine evidence across heads and do not reopen already-green runtime/provider lanes without evidence.
6. Require all 15 permanent workflow families green on one exact reviewed PR215 head and all objective review threads resolved.
7. Squash merge PR215 only with expected-head protection.
8. Verify resulting exact `main`, post-merge Stability including chromium-stability/deployed-site-smoke, Pages/deployed coherent `v1.9.1 / 1.9.1-r6` shell, and Release Integration Burn-In as applicable.
9. If and only if that complete boundary is clean, update the four Shared Setup capabilities from 90% to 100% MDP lifecycle maturity and award their combined `+2.50`, reporting `MDP task delta: +2.50 (39.00 -> 41.50)`. Do not move SSJR.
10. Then tell the owner `YOUR TESTING TIME` and conduct only the minimum genuine two-account Shared Setup acceptance. Both managers must see all full gameplay screens. Preserve pairing + exact ACTIVE before League Wheel/Club Packs.
11. Record each private observation only through stdin with `npm run record:ssjr-production-shared-setup`; retain sanitized evidence only. Validate the pair with `npm run validate:ssjr-production-shared-setup -- <player-one-evidence.json> <player-two-evidence.json>`.
12. Recalculate SSJR only if the fixed validator/model accepts genuine production evidence.
13. Do not begin transfer/results/scoring transport before the Shared Setup evidence boundary is resolved unless a later explicit owner instruction supersedes this stop condition.

## 13. Required owner progress format

At meaningful checkpoints keep the existing compact owner-facing fields:

`Session handoff proximity: N%`
`Shared Showdown Journey readiness: N/100`
`Milestone Delivery Progress: N.NN/100`
`Current lane: ...`
`Concrete dependency completed: ...`
`Next unlock: ...`
`Blocker: ...`
`Sidequest check: NONE` unless a genuine sidequest exists.

Also report `MDP task delta: +X.XX (AA.AA -> BB.BB)` for the current task/checkpoint.

## 14. Recursive SLE rule

Every future developer inherits SLE = Smart Lean Efficient. At a real transition boundary generate a newest versioned `START_NEXT_SESSION_...md`, a complete `SUCCESSOR_HANDOFF_..._SLE_...md`, byte-identical project mirrors, refreshed `SESSION_BOOTSTRAP.json`/live pointers, and a freshly generated repository-first owner prompt. Validate the package. Give the owner the short prompt by default and keep the starter as fallback. Do not burden the owner with the full handoff unless deep recovery is genuinely needed or explicitly requested.

At Session Handoff Proximity 100, stop. Do not begin the successor’s next substantial milestone in the closing environment.
