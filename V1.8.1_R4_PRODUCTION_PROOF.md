# v1.8.1-r4 Production Proof

Status: DEPLOYED / PRODUCTION-PROVEN
Recorded: 2026-08-27 ET
Application: `v1.8.1`
Runtime: `1.8.1-r4`
Previous known-good whole-shell recovery runtime: `1.8.1-r3`
Remote Joining readiness: `82/100` under fixed RJR-1

## Exact source and publication boundary

- Pull request: #160 `Harden Stage 4 App Check lifecycle`
- Final exact PR head: `9b39d9b6032eb24ef98a252ec7de13e129443c95`
- Exact-head PR validation: all 14 permanent workflow families succeeded on that unchanged head.
- Submitted reviews before merge: 0.
- Inline review threads before merge: 0.
- Mergeability before merge: true.
- Publication: expected-head squash merge under standing owner authorization.
- Runtime merge / live-main SHA: `2964527c4f7fc80b16d6d5ce73bd4f5823487d2c`.
- Live `main` was independently re-read and matched the merge SHA exactly.

## Capability proven

Permanent `tests/contracts/stage4-token-lifecycle-contracts.cjs` had already passed on exact proof head `ac465bc781b038860f91620debb7ae7fc7a3e05d` before the final packaging-only compatibility work. It executes the production Firebase runtime boundary and proves:

- Firebase App Check token auto-refresh remains SDK-owned through `isTokenAutoRefreshEnabled: true`;
- a distinct later expiry can be observed through `onTokenChanged` without fabricating transitions from duplicate same-expiry notifications;
- bounded explicit `getToken(appCheck, true)` force-refresh success and provider failure are handled without turning attestation monitoring into gameplay/save authority;
- observer callback failure is metadata-only;
- raw App Check token strings never enter diagnostics or exposed lifecycle state;
- existing Auth and Firestore service identity is preserved;
- Connected Rivalry authority remains unchanged;
- the canonical local Save Library fixture remains byte-for-byte unchanged;
- no custom refresh scheduler/timer is added;
- App Check enforcement remains OFF.

The fixed RJR-1 score moved exactly once, `81 → 82`, in `production-cloud-security`. CI volume, release packaging, merge and deployment receive zero duplicate readiness credit.

## Post-merge production evidence

Exactly 15 push/deployment workflow runs were created for live main `2964527c4f7fc80b16d6d5ce73bd4f5823487d2c`. At final verification there were no queued, in-progress, null-conclusion or failed runs.

Key production runs:

- GitHub Pages: run `33035579363` — SUCCESS.
- Release Integration Burn-In: run `33035579462` — SUCCESS.
  - `integration-burnin-pass-1` job `98397439170` — SUCCESS through the complete stateful integration journey.
  - `integration-burnin-pass-2` job `98397439279` — SUCCESS through the complete stateful integration journey.
- Stability Lane: run `33035579438` — SUCCESS.
  - `stability-contracts` job `98397439119` — SUCCESS.
  - `chromium-stability` job `98397532686` — SUCCESS through canonical runtime, Save Library, offline lifecycle and the complete integration journey.
  - `deployed-site-smoke` job `98397917248` — SUCCESS after all deployed checks completed.

The deployed-site smoke proved, against the published Pages site:

1. every runtime byte matched the expected runtime artifact;
2. deployed runtime-error provenance was clean;
3. the production App Check token path succeeded;
4. Home visual audit succeeded;
5. visible Save Library audit succeeded;
6. manager identity linkage audit succeeded;
7. identity-safe Career Analytics audit succeeded;
8. crop-safe licensed football-photo audit succeeded;
9. Candidate A backup export audit succeeded;
10. Candidate B import analysis succeeded;
11. Candidate C atomic restore and critical recovery audit succeeded;
12. install/offline boundary audit succeeded;
13. the complete deployed journey succeeded.

## Production identity and rollback

`v1.8.1 / 1.8.1-r4` is therefore the production-proven runtime. `1.8.1-r3` remains the immediate previous known-good whole-shell recovery target.

Do not partially roll back token-lifecycle files. Recovery remains a whole-runtime-generation operation and must preserve canonical storage, strict exact raw snapshot authority, transaction-owned Candidate C mutation, ownership-scoped rollback, Installable Offline App behavior, Google popup/session-only authentication, memory-only Firestore and App Check enforcement OFF.

## Uncredited remaining Remote Joining capability

This production proof does not credit or close:

- authenticated third-account / revoked registered-device production negatives, which still require legitimate authenticated production identity/device state;
- two-physical-network behavior;
- actual Remote Joining host/join/session orchestration;
- real-device token-lifecycle acceptance;
- abuse hardening;
- production rollback proof;
- final stable Remote Joining release acceptance.

Stage 5 remains locked until the remaining explicit pre-Stage-5 gates are genuinely closed. Do not repeat already-consumed owner pairing/device recovery, unavailable-code, destructive reconciliation, exact replay, adverse-provider or deterministic token-lifecycle proof merely for confidence.