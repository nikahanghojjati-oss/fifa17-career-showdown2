const assert=require('node:assert/strict');
const crypto=require('node:crypto');
const fs=require('node:fs');
const os=require('node:os');
const path=require('node:path');
const {spawnSync}=require('node:child_process');
const NegativePath=path.resolve('js/ssjrProductionNegativeEvidence.js');
const RunnerPath=path.resolve('js/ssjrProductionNegativeProbeRunner.js');
const assembler=path.resolve('scripts/assemble-ssjr-shared-setup-safe-evidence.mjs');
const validator=path.resolve('scripts/validate-ssjr-shared-setup-production-evidence.mjs');
const H=value=>`sha256:${crypto.createHash('sha256').update(String(value)).digest('hex')}`;
function stable(value){if(Array.isArray(value))return value.map(stable);if(value&&typeof value==='object')return Object.fromEntries(Object.keys(value).sort().map(k=>[k,stable(value[k])]));return value;}
const D=value=>`sha256:${crypto.createHash('sha256').update(JSON.stringify(stable(value))).digest('hex')}`;
const finalCore={leagueId:'premier_league',clubs:{playerOne:'Arsenal',playerTwo:'Liverpool'},clubLeagueIds:{playerOne:'premier_league',playerTwo:'premier_league'},totalSeasons:3,confirmedRoles:['playerOne','playerTwo'],phase:'SHOWDOWN_CONFIRMED',revision:6};const finalSetup={...finalCore,digest:D(finalCore)},seedDigest=H('shared-seed-rev4');
function positive(managerRole,remoteRole,suffix){return {evidenceType:'SSJR-1.1-production-shared-setup-guided-positive-draft',privacySafe:true,rawAuthorityIncluded:false,canonicalStorageRawIncluded:false,generatedAt:'2026-09-08T20:00:06Z',schemaVersion:1,runtimeRevision:'1.9.1-r6',startedAt:'2026-09-08T20:00:00Z',managerRole,remoteRole,accountFingerprint:H(`account-${suffix}`),deviceFingerprint:H(`device-${suffix}`),rivalryFingerprint:H('shared-rivalry'),canonicalStorageBeforeHash:H('canonical'),canonicalStorageAfterHash:H('canonical'),canonicalStorageViolation:false,pairedActiveBeforeSetup:{at:'2026-09-08T20:00:01Z',paired:true,sessionState:'active',setupMutationSeen:false,sessionFingerprint:H('initial-session')},authoritativeSetupObserved:{at:'2026-09-08T20:00:02Z',revision:4,setupDigest:seedDigest},identicalFinalSetup:{at:'2026-09-08T20:00:03Z',setupDigest:finalSetup.digest},reloadResume:{at:'2026-09-08T20:00:04Z',setupDigest:finalSetup.digest,resetOrRedraw:false},freshActiveSessionResume:{at:'2026-09-08T20:00:05Z',setupDigest:finalSetup.digest,resetOrRedraw:false,sessionFingerprint:H('fresh-session')},finalSetup:structuredClone(finalSetup)};}
const rules={wrongSession:['production-shared-setup-wrong-session-probe','SETUP_SESSION_INVALID'],expiredSession:['production-shared-setup-expired-session-probe','SETUP_ACTIVE_SESSION_REQUIRED'],unrelatedAccount:['stage5f-third-account-provider-probe','STAGE5F_THIRD_ACCOUNT_PROVIDER_DENIED'],revokedIdentity:['stage5f-revoked-device-provider-probe','STAGE5F_REVOKED_DEVICE_PROVIDER_DENIED'],staleRevision:['production-shared-setup-stale-revision-probe','SETUP_STALE_BASE_REVISION'],replayConflict:['production-shared-setup-replay-conflict-probe','SETUP_IDEMPOTENCY_CONFLICT'],directFieldSubstitution:['shared-setup-production-direct-field-probe','SETUP_DRAW_MISMATCH'],coordinatorBypass:['shared-setup-production-coordinator-bypass-probe','SETUP_COORDINATOR_REQUIRED']};
function negatives(p){return {schemaVersion:2,evidenceType:'SSJR-1.1-production-shared-setup-negatives-safe',runtimeRevision:'1.9.1-r6',managerRole:p.managerRole,accountFingerprint:p.accountFingerprint,deviceFingerprint:p.deviceFingerprint,rivalryFingerprint:p.rivalryFingerprint,observations:Object.fromEntries(Object.entries(rules).map(([name,[source,code]])=>[name,{at:'2026-09-08T20:00:02Z',status:'denied',source,code,localStorageUnchanged:true,provenance:'direct-production-probe-v1'}]))};}
function write(dir,name,value){const file=path.join(dir,name);fs.writeFileSync(file,JSON.stringify(value));return file;}function assemble(p,n,dir,label){return spawnSync(process.execPath,[assembler,write(dir,`${label}-p.json`,p),write(dir,`${label}-n.json`,n)],{encoding:'utf8'});}
(async()=>{
  let stored=null;global.sessionStorage={getItem:()=>stored,setItem:(_k,v)=>{stored=v},removeItem:()=>{stored=null}};
  global.CareerModeSSJRProductionNegativeProbeRunner={probeContract:'ssjr-negative-probe-runner-v1',run:async name=>({ok:true,probeContract:'ssjr-negative-probe-runner-v1',denied:true,source:rules[name][0],code:rules[name][1],localStorageUnchanged:true,managerRole:'playerOne',accountFingerprint:H('account-one'),deviceFingerprint:H('device-one'),rivalryFingerprint:H('shared-rivalry')})};
  delete require.cache[NegativePath];const Negative=require(NegativePath);assert.equal(Negative.record,undefined,'generic caller proof ingestion must not exist');Negative.clear();Negative.bindIdentity({managerRole:'playerOne',accountFingerprint:H('account-one'),deviceFingerprint:H('device-one'),rivalryFingerprint:H('shared-rivalry')});await Negative.runProbe('staleRevision');assert.equal(Negative.getState().completedCount,1);assert.equal(JSON.parse(stored).observations.staleRevision.provenance,'direct-production-probe-v1');
  const poisoned=JSON.parse(stored);poisoned.sessionId='session_'+('a'.repeat(64));stored=JSON.stringify(poisoned);delete require.cache[NegativePath];const Reload=require(NegativePath);assert.equal(Reload.getState().completedCount,0);assert.equal(Reload.getState().managerRole,null);
  global.crypto=crypto.webcrypto;global.captureCareerModeRawBackupInputs=()=>({saveLibrary:'private-save',legacyShowdowns:null,preferences:'private-preferences'});global.CareerModeProductionSharedShowdownSetup={getState:()=>({ready:true,accountId:'manager1',rivalryId:'pair_'+('1'.repeat(64)),sessionId:'session_'+('2'.repeat(64)),deviceId:'device_'+('3'.repeat(32)),managerRole:'playerOne'})};global.CareerModeSparkSharedShowdownSetup={read:async opts=>opts.sessionId.endsWith('0')?{ok:false,code:'SETUP_SESSION_INVALID'}:{ok:true,revision:0,state:null},mutate:async()=>({ok:false,code:'SETUP_STALE_BASE_REVISION'})};global.CareerModeProductionFirebaseRuntime={ensureAccountServices:async()=>({ok:true,auth:{currentUser:{uid:'manager1'}},firestore:{},firestoreSdk:{}})};delete require.cache[RunnerPath];const Runner=require(RunnerPath);const direct=await Runner.run('wrongSession');assert.equal(direct.denied,true);assert.equal(direct.source,rules.wrongSession[0]);assert.equal(direct.probeContract,'ssjr-negative-probe-runner-v1');
  assert.equal(direct.accountFingerprint,H('manager1'));assert.equal(direct.deviceFingerprint,H('device_'+('3'.repeat(32))));
  const captured=global.captureCareerModeRawBackupInputs;delete global.captureCareerModeRawBackupInputs;
  await assert.rejects(Runner.run('wrongSession'),{code:'SSJR_NEGATIVE_STORAGE_AUTHORITY_UNAVAILABLE'});global.captureCareerModeRawBackupInputs=captured;
  const identity={managerRole:'playerOne',accountFingerprint:H('account-one'),deviceFingerprint:H('device-one'),rivalryFingerprint:H('shared-rivalry')};
  Negative.clear();Negative.bindIdentity(identity);const ownedRunner=global.CareerModeSSJRProductionNegativeProbeRunner;
  global.CareerModeSSJRProductionNegativeProbeRunner={...ownedRunner,run:async name=>({...await ownedRunner.run(name),accountFingerprint:H('another-account')})};
  await assert.rejects(Negative.runProbe('staleRevision'),{code:'SSJR_NEGATIVE_IDENTITY_CHANGED'});assert.equal(Negative.getState().completedCount,0);
  let finish;global.CareerModeSSJRProductionNegativeProbeRunner={...ownedRunner,run:()=>new Promise(resolve=>{finish=resolve})};
  const pending=Negative.runProbe('staleRevision');Negative.clear();Negative.bindIdentity(identity);finish(await ownedRunner.run('staleRevision'));
  await assert.rejects(pending,{code:'SSJR_NEGATIVE_IDENTITY_CHANGED'});assert.equal(Negative.getState().completedCount,0);global.CareerModeSSJRProductionNegativeProbeRunner=ownedRunner;
  let expiresAt=Date.now()+60000;const state=global.CareerModeProductionSharedShowdownSetup.getState();let liveState=state;const calls=[];
  global.CareerModeProductionSharedShowdownSetup.getState=()=>liveState;
  const sdk={doc:(_db,...parts)=>parts.join('/'),runTransaction:async fnDb=>fnDb};
  sdk.runTransaction=async (_db,fn)=>fn({get:async ref=>({exists:()=>true,data:()=>ref.includes('/sessions/')?{data:{state:'active',hostAccountId:'manager1',memberAccountIds:['manager1','manager2'],expiresAt:{toMillis:()=>expiresAt}}}:{data:{managerSlots:[{slotId:'playerOne',accountId:'manager1',profileId:'profile1',saveId:'save1',entitlementState:'active'}]}}})});
  const services={ok:true,auth:{currentUser:{uid:'manager1'}},firestore:{},firestoreSdk:sdk};global.CareerModeProductionFirebaseRuntime.ensureAccountServices=async()=>services;
  global.CareerModeSparkSharedShowdownSetup.read=async options=>{calls.push(options.nowEpochMs);return {ok:false,code:'SETUP_ACTIVE_SESSION_REQUIRED'}};
  await assert.rejects(Runner.run('expiredSession'),{code:'SSJR_NEGATIVE_SESSION_NOT_EXPIRED'});assert.equal(calls.length,0,'a future clock must not manufacture expiry');
  expiresAt=Date.now()-1000;liveState={ready:false};const start=Date.now();const expired=await Runner.run('expiredSession');assert.equal(expired.denied,true);assert.ok(calls[0]>=start&&calls[0]<=Date.now(),'adapter must receive actual wall time');
  await assert.rejects(Runner.run('unrelatedAccount',{operatorConfirmedThirdAccount:true,denied:true}),{code:'SSJR_NEGATIVE_ACCOUNT_CONTEXT_CONFLICT'});
  liveState={...state,accountId:'different-manager'};await assert.rejects(Runner.run('wrongSession'),{code:'SSJR_NEGATIVE_IDENTITY_CHANGED'});liveState=state;
  const dir=fs.mkdtempSync(path.join(os.tmpdir(),'ssjr-safe-assembly-'));try{const p1=positive('playerOne','host','one'),p2=positive('playerTwo','peer','two');let a=assemble(p1,negatives(p1),dir,'one'),b=assemble(p2,negatives(p2),dir,'two');assert.equal(a.status,0,a.stderr);assert.equal(b.status,0,b.stderr);const outA=JSON.parse(a.stdout),outB=JSON.parse(b.stdout);assert.deepEqual(Object.values(outA.negatives),Array(8).fill('denied'));if(fs.existsSync(validator)){const aPath=write(dir,'assembled-one.json',outA),bPath=write(dir,'assembled-two.json',outB);const pair=spawnSync(process.execPath,[validator,aPath,bPath],{encoding:'utf8'});assert.equal(pair.status,0,pair.stderr);const summary=JSON.parse(pair.stdout);assert.equal(summary.ok,true);assert.equal(summary.negativesProvenPerManager,8);}for(const path of [['pairedActiveBeforeSetup'],['authoritativeSetupObserved'],['identicalFinalSetup'],['reloadResume'],['freshActiveSessionResume'],['finalSetup'],['finalSetup','clubs'],['finalSetup','clubLeagueIds']]){const invalid=structuredClone(p1);let target=invalid;for(const key of path)target=target[key];target.unexpected='reject';const result=assemble(invalid,negatives(invalid),dir,'nested-'+path.join('-'));assert.notEqual(result.status,0,path.join('.'));assert.equal(result.stdout,'','invalid evidence must never be emitted');}const invalidDigest=structuredClone(p1);invalidDigest.finalSetup.totalSeasons=5;assert.notEqual(assemble(invalidDigest,negatives(invalidDigest),dir,'invalid-digest').status,0);const badOrder=structuredClone(p1);badOrder.reloadResume.at='2026-09-08T19:00:00Z';assert.notEqual(assemble(badOrder,negatives(badOrder),dir,'bad-order').status,0);const mismatch=negatives(p1);mismatch.accountFingerprint=H('other-account');a=assemble(p1,mismatch,dir,'mismatch');assert.notEqual(a.status,0);assert.match(a.stderr,/accountFingerprint differ/i);const missing=negatives(p1);delete missing.observations.wrongSession;a=assemble(p1,missing,dir,'missing');assert.notEqual(a.status,0);assert.match(a.stderr,/missing field wrongSession|must be an object/i);const invented=negatives(p1);invented.observations.staleRevision.source='invented';a=assemble(p1,invented,dir,'invented');assert.notEqual(a.status,0);assert.match(a.stderr,/exact rule table|direct proven denial/i);const old=negatives(p1);old.schemaVersion=1;a=assemble(p1,old,dir,'old');assert.notEqual(a.status,0);assert.match(a.stderr,/direct-probe evidence v2/i);const raw=negatives(p1);raw.observations.staleRevision.sessionId='session_'+('a'.repeat(64));a=assemble(p1,raw,dir,'raw');assert.notEqual(a.status,0);assert.match(a.stderr,/unknown field sessionId|Forbidden raw authority/i);}finally{fs.rmSync(dir,{recursive:true,force:true});}
  const bootstrap=fs.readFileSync(path.resolve('js/ssjr.js'),'utf8'),sw=fs.readFileSync(path.resolve('service-worker.js'),'utf8');for(const file of ['js/stage5fProductionAuthenticatedNegatives.js','js/ssjrProductionNegativeProbeRunner.js','js/ssjrProductionNegativeEvidence.js']){assert.match(bootstrap,new RegExp(file.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));assert.match(sw,new RegExp(file.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));}
  console.log('PASS SSJR safe evidence assembly contracts: direct production probes own denial provenance, poisoned persisted state is discarded, exact source/code rules are enforced, r6 acceptance loads the negative tooling, and no raw authority is assembled.');
})().catch(e=>{console.error(e);process.exit(1)});
