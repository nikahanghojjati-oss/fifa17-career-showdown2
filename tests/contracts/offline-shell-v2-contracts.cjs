const A=require('node:assert/strict'),fs=require('node:fs'),path=require('node:path');const root=path.resolve(__dirname,'../..'),read=p=>fs.readFileSync(path.join(root,p),'utf8'),exists=p=>fs.existsSync(path.join(root,p));const html=read('index.html'),app=read('js/app.js'),offline=read('js/offlineApp.js'),ocss=read('css/offline.css'),settings=read('js/settings.js'),scss=read('css/settings.css'),worker=read('service-worker.js'),manifest=JSON.parse(read('manifest.webmanifest')),rev=(html.match(/app-asset-revision"\s+content="([^"]+)/)||[])[1],ver=(app.match(/const APP_VERSION\s*=\s*"([^"]+)"/)||[])[1],wrev=(worker.match(/const RUNTIME_REVISION\s*=\s*"([^"]+)"/)||[])[1],prev=(worker.match(/const PREVIOUS_RUNTIME_REVISION\s*=\s*"([^"]*)"/)||[])[1];A.ok(rev&&ver);A.match(rev,new RegExp(`^${ver.replace(/\./g,'\\.')}\\-r[1-9]\\d*$`));A.equal(wrev,rev);A.notEqual(prev,wrev);A.equal(manifest.start_url,'./');A.equal(manifest.scope,'./');A.equal(manifest.display,'standalone');A.ok(manifest.theme_color&&manifest.background_color);A.ok(manifest.icons.some(i=>i.sizes==='192x192')&&manifest.icons.some(i=>i.sizes==='512x512'&&/maskable/.test(i.purpose||'')));for(const i of manifest.icons){const u=new URL(i.src,'https://x.invalid/app/'),p=u.pathname.replace(/^\/app\//,'');A.equal(u.searchParams.get('v'),rev);A.ok(exists(p))}const eager=[...html.matchAll(/<script\b[^>]*\bsrc="([^"]+)"/gi)].map(m=>m[1]);A.equal(eager.length,7);A.equal(eager.some(x=>x.includes('offlineApp.js')),false);A.ok(app.includes('manifest.webmanifest')&&app.includes('js/offlineApp.js'));const sm=worker.match(/const SHELL_PATHS\s*=\s*Object\.freeze\((\[[\s\S]*?\])\);/);A.ok(sm);const shell=JSON.parse(sm[1]);A.ok(shell.length>=50);A.equal(new Set(shell).size,shell.length);for(const p of ['index.html','manifest.webmanifest','css/offline.css','css/settings.css','js/offlineApp.js','js/settings.js','js/backup.js','js/importAnalysis.js','js/restore.js','js/storageTransaction.js'])A.ok(shell.includes(p),`missing ${p}`);for(const p of shell)A.ok(exists(p),p);const installStart=worker.indexOf('self.addEventListener("install"'),messageStart=worker.indexOf('self.addEventListener("message"');A.ok(installStart>=0&&messageStart>installStart);const install=worker.slice(installStart,messageStart);A.ok(install.includes('populateCurrentCache'));A.equal(install.includes('skipWaiting'),false);for(const n of ['CMS_ACTIVATE_UPDATE','chooseNavigationRuntime','CMS_PROBE_NETWORK','CMS_GET_CACHE_STATUS','CMS_ROLLBACK_TO_PREVIOUS','!keepShellCaches.has(name)'])A.ok(worker.includes(n),n);for(const n of ['new Set(["mainMenu","dashboard"])','[data-transaction-busy="true"],[data-critical-recovery="true"]','activationRequested','controllerchange','location.reload()','updateViaCache:"none"','CMS_PROBE_NETWORK','OFFLINE · YOUTUBE MEDIA REQUIRES A CONNECTION','Add to Home Screen','getOfflineAppSettingsState','requestOfflineAppInstall'])A.ok(offline.includes(n),n);for(const n of ['offlineAppRail','offlineAppPanel','ensureOfflineUI','offlineInstallPanel']){A.equal(offline.includes(n),false,n);A.equal(ocss.includes(n),false,n)}A.equal(/position\s*:\s*(fixed|sticky)/i.test(ocss),false);A.ok(settings.includes('createOfflinePanel')&&settings.includes('settingsOfflineInstallButton')&&settings.includes('requestOfflineAppInstall')&&settings.includes('activateWaitingOfflineUpdate'));A.ok(scss.includes('.settingsOfflinePanel')&&scss.includes('.settingsOfflineInstallButton'));A.ok(ocss.includes('--startup-mobile-top-band:clamp(46px,12vw,54px)'));A.ok(ocss.includes('height:calc(100% - var(--startup-mobile-top-band))'));A.ok(ocss.includes('object-fit:cover')&&ocss.includes('object-position:54% 50%'));A.equal(/\b100(?:d|s|l)?vh\b/i.test(ocss),false);const workflows=fs.readdirSync(path.join(root,'.github/workflows')).filter(f=>/\.ya?ml$/i.test(f)).map(f=>({f,s:read('.github/workflows/'+f)})),owners=re=>workflows.flatMap(x=>[...x.s.matchAll(re)].map(()=>x.f));A.deepEqual(owners(/tests\/browser\/offline-boundary-audit\.cjs/g),['validate-stability-lane.yml','validate-stability-lane.yml']);A.deepEqual(owners(/tests\/browser\/offline-cache-lifecycle-v2-audit\.cjs/g),['validate-stability-lane.yml']);A.deepEqual(owners(/tests\/browser\/offline-app-audit\.cjs/g),[]);A.deepEqual(owners(/tests\/browser\/settings-install-audit\.cjs/g),['validate-settings-workstream.yml']);const stability=workflows.find(x=>x.f==='validate-stability-lane.yml')?.s||'';A.ok(stability.includes('CMS_CHROMIUM_MULTI_CONTEXT=1')&&stability.includes('Run install and offline boundary audit against deployed Pages'));console.log(`Offline v2 contracts passed for ${rev}: Settings-only install, bounded standalone loading, ${shell.length} shell resources, split Stability ownership.`);