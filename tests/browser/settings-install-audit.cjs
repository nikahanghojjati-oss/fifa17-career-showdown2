const assert=require('node:assert/strict');const{chromium}=require('playwright');const{resolveChromiumRuntime}=require('../support/chromium-runtime.cjs');const baseUrl=new URL(process.env.CMS_BASE_URL||'http://127.0.0.1:4173/');async function run(){const runtime=await resolveChromiumRuntime();const browser=await chromium.launch(runtime);const context=await browser.newContext({viewport:{width:390,height:844},deviceScaleFactor:2,isMobile:true,hasTouch:true,userAgent:'Mozilla/5.0 (iPhone; CPU iPhone OS 18_6 like Mac OS X) AppleWebKit/605.1.15 Version/18.6 Mobile/15E148 Safari/604.1'});const page=await context.newPage();const errors=[];page.on('pageerror',e=>errors.push(e.message));try{await page.goto(baseUrl.href,{waitUntil:'domcontentloaded'});await page.locator('#loadingScreen').waitFor({state:'hidden',timeout:15000});const opener=page.locator('#settingsButton');await opener.waitFor({state:'visible'});await page.waitForFunction(()=>typeof window.getOfflineAppSettingsState==='function',{timeout:15000});assert.equal(await page.locator('#offlineAppRail,#offlineAppPanel,#offlineInstallAction').count(),0,'Global install rail/panel/action must not exist.');await opener.click();const overlay=page.locator('#settingsOverlay');await overlay.waitFor({state:'visible',timeout:15000});const panel=overlay.locator('.settingsOfflinePanel');await panel.waitFor({state:'visible'});const install=panel.locator('.settingsOfflineInstallButton');assert.equal(await install.count(),1,'Settings must expose one install action.');assert.equal(await page.locator('.settingsOfflineInstallButton').count(),1,'Install action must not be duplicated globally.');assert.equal(await install.evaluate(el=>Boolean(el.closest('#settingsOverlay'))),true,'Install action must be owned by Settings overlay.');const state=await page.evaluate(()=>window.getOfflineAppSettingsState());assert.ok(state.installationLabel&&state.shellLabel&&state.connectivityLabel,'Settings state must expose installation, shell and connectivity labels.');assert.match(await panel.innerText(),/OFFLINE APP/i);assert.match(await panel.innerText(),/INSTALLATION/i);assert.match(await panel.innerText(),/OFFLINE SHELL/i);assert.match(await panel.innerText(),/CONNECTIVITY/i);if(!(await install.isDisabled())){await install.click();await page.waitForTimeout(100);assert.ok((await panel.locator('.settingsOfflineNote').innerText()).trim().length>20,'Install action must surface device/browser guidance or native-prompt outcome inside Settings.');}const data=overlay.locator('.settingsDataButton');await data.focus();assert.equal(await data.evaluate(el=>el===document.activeElement),true,'Data Management control must accept focus before connectivity rerender.');await context.setOffline(true);await page.waitForTimeout(150);assert.equal(await data.evaluate(el=>el===document.activeElement),true,'Offline-state rerender must restore focus to the equivalent Settings control.');assert.equal(await data.evaluate(el=>Boolean(el.closest('#settingsDialog'))),true,'Focus must remain inside the Settings dialog after connectivity rerender.');await context.setOffline(false);await page.waitForTimeout(150);assert.equal(await data.evaluate(el=>el===document.activeElement),true,'Online-state rerender must preserve Settings focus a second time.');const done=overlay.locator('.settingsFooter button');await done.focus();await page.keyboard.press('Tab');assert.equal(await page.locator('#settingsClose').evaluate(el=>el===document.activeElement),true,'Tab from the last control must wrap to Settings Close.');await page.keyboard.press('Escape');await overlay.waitFor({state:'hidden'});assert.equal(await opener.evaluate(el=>el===document.activeElement),true,'Escape must restore focus to the Settings tile.');assert.equal(await page.locator('#app').evaluate(el=>el.hasAttribute('inert')),false,'Closing Settings must release background inert state.');assert.equal(await page.locator('#offlineAppRail,#offlineAppPanel,#offlineInstallAction').count(),0,'Closing Settings must not leave persistent install UI behind.');assert.equal(errors.length,0,`Settings install audit emitted page errors: ${errors.join(' | ')}`);console.log('Settings-owned install UI and focus lifecycle passed: connectivity rerenders preserve focus, Tab stays trapped, Escape restores opener.');}finally{await context.close();await browser.close();}}run().catch(error=>{console.error(error);process.exitCode=1;});