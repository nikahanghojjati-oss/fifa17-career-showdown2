# RJR score provenance audit: owner-reported 81, fixed-ledger 77, fixed-ledger 78

Recorded: 2026-08-24 ET
Status: mandatory successor investigation; no score mutation authorized by this record
Current numerical authority: `REMOTE_JOINING_READINESS.json`, model `RJR-1`, `78/100`

## Evidence classification

| Score | Evidence currently available | Classification |
| --- | --- | --- |
| 81 | The owner reports that an earlier work environment displayed or reported 81. Exact-source searches of current `HEAD`, reachable Git history and available prior-context retrieval did not locate the originating statement, calculation, model, denominator or credited inputs. | Real owner-reported discrepancy, but provenance unresolved. Do not claim why it changed yet. |
| 77 | `REMOTE_JOINING_READINESS.json` event `production-stage4-stale-recovery-revision-one-convergence-proof` records `74 -> 77` on 2026-08-24. `PRODUCTION_STAGE4_CONNECTED_RIVALRY_PROOF_2026-08-24.md` independently lists `69 -> 72 -> 74 -> 77`. | Reconstructable fixed RJR-1 checkpoint. The final +3 credits stale recovery, monotonic revision-1 publication and cross-manager convergence. |
| 78 | `REMOTE_JOINING_READINESS.json` event `production-stage4-two-physical-device-hardening-proof` records `77 -> 78`. `PRODUCTION_TWO_PHYSICAL_DEVICE_CONNECTED_RIVALRY_PROOF_2026-08-24.md` independently records the same +1. | Reconstructable fixed RJR-1 checkpoint. The +1 credits Chromebook+iPhone two-physical-device hardening only. |

## Search evidence for 81

The following source searches returned no match before this audit file was created:

- exact/current source: `81/100`, `81 / 100`, RJR near 81 and Remote Joining readiness near 81;
- reachable Git history using both content-delta and regex history searches;
- available prior-context retrieval for an exact 81 statement or calculation.

This negative result means the repository cannot yet explain 81. It does not prove the owner was never shown 81.

## Applicable RJR-1 rules

`REMOTE_JOINING_READINESS.json` and `authority-history/REMOTE_JOINING_READINESS_MODEL_RJR1_2026-08-20.md` require:

- a decrease only when previously credited evidence is invalidated or a proven regression removes capability;
- a new model version, explicit rationale and historical backcast if weights or denominator change;
- no points for source, PRs, CI, documentation, WEC/SLE or screenshots themselves.

The model provenance also records that RJR-1 began with a reconstructed baseline of approximately 58 after earlier denominator drift between prerequisite completion and true end-to-end Remote Joining readiness. That makes a pre-RJR or non-comparable 81 plausible, but it is only a hypothesis until the originating 81 artifact is found.

## Mandatory successor investigation

1. Locate the exact 81 artifact in the prior chat/export, Library file, older handoff, branch, tag, commit, issue or PR evidence available to the successor.
2. Record its timestamp, exact wording, model/version, denominator, domain weights and every credited capability.
3. Backcast the 81 inputs through fixed RJR-1 and compare them checkpoint-by-checkpoint with the ledger's `58, 59, 61, 62, 63, 69, 72, 73, 74, 77, 78` history.
4. If 81 was already comparable RJR-1, identify the exact invalidated evidence or regression that legally produced 77. If none exists, flag the 77 calculation/reporting boundary as inconsistent and propose a tested correction.
5. If 81 used a different or intuition-based denominator, label it non-comparable, explain the denominator drift and publish the RJR-1 backcast instead of describing 81 to 77 as a capability loss.
6. Recalculate the five fixed domains from evidence. Change the official ledger only after the audit produces exact comparable proof and all RJR/SLE contracts pass.

Until this audit closes, the evidence-backed official score remains `78/100`; the reason for the owner-observed `81 -> 77` transition remains explicitly unresolved.
