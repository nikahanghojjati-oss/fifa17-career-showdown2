# Master Developer Implementation Prompt — Visual R8.25 Proposal Complete

Use this file when transitioning the finished visual proposal into the master development lane.

Status: VISUAL PROPOSAL COMPLETE / SELECTIVE IMPLEMENTATION REQUIRED

Controller: GPT-5.6 Sol reasoning

Image generation: CLOSED

## Mission

Integrate the finished Career Mode Showdown R8 visual system selectively into then-current production, beginning with the bounded global + Home slice, without changing gameplay, routing authority, storage authority, Firebase authority, scoring, Save Library identity, pairing, Remote Joining or billing state.

Do not merge the visual proposal branch wholesale.

Do not begin by generating images.

## Mandatory first read

Read in this order:

1. `R8_25_PROPOSAL_FINISH_EXACT_HOME_GLOBAL_INTEGRATION_BLUEPRINT.md`
2. `R8_25_PROPOSAL_COMPLETE_ACCEPTANCE.json`
3. `implementation/r8-black-gold-presentation.proposed.css`
4. `implementation/r8-final-art-layer.proposed.js`
5. `implementation/R8_25_ASSET_PATH_MANIFEST.json`
6. `MASTER_DEVELOPER_HANDOFF.md`
7. `R8_24_PACKAGE_5_RESPONSIVE_ACCESSIBILITY_NONINTERFERENCE_QA.md`
8. `R8_24_PACKAGE_5_STATIC_ACCESSIBILITY_QA_RESULTS.json`
9. `R8_23_PACKAGE_4_EXACT_DOM_PRESENTATION_ASSET_DECISION.md`
10. `R8_22_R11_DRIFT_AND_PACKAGE_3_STATIC_COMPOSITION_QA.md`
11. `R8_18_PACKAGE_1_2_FROZEN_MASTER_PLACEMENT_CONTRACT.md`
12. `00_VISUAL_REASONING_CONTROLLER_CONTRACT.md`

## Last verified authority before R8.25 seal

- main: `32723b900dee45f689391908dc976f0ce5d06dd5`
- runtime / asset revision: `1.9.1-r11`
- proposal branch: `visual/r8-5-approved-character-identity-lock`

Independently re-resolve all three before implementation. The development lane may have advanced after the visual proposal was sealed.

## Proposal completion meaning

The visual proposal is 100% complete.

This does not mean production integration has happened.

The remaining tasks are developer-lane tasks:

- materialize the already-frozen A01/A02 binaries into the repository at approved deployment paths;
- verify their SHA-256 values;
- selectively adapt the R8 proposal CSS/JS to then-current production;
- implement bounded slices;
- run real linked-browser acceptance;
- merge/deploy only under normal development/release authority.

A03/A04/A05/A06 are not unfinished required art. Current evidence proved reuse/state-derived presentation is sufficient. Do not generate them just to fill historical slots.

## First implementation branch scope

Create a bounded developer branch from freshly resolved main.

The first branch should contain only:

1. R8 black/gold presentation tokens and global shell treatment;
2. the explicit `#appRuntimeNotice button:focus-visible` guard;
3. the noninteractive final-art infrastructure;
4. Home presentation;
5. frozen A02 Daniel left + A01 Nik right on wide desktop only;
6. compact/mobile omission rules;
7. no domain-state change.

Do not include Create Showdown, League Wheel, Club Assignment or Package 3/4 styling until this slice passes.

## Current r11 Home reconciliation

At the last verified baseline:

- `index.html` owns the six real Home menu buttons and media shell;
- `js/menuExperience.js` creates the media selector and currently injects the Marco Reus `.menuCoverAthlete` treatment into `#continueCareer`;
- `js/app.js` loads `css/visual-fidelity-r3.css`;
- `visual-fidelity-r3.css` protects the current loading composition and also styles the Reus Home tile.

First-slice R8 strategy:

- preserve the loading-screen Reus treatment;
- do not delete the Reus behavior path initially;
- load/adapt the R8 stylesheet after current visual-fidelity CSS;
- use the root gate `html[data-r8-visual="active"]`;
- under that gate, suppress Home `.menuCoverAthlete` and `#menuAthleteCredit` visually;
- use R8 A02/A01 outer/background rails instead;
- leave all Home control/media behavior intact.

This is intentionally reversible and behavior-minimizing.

## Frozen shipping assets

A01 Nik Core Thinking Hero:

- source file ID `file_00000000bbf081f7b29944352f5e76a5`
- SHA-256 `17972b8afb73b90483c8f874c4bd964ac1bda196daa76e134a6030676bbd4219`
- dimensions 1086x1448 RGBA
- expected deploy path `assets/visual/r8/a01-nik-core-thinking-hero.png`

A02 Daniel Core Pointing Hero:

- source file ID `file_000000007fe081f59bbac0a09a9eeca3`
- SHA-256 `9b1545b52a5d96a240c92b9901dcd8a4558148dde05a0331882df7bd988177cc`
- dimensions 1086x1448 RGBA
- expected deploy path `assets/visual/r8/a02-daniel-core-pointing-hero.png`

Do not regenerate either one. Verify hashes after materialization.

## First-slice viewport rule

R8.25 chooses the conservative production starting point:

- >=1280px: full approved Home Daniel-left / Nik-right composition;
- 1180-1279px: reduced/cropped treatment only if clear;
- <=1179px: omit both large figures;
- 1024x768 Chromebook target: no large figures;
- 390x844 mobile target: no large figures.

Black/gold UI remains active at every width.

Artwork disappears before any live UI compromise.

## Required first-slice acceptance

On one exact implementation head prove:

- startup finishes normally;
- Home opens normally;
- all six Home buttons retain behavior;
- Continue enable/disable and dynamic text remain correct;
- media selector is still complete;
- Play/Pause/Mute still work;
- art wrappers are `aria-hidden` and noninteractive;
- no focusable descendants exist in final art;
- runtime-notice dismiss has explicit visible focus;
- no yellow-only focus signal is relied on against light UI;
- no horizontal overflow at 1600x900, 1024x768 or 390x844;
- no character art at 1024x768 or 390x844;
- reduced-motion changes decoration only;
- no storage behavior changes;
- no scoring changes;
- no Save Library identity changes;
- no pairing/Remote Joining changes;
- no Firebase provider/rules changes;
- Firebase remains Spark-only;
- billing remains off;
- removing/disabling R8 final art leaves a complete functioning app.

Do not expand scope until this passes.

## After first-slice pass

Continue bounded integration in this order:

1. Create Showdown;
2. League Wheel;
3. Club Assignment;
4. Showdown Home / Package 3 competitive surfaces under sealed state-authority rules;
5. Package 4 utilities with no character art;
6. final accessibility polish;
7. linked-browser wide/Chromebook/mobile keyboard/pointer/touch/reduced-motion acceptance.

## Generation gate

Generation remains CLOSED throughout normal integration.

Only return to GPT-5.6 Sol for an asset decision if integrated evidence proves all of the following:

- exact screen/state is known;
- exact viewport is known;
- exact protected DOM region is known;
- A01/A02 deterministic reuse fails;
- CSS/SVG/procedural treatment fails;
- the missing asset materially improves the actual product rather than filling a historical ledger slot.

Then seal a generation brief before any image-model call.

## Permanent cost/authority locks

- Firebase Spark only;
- billing permanently off;
- no Blaze;
- no Cloud Functions;
- no Cloud Run;
- no paid visual dependency;
- no artwork protocol fields;
- no visual-state Firestore writes;
- no generated image bytes through Firebase;
- no scoring mutation as visual work;
- no Save Library authority mutation as visual work;
- no pairing/Remote Joining authority mutation as visual work.

## Finish condition for the developer lane

Do not call the shipping visual integration complete merely because the proposal is complete.

The developer lane finishes only after selective integration and actual linked-browser acceptance on the integrated candidate.
