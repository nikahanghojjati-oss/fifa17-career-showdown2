# Master Developer Implementation Prompt — Visual R8.24

Use this file when the owner hands the visual proposal to the next master development session.

## Mission

Selectively integrate the approved Career Mode Showdown visual proposal into the exact current production application without changing product authority, gameplay behavior, storage, Firebase rules, billing state or private-session semantics.

The visual proposal branch is:

`visual/r8-5-approved-character-identity-lock`

Do not merge it wholesale. Read the proposal as design/QA authority, then implement bounded production slices against current `main`.

## First action

Before editing anything:

1. independently resolve current `main` SHA and runtime/asset revision;
2. resolve the exact current visual branch head;
3. inspect the current production DOM/CSS/JS for the first slice you intend to integrate;
4. compare current production truth against the R8.24 handoff;
5. if production advanced, adapt the proposal forward rather than forcing production backward.

Last verified production checkpoint from the visual lane:

- main: `32723b900dee45f689391908dc976f0ce5d06dd5`
- runtime / asset revision: `1.9.1-r11`

The preceding visual QA baseline was `61e16bb0357352a5c38c02aa072233e851226caf`. The only observed drift to `32723b9...` was milestone/readiness/test metadata; `index.html` remains `1.9.1-r11`, so the Package 5 DOM/CSS analysis remains applicable. Reverify this again when starting implementation.

## Mandatory visual read order

1. `MASTER_DEVELOPER_HANDOFF.md`
2. `VISUAL_TRACK_STATUS.json`
3. `R8_24_PACKAGE_5_RESPONSIVE_ACCESSIBILITY_NONINTERFERENCE_QA.md`
4. `R8_24_PACKAGE_5_STATIC_ACCESSIBILITY_QA_RESULTS.json`
5. `R8_23_PACKAGE_4_EXACT_DOM_PRESENTATION_ASSET_DECISION.md`
6. `R8_22_R11_DRIFT_AND_PACKAGE_3_STATIC_COMPOSITION_QA.md`
7. `R8_22_PACKAGE_3_STATIC_QA_RESULTS.json`
8. `R8_21_PACKAGE_3_EXACT_DOM_PRESENTATION_ASSET_DECISION.md`
9. `R8_20_PACKAGE_1_2_STATIC_RESPONSIVE_OBSTRUCTION_QA.md`
10. `R8_18_PACKAGE_1_2_FROZEN_MASTER_PLACEMENT_CONTRACT.md`
11. `R8_10_PACKAGE_1_2_DOM_PRESENTATION_SPEC.md`
12. `R8_10_ASSET_LEDGER.json`
13. `APPROVED_CHARACTER_IDENTITY_LOCK.json`
14. `IDENTITY_EXPRESSION_CONTRACT.md`

## Controller rule

GPT-5.6 Sol reasoning is the visual operating system. Image generation is subordinate execution only.

Do not generate an image because an old asset slot exists, because the owner uploaded a reference, or because the next visual step sounds cinematic.

Generation is CLOSED at handoff.

Only reopen it if integrated live-DOM evidence proves a specific missing asset that cannot be solved safely through the frozen assets, crop/mask/mirror, CSS, SVG, procedural treatment or typography. Before any generation call, seal a brief defining target surface/state, reference authority, safe zones, reuse destinations and forbidden outputs.

## Frozen assets

A01 Nik core thinking hero:

- owner-approved frozen;
- file ID `file_00000000bbf081f7b29944352f5e76a5`;
- SHA-256 `17972b8afb73b90483c8f874c4bd964ac1bda196daa76e134a6030676bbd4219`;
- 1086x1448 RGBA.

A02 Daniel core pointing hero:

- owner-approved frozen;
- file ID `file_000000007fe081f59bbac0a09a9eeca3`;
- SHA-256 `9b1545b52a5d96a240c92b9901dcd8a4558148dde05a0331882df7bd988177cc`;
- 1086x1448 RGBA.

Do not regenerate either asset for placement changes.

A03/A04 tactical and A05/A06 celebration are HOLD slots only. They are not current requirements. Package 3 proved A01/A02 reuse plus presentation-only state emphasis is sufficient across the current mapped states.

Optional C01/C02 pack-holder candidates are not frozen shipping assets. Test them only as an optional wide-desktop Club Assignment opening-stage cinematic sublayer after core integration is stable. Omit them on compact/mobile.

## Presentation architecture

The production application remains the authority. Visual integration must be additive.

Use the current live DOM for every interactive or dynamic element. Generated or decorative assets must never replace buttons, forms, labels, score data, wheel labels, status messages, table data, Save Library identities, recovery choices, private-session capabilities or remote authority state.

Final-art wrappers must:

- use `pointer-events:none`;
- use `user-select:none`;
- be `aria-hidden=true`;
- have no focusable descendants;
- remain outside functional grid/flex sizing;
- stay behind real controls/status regions;
- never create horizontal document overflow.

If artwork conflicts with UI, artwork loses and is cropped, faded or omitted.

## Visual direction

Preserve the approved richer black/gold Career Mode Showdown identity. Avoid a pale, empty or generic dashboard treatment.

Use black/gold/cream as the premium presentation base, with cyan and semantic success/warning/danger colors only where they communicate real application state.

Maintain strong editorial typography, metallic/stadium atmosphere, trophy/sport motifs and two-manager rivalry rhythm while respecting the exact current product structure.

## Slice 1 — global presentation infrastructure

Create the smallest production-safe foundation first:

- black/gold design tokens;
- additive presentation stylesheet/layering hooks;
- noninteractive final-art wrapper contract;
- responsive omission/crop rules;
- reduced-motion safety;
- explicit runtime-notice dismiss `:focus-visible` treatment;
- no domain-state code changes.

Do not replace current application initialization or routing.

Acceptance before advancing:

- application starts normally;
- no horizontal overflow;
- keyboard focus remains visible;
- art wrappers cannot receive pointer/focus events;
- reduced-motion removes decorative motion only;
- zero storage/Firebase behavior changes.

## Slice 2 — Home

Use approved composition direction with Daniel on the left and Nik on the right where wide desktop allows it.

Real `.fifaMenuGrid`, all menu tiles, media controls and bottom strip remain primary.

Do not make the characters clickable tile backgrounds and do not rasterize menu labels into artwork.

At compact widths reduce/crop or omit characters before touching the live menu geometry. Mobile omits large character art.

## Slice 3 — Create Showdown

A02 may occupy the left outer rail; A01 may occupy the right outer rail.

Never overlap the live form card, validation, season-length control, Start Showdown or Back action.

No Create-specific generation is authorized.

## Slice 4 — League Wheel

A02's pointing pose has a legitimate wide-desktop purpose and may visually direct attention toward the real wheel without entering its protected region. A01 balances the opposite rail.

The real wheel, pointer, five league labels, selected-league status, shared state message, `SPIN WHEEL` and Back remain unobstructed.

No League-specific generation is authorized.

## Slice 5 — Club Assignment

Use only narrow edge framing from A01/A02 if it remains safe. The live pack cards, reveal progression, VS state, confirmation, status and CTAs own the screen.

Do not push the pack shell inward to preserve art.

Optional C01/C02 pack-holder candidates may be evaluated later only for the existing wide-desktop `opening` stage. They may not determine club choice or replace the real cards.

## Slice 6 — Package 3 season/competitive surfaces

Use the sealed Package 3 state matrix as the visual-state contract.

Transfer Challenge:

- wide desktop A01/A02 outer rails only;
- omit at compact/mobile;
- timer/forms/status/actions dominate.

Shared Results and Shared Season Commit:

- remain neutral until actual authoritative outcome state exists;
- never reveal or imply hidden rival data through artwork.

Shared canonical scoring:

- winner/draw presentation may derive only from actual authoritative `SCORING_RECONCILED` state after the acknowledged Season Commit gates;
- no new Firebase visual-state write;
- no second winner authority.

Local Season Summary:

- presentation may derive from completed `roundRecord.winner`.

Current evidence does not justify A03/A04/A05/A06 generation.

## Slice 7 — Package 4 utilities

Do not use A01/A02 as generic utility decoration.

Statistics, Trophy Room and Legacy:

- prioritize records, tables, score hierarchy, archive readability and procedural/CSS/SVG motifs.

Trophy Room:

- procedural/CSS/SVG trophy treatment first;
- no character cabinet decoration by default;
- raster atmosphere remains conditional and closed.

Rule Book:

- reading surface only; no character art.

Settings:

- preserve modal keyboard/focus semantics and status hierarchy;
- no character art.

Save Library:

- runtime save/profile identity is authoritative;
- never equate A01/A02 with arbitrary Local Profiles.

Restore/Recovery:

- exact plan, conflict, warning, rollback and critical-recovery states dominate;
- no character art.

Connected Rivalry:

- preserve the distinction between refresh/preview and publish/apply;
- never visually claim remote completion before runtime authority.

Remote Joining:

- preserve private capability, page-memory, unresolved/retry and terminal-state semantics.

Global notices:

- keep semantic notice styling dominant;
- add an explicit high-contrast focus-visible treatment for the dismiss button.

## Accessibility and contrast guards

Do not rely on yellow alone for focus indication against white/light surfaces. At the verified token values, `#f0d900` against white is only approximately 1.44:1.

Retain or add a dark/blue high-contrast boundary where focus appears on light UI.

Preserve Settings:

- `role=dialog`;
- `aria-modal=true`;
- labelled heading;
- Tab/Shift+Tab focus wrap;
- Escape close;
- background `inert`/`aria-hidden` while open;
- prior-focus restoration;
- radio keyboard behavior;
- targeted focus restoration after re-render.

Do not add decorative nodes to the tab order.

## Zero-dollar / remote locks

Permanent constraints:

- Firebase Spark only;
- billing off;
- no Blaze;
- no Cloud Functions;
- no Cloud Run;
- no paid image CDN;
- no paid fonts;
- no paid stock dependency;
- no Firebase artwork bytes;
- no visual-state Firestore writes;
- no artwork protocol fields.

Visual integration must not alter scoring, Save Library authority, pairing, Remote Joining, provider authorization or Firestore Rules.

## Required integrated acceptance

After each bounded slice, test the actual integrated application rather than relying on proposal screenshots.

Minimum final acceptance matrix:

- wide desktop around 1600x900;
- compact desktop / Chromebook around 1024x768;
- mobile portrait around 390x844;
- keyboard-only navigation;
- visible focus on all interactive elements near visual changes;
- pointer/touch usability;
- reduced-motion preference;
- no horizontal overflow;
- no decorative hit testing;
- no covered status/validation/error text;
- no authority/state mismatch;
- no zero-dollar violation.

Do not claim the visual integration complete until the linked production candidate passes real browser acceptance.

## Stop / return-to-visual rule

If a concrete integrated composition exposes a genuine missing visual asset, do not improvise one inside the development lane. Record the exact screen/state, viewport, protected DOM region and reuse failure, then return that evidence to GPT-5.6 Sol for a generation-gate decision.

Otherwise continue implementation without image generation.
