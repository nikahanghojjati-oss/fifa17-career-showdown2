# Master Developer Handoff — R8.24 Visual Proposal Integration

Classification: VISUAL PROPOSAL ONLY / SELECTIVE IMPLEMENTATION REQUIRED

Controller: GPT-5.6 Sol reasoning

Do not merge the visual branch wholesale. Do not deploy automatically. Re-resolve current live `main`, runtime/asset revision and exact DOM before implementing any slice.

## Current proposal authority

Last verified production authority before this handoff write:

- live main: `61e16bb0357352a5c38c02aa072233e851226caf`
- runtime / asset revision: `1.9.1-r11`
- visual proposal branch: `visual/r8-5-approved-character-identity-lock`
- visual branch before final handoff packaging: `189b40b467a5b012a857f5115d488a49dddc8e02`

The parallel development lane may advance at any time. Current production truth always outranks this checkpoint.

## Read order

1. `00_VISUAL_REASONING_CONTROLLER_CONTRACT.md`
2. `VISUAL_TRACK_STATUS.json`
3. `R8_24_PACKAGE_5_RESPONSIVE_ACCESSIBILITY_NONINTERFERENCE_QA.md`
4. `R8_24_PACKAGE_5_STATIC_ACCESSIBILITY_QA_RESULTS.json`
5. `R8_23_PACKAGE_4_EXACT_DOM_PRESENTATION_ASSET_DECISION.md`
6. `R8_22_R11_DRIFT_AND_PACKAGE_3_STATIC_COMPOSITION_QA.md`
7. `R8_22_PACKAGE_3_STATIC_QA_RESULTS.json`
8. `R8_21_PACKAGE_3_EXACT_DOM_PRESENTATION_ASSET_DECISION.md`
9. `R8_20_PACKAGE_1_2_STATIC_RESPONSIVE_OBSTRUCTION_QA.md`
10. `R8_18_PACKAGE_1_2_FROZEN_MASTER_PLACEMENT_CONTRACT.md`
11. `R8_10_PACKAGE_1_2_DOM_PRESENTATION_SPEC.md`
12. `R8_10_ASSET_LEDGER.json`
13. `APPROVED_CHARACTER_IDENTITY_LOCK.json`
14. `IDENTITY_EXPRESSION_CONTRACT.md`

## Operating hierarchy

GPT-5.6 Sol reasoning remains the visual operating system. Image generation is subordinate execution only.

Do not generate simply because an old ledger slot exists, a reference image is available, or the owner says continue. A generation call requires a concrete live-DOM need, proven reuse failure, sealed brief, safe zones and explicit reference authority.

At this checkpoint image generation is CLOSED.

## Frozen identity authority

A01 Nik core thinking hero is owner-approved and frozen.

- file ID: `file_00000000bbf081f7b29944352f5e76a5`
- SHA-256: `17972b8afb73b90483c8f874c4bd964ac1bda196daa76e134a6030676bbd4219`
- dimensions: 1086x1448 RGBA

A02 Daniel core pointing hero is owner-approved and frozen.

- file ID: `file_000000007fe081f59bbac0a09a9eeca3`
- SHA-256: `9b1545b52a5d96a240c92b9901dcd8a4558148dde05a0331882df7bd988177cc`
- dimensions: 1086x1448 RGBA

Never regenerate A01 or A02 for crop, scale, mirror, responsive placement or screen assembly. Use deterministic presentation transforms only.

A03/A04 tactical and A05/A06 celebration remain planned hold slots. Current Package 3 evidence proved that A01/A02 plus presentation-only luminance/state treatment are sufficient. They are not implementation requirements and must not be generated merely to complete an old asset count.

Optional C01/C02 pack-holder candidates remain owner-liked but non-frozen and optional. They are allowed only as a bounded wide-desktop Club Assignment opening-stage cinematic sublayer if the master developer later proves the exact integrated composition remains safe. Omit them at compact/tablet/mobile widths.

## Exact presentation architecture

Integration is additive and presentation-only:

1. current production DOM/CSS/JS remains behavioral authority;
2. add black/gold presentation treatment selectively;
3. add a decorative final-art layer only where approved;
4. keep every real button, input, label, score, status, table, wheel, card and action as live DOM;
5. decorative wrappers use `pointer-events:none`, `user-select:none` and `aria-hidden=true`;
6. decorative nodes contain no focusable descendants;
7. artwork never participates in functional grid/flex sizing;
8. crop, fade or omit artwork before moving or shrinking a functional control;
9. no visual state may create or modify storage/Firebase/gameplay authority.

Artwork must be removable without breaking the product.

## Package 1 / 2 implementation truth

Home, Create Showdown, League Wheel and Club Assignment have already passed static responsive/obstruction analysis.

Wide desktop may use frozen A01/A02 only inside mapped outer/background safe zones.

- Home: Daniel left, Nik right is approved direction, but menu tiles/media controls remain dominant.
- Create Showdown: A02 left and A01 right outer rails are safe; no body/hand/glow may enter the form.
- League Wheel: Daniel may point toward, never into, the real wheel; `SPIN WHEEL`, selected league and state messaging remain fully clear.
- Club Assignment: character art is edge crop or omission only; real pack cards, progression, confirmation and CTA own the screen.

At approximately 1024x768, crop/omit aggressively. At mobile portrait, omit large character figures.

Do not create replacement whole-page screenshots.

## Package 3 implementation truth

The sealed Package 3 matrix covered 22 states across 1600x900, 1024x768 and 390x844: 66/66 static cases passed with zero recorded horizontal overflow, protected-DOM intersections or central-stage intersections.

Transfer Challenge:

- wide desktop may use A01/A02 role-aligned outer rails;
- compact/mobile omit large figures;
- timer, forms, privacy/status and action controls remain dominant;
- no A03/A04 tactical generation is currently justified.

Shared Results and Shared Season Commit:

- remain neutral before canonical result authority;
- do not use character emotion to imply hidden opponent data or completion before runtime authority.

Shared canonical scoring under r11:

- presentation may derive winner/draw emphasis only when `CareerModeProductionSharedCanonicalScoring.getState()` is authoritative and phase is `SCORING_RECONCILED` after the acknowledged Season Commit gates;
- use presentation-only emphasis; create no second winner authority and write no visual state to Firebase.

Local Season Summary:

- completed `roundRecord.winner` may drive presentation-only winner/draw emphasis.

A05/A06 celebration generation remains closed because A01/A02 reuse plus CSS outcome emphasis passed current static composition QA.

Tiebreak remains deferred because no dedicated live runtime/route/DOM authority was present at the verified r11 checkpoint.

## Package 4 implementation truth

Package 4 is utility/data first and uses no owner-character art.

- Statistics: data hierarchy, score typography, cards/tables only.
- Trophy Room: CSS/SVG/procedural trophy language first. No Nik/Daniel cabinet decoration by default. Raster atmosphere fallback remains closed.
- Legacy: archive readability plus destructive/recovery hierarchy. No character art.
- Rule Book: reading surface. No character art.
- Settings: utility/focus/status first. No character art.
- Save Library: real profile/save identity is runtime authority. Never use A01/A02 as generic profile avatars or identity indicators.
- Restore/Recovery: exact plan, warning, rollback and critical-recovery semantics dominate. No character art.
- Connected Rivalry: distinguish read-only refresh/preview from mutating publish/apply. No character art or fake completion state.
- Remote Joining: preserve exact private capability and unresolved/retry semantics. No character art.
- Global notices: semantic status layer only. No character art.

## Package 5 static acceptance

Current result: `STATIC_PROPOSAL_PASS_WITH_IMPLEMENTATION_GUARDS`.

No static/source blocker requires another concept cycle or new raster generation.

The current proposal is ready for selective master-developer implementation, followed by real integrated browser acceptance.

Required implementation guards:

1. add an explicit high-contrast `:focus-visible` treatment for the global runtime-notice dismiss button;
2. do not rely on yellow alone as a focus indication on white/light surfaces; retain a dark/blue high-contrast boundary;
3. keep final-art wrappers noninteractive, `aria-hidden` and outside functional layout sizing;
4. preserve current responsive behavior of all Package 4 utility surfaces;
5. preserve Settings modal focus trap, Escape close, background inertness and focus restoration;
6. reduced-motion must remove decorative movement only, never change gameplay/session/reveal state;
7. after integration, perform actual browser keyboard, pointer/touch, responsive, obstruction and accessibility acceptance.

Static contrast spot checks support the black/gold token direction on dark surfaces. The yellow `#f0d900` against white is only about 1.44:1, so yellow-only thin focus outlines on light UI are specifically prohibited.

## Zero-dollar and authority locks

The visual implementation may not:

- enable billing;
- move off Firebase Spark;
- require Blaze, Cloud Functions or Cloud Run;
- add paid image hosting, paid fonts or paid stock dependencies;
- write visual state to Firestore;
- add artwork fields to remote protocols;
- transfer generated image bytes through Firebase;
- modify scoring, Save Library authority, pairing, Remote Joining, provider authorization or generated Firestore Rules as part of visual work.

Zero-dollar remains permanent.

## Recommended selective implementation order

Implement one bounded slice at a time against freshly resolved main:

1. global black/gold tokens and noninteractive final-art infrastructure;
2. Home;
3. Create Showdown;
4. League Wheel;
5. Club Assignment;
6. Showdown Home / Package 3 competitive surfaces using the sealed state rules;
7. Package 4 utility styling without character art;
8. global notice focus guard and final accessibility polish;
9. integrated responsive/browser acceptance.

Do not bulk-copy stale proposal CSS over newer production behavior. Adapt the approved presentation to current truth.

## Acceptance rule for each slice

A slice is acceptable only when:

- current product behavior is unchanged;
- all real controls remain visible, focusable and unobstructed;
- approved Nik/Daniel identity is preserved where used;
- artwork disappears before UI compromise;
- screen-state emotion is derived only from real authority;
- Package 4 remains data/utility first;
- no horizontal overflow is introduced;
- reduced-motion remains safe;
- no paid dependency or Firebase authority change is introduced;
- removing the art layer leaves a complete functioning application.

## What remains after this handoff

The visual proposal itself is at a clean implementation handoff checkpoint. The remaining work is not speculative concept generation. It is master-developer selective integration into then-current production and real linked-browser acceptance.

If integrated evidence later exposes a concrete missing expression/asset, return to the GPT-5.6 Sol generation gate and prove that need before creating anything new.
