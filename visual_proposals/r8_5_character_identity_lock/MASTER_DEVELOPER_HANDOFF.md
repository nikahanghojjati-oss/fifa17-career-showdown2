# Master Developer Handoff — R8.25 Visual Proposal Complete

Classification: VISUAL PROPOSAL COMPLETE / SELECTIVE IMPLEMENTATION REQUIRED

Controller: GPT-5.6 Sol reasoning

Image generation: CLOSED

Do not merge the visual branch wholesale. Do not deploy automatically. Re-resolve current live `main`, runtime/asset revision and exact DOM before implementing any slice.

## Current proposal authority

Last verified production authority before the R8.25 seal:

- live main: `32723b900dee45f689391908dc976f0ce5d06dd5`
- runtime / asset revision: `1.9.1-r11`
- visual proposal branch: `visual/r8-5-approved-character-identity-lock`

The visual lane has not modified production `main` and has not deployed production.

The parallel development lane may advance after this handoff. Current live production truth always outranks this checkpoint.

## Proposal completion status

Visual proposal progress: `100%`.

This 100% means the proposal itself is finished. It does not claim shipping integration is complete.

The following are complete:

- approved identity authority;
- exact production DOM/runtime screen mapping;
- A01 Nik and A02 Daniel frozen core masters;
- screen/state expression architecture through reuse-first and authoritative state derivation;
- R8 black/gold presentation system;
- responsive safe-zone and obstruction analysis;
- Package 3 state/privacy/outcome mapping;
- Package 4 utility/data presentation decisions;
- Package 5 static accessibility/noninterference QA;
- concrete global + Home integration source;
- developer handoff and acceptance plan.

A03/A04/A05/A06 are not missing required work. Current evidence proved they are unnecessary for the approved implementation architecture. Do not generate them merely to fill historical slots.

## Mandatory successor read order

1. `START_NEXT_DEVELOPER_VISUAL_R8_25_PROPOSAL_COMPLETE_SAFE_TRANSFER.md`
2. `R8_25_PROPOSAL_FINISH_EXACT_HOME_GLOBAL_INTEGRATION_BLUEPRINT.md`
3. `R8_25_PROPOSAL_COMPLETE_ACCEPTANCE.json`
4. `MASTER_DEVELOPER_IMPLEMENTATION_PROMPT_R8_25.md`
5. `implementation/r8-black-gold-presentation.proposed.css`
6. `implementation/r8-final-art-layer.proposed.js`
7. `implementation/R8_25_ASSET_PATH_MANIFEST.json`
8. `R8_24_PACKAGE_5_RESPONSIVE_ACCESSIBILITY_NONINTERFERENCE_QA.md`
9. `R8_24_PACKAGE_5_STATIC_ACCESSIBILITY_QA_RESULTS.json`
10. `R8_23_PACKAGE_4_EXACT_DOM_PRESENTATION_ASSET_DECISION.md`
11. `R8_22_R11_DRIFT_AND_PACKAGE_3_STATIC_COMPOSITION_QA.md`
12. `R8_22_PACKAGE_3_STATIC_QA_RESULTS.json`
13. `R8_21_PACKAGE_3_EXACT_DOM_PRESENTATION_ASSET_DECISION.md`
14. `R8_20_PACKAGE_1_2_STATIC_RESPONSIVE_OBSTRUCTION_QA.md`
15. `R8_18_PACKAGE_1_2_FROZEN_MASTER_PLACEMENT_CONTRACT.md`
16. `R8_10_PACKAGE_1_2_DOM_PRESENTATION_SPEC.md`
17. `R8_10_ASSET_LEDGER.json`
18. `APPROVED_CHARACTER_IDENTITY_LOCK.json`
19. `IDENTITY_EXPRESSION_CONTRACT.md`
20. `00_VISUAL_REASONING_CONTROLLER_CONTRACT.md`

## GPT-5.6 controller rule

The user's prompt is not itself an image-generation command.

GPT-5.6 Sol first interprets the request against roadmap, live DOM/runtime authority, current assets, safe zones, cost and product value. Image generation is a subordinate execution specialist used only if GPT-5.6 proves a mapped asset need and seals a bounded generation brief.

At this handoff generation is CLOSED.

## Frozen identity authority

A01 Nik Core Thinking Hero:

- file ID `file_00000000bbf081f7b29944352f5e76a5`
- SHA-256 `17972b8afb73b90483c8f874c4bd964ac1bda196daa76e134a6030676bbd4219`
- 1086x1448 RGBA
- status `OWNER_APPROVED_FROZEN`
- expected deploy path `assets/visual/r8/a01-nik-core-thinking-hero.png`

A02 Daniel Core Pointing Hero:

- file ID `file_000000007fe081f59bbac0a09a9eeca3`
- SHA-256 `9b1545b52a5d96a240c92b9901dcd8a4558148dde05a0331882df7bd988177cc`
- 1086x1448 RGBA
- status `OWNER_APPROVED_FROZEN`
- expected deploy path `assets/visual/r8/a02-daniel-core-pointing-hero.png`

Never regenerate either master for crop, scale, mirror, responsive placement, avatar chips or screen assemblies.

## Exact first developer slice

The first selective implementation slice is now concretely specified, not just conceptual.

Scope:

- global black/gold presentation tokens and shell;
- explicit runtime-notice dismiss `:focus-visible` guard;
- noninteractive final-art infrastructure;
- Home presentation only;
- A02 Daniel left and A01 Nik right on wide desktop only;
- large character omission on Chromebook/mobile;
- no gameplay/storage/Firebase/authority change.

Concrete source authority:

- `R8_25_PROPOSAL_FINISH_EXACT_HOME_GLOBAL_INTEGRATION_BLUEPRINT.md`
- `implementation/r8-black-gold-presentation.proposed.css`
- `implementation/r8-final-art-layer.proposed.js`
- `implementation/R8_25_ASSET_PATH_MANIFEST.json`

These files remain proposal-only until a master developer reconciles them with then-current production.

## r11 Home reconciliation already solved in the proposal

At the last verified main:

- `index.html` owns the six real Home buttons and media shell;
- `js/menuExperience.js` dynamically creates media selector controls and injects the current Marco Reus `.menuCoverAthlete` treatment into `#continueCareer`;
- `js/app.js` loads `css/visual-fidelity-r3.css`;
- `visual-fidelity-r3.css` contains both the protected loading treatment and current Reus Home treatment.

R8.25 first-slice decision:

- keep protected loading Reus intact;
- do not delete the Reus code path in the first slice;
- adapt/load R8 presentation after current visual-fidelity CSS;
- use `html[data-r8-visual="active"]` as a reversible presentation gate;
- under that gate suppress Home `.menuCoverAthlete` and `#menuAthleteCredit` visually;
- render frozen Daniel/Nik only in the separate decorative Home layer;
- keep all live menu/media behavior unchanged.

This prevents a crowded Reus + Daniel + Nik Home while minimizing functional risk.

## First-slice viewport contract

- >=1280px: approved Daniel-left / Nik-right wide Home composition;
- 1180-1279px: reduced/cropped figures only if unobstructed;
- <=1179px: omit both large figures;
- 1024x768 Chromebook: no large characters;
- 390x844 mobile: no large characters.

The black/gold UI remains present at all widths.

Artwork always disappears before UI compromise.

## Package 1 / 2 truth

Home, Create Showdown, League Wheel and Club Assignment passed static responsive/obstruction analysis.

After the first Home slice passes, continue bounded integration:

- Create Showdown: A02 left / A01 right outer rails on wide desktop; no form overlap; mobile omit.
- League Wheel: A02 may point toward but never enter the real wheel; CTA/status band protected; mobile omit.
- Club Assignment: extreme-edge crop or omission only; real pack cards/progression/confirmation own the screen; optional pack-holder candidates remain non-required.

## Package 3 truth

The sealed Package 3 matrix covered 22 states x 3 viewports = 66 cases and passed all 66 static cases with zero recorded horizontal-overflow failures, protected-DOM character intersections or visible character/central-stage intersections after rail clipping.

Transfer Challenge may use role-aligned A01/A02 outer rails on wide desktop only.

Shared Results and Shared Season Commit remain neutral until authoritative outcome state exists.

Shared winner/draw emphasis may derive only from authoritative `CareerModeProductionSharedCanonicalScoring.getState()` with `authoritative===true` and `phase===SCORING_RECONCILED` after the acknowledged Season Commit gates.

Local Season Summary may derive presentation from completed `roundRecord.winner`.

No visual-state Firebase write and no second winner authority are allowed.

## Package 4 truth

Package 4 is utility/data first and uses no owner-character art:

- Statistics: data hierarchy only;
- Trophy Room: CSS/SVG/procedural trophy language first;
- Legacy: archive/destructive/recovery hierarchy;
- Rule Book: reading surface;
- Settings: utility/focus/status first;
- Save Library: runtime profile/save identity remains authoritative;
- Restore/Recovery: exact plan/warning/rollback authority;
- Connected Rivalry: preserve read-only vs mutating distinction;
- Remote Joining: preserve private capability/status/retry semantics;
- global notices: semantic status layer only.

## Package 5 implementation guards

The static proposal result is `STATIC_PROPOSAL_PASS_WITH_IMPLEMENTATION_GUARDS`.

Required guards:

1. explicit high-contrast `:focus-visible` styling for the global runtime-notice dismiss button;
2. no yellow-only focus indicator on white/light surfaces;
3. decorative wrappers remain `aria-hidden`, noninteractive and outside functional layout sizing;
4. preserve current utility responsive behavior;
5. preserve Settings modal focus trap, Escape close, background inertness and focus restoration;
6. reduced-motion removes decoration only and never changes product state;
7. actual linked-browser acceptance after implementation.

## Zero-dollar and authority locks

The developer integration may not:

- enable billing;
- leave Firebase Spark;
- add Blaze, Cloud Functions or Cloud Run;
- add paid image hosting/fonts/stock dependencies;
- write visual state to Firestore;
- add artwork fields to remote protocols;
- transfer generated image bytes through Firebase;
- modify scoring as visual work;
- modify Save Library identity authority as visual work;
- modify pairing/Remote Joining authority as visual work;
- modify provider authorization or generated Firestore Rules as visual work.

## Developer acceptance order

Implement and prove one bounded slice at a time:

1. global + Home;
2. Create Showdown;
3. League Wheel;
4. Club Assignment;
5. Package 3 competitive surfaces;
6. Package 4 utilities;
7. accessibility polish;
8. linked-browser 1600x900, 1024x768 and 390x844 plus keyboard, pointer/touch and reduced-motion acceptance.

Do not bulk-copy stale proposal CSS over newer production behavior.

## Final visual-lane stop

The independent visual proposal lane is finished at 100% proposal completion and should stop here.

The next work belongs to the master developer integration lane.

Only reopen image generation if a real integrated screen proves a concrete missing asset after A01/A02 reuse, deterministic crop/mask/mirror, CSS, SVG and procedural treatments fail. GPT-5.6 Sol must seal that generation brief first.
