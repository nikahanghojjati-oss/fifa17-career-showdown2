# Career Mode Showdown — Current Successor Packet

Status: `RECOVERY_READY`

This file is the active successor bootstrap for POS9. It is deliberately current-state oriented and must be regenerated whenever its declared durable boundary changes.

## Authority precedence

1. verified live repository/runtime/provider authority for mutable facts;
2. current operating-system and product-guard authority on the live branch;
3. this current successor packet;
4. archived handoffs / historical transfer artifacts;
5. conversation, project memory, summaries, or remembered percentages.

Historical material may explain provenance but must never resurrect a retired active concept or override current live authority.

## Retired reporting concepts

`Handoff proximity`, `SHP`, and `HTR` percentage-style transfer reporting are retired from active status reporting. Historical files may contain them. They are not current continuation gates and must not be emitted as active metrics.

## Verified durable boundary

Repository: `nikahanghojjati-oss/fifa17-career-showdown2`

POS9 base main observed before bootstrap: `831a13ee93ac1b279c1f7c0985c1c5111922829f` (PR #220 POS8 merge).

Always re-resolve live `main`, active POS9 candidate head, shadow recovery head, exact-head workflows, reviews/comments, and deployed/provider state when applicable. Recorded SHAs are orientation only.

## Active work

Implement POS9 Continuous Recovery so hidden ChatGPT context/session/five-hour/weekly limits are never predicted as a safety mechanism. Continuing substantive work is safe only while a valid current successor packet already exists.

Required recovery states:

- `RECOVERY_READY`: current successor packet is valid for the last durable project boundary.
- `RECOVERY_STALE`: durable project state changed after the packet boundary and the packet must be refreshed before additional substantive work.
- `RECOVERY_BLOCKED`: a valid recovery packet cannot be established; stop mutation and preserve the last recoverable state.

A real owner/platform warning, severe observable context damage, or unavailable live authority remains a hard transition input. Account plan, message count, tool-call count, elapsed time, guessed five-hour usage, guessed weekly usage, and similar hidden-capacity estimates are forbidden safety predictors.

## POS8 retained

Keep TX-8/SRB-8/WAL-8/PG-8 shadow recovery and single-promotion discipline. Keep IMPACT-7 exact risk-based validation. POS9 strengthens recovery readiness; it does not weaken product/security/privacy/release gates.

## Permanent product locks

Billing permanently OFF. Firebase Spark only. No Blaze, Cloud Billing, Cloud Run, or Cloud Functions. App Check enforcement OFF. Firestore browser persistence memory-only. Google Auth popup-only `browserSessionPersistence` with no extra scopes. Exactly two private managers. Connected Rivalry pairing plus exact ACTIVE before league or club authority. Candidate C remains the sole destructive remote-to-local Apply authority with transaction-owned exact rollback. Canonical local storage remains exactly `careerModeShowdown.saveLibrary`, `careerModeShowdown.legacyShowdowns`, and `careerModeShowdown.preferences`. No public discovery/listing/lobby/matchmaking/community/rankings/global leaderboards. `SSJR-DUAL-FULL-SCREEN-1` remains permanent.

## Product return

POS9 earns zero SSJR/MDP credit. After POS9 is exact-head green and merged, return immediately to production Shared Setup / SSJR-1.1 two-account evidence, including the remaining production-safe denial/adverse evidence automation before unavoidable physical acceptance.
