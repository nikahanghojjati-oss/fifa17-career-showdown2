# r16 Local Reconciliation candidate

Starting authority: main `928f8c9e945f57ee1044debcc81ed4400712f605`; MDP 86.50/100; SSJR 0/100.

Frozen capability: `local-reconciliation`, weight 5. This candidate integrates Shared Journey state with the existing Connected Rivalry Candidate B/C reconciliation authority. It adds no new Firestore collection, list authority, provider mutation path or direct canonical-storage writer. Read-only preview may use the already-observed exact remote envelope while offline; Apply is explicitly denied offline and requires user confirmation online. Candidate C remains the sole destructive local Apply authority, including backup, exact remote freshness guards, stale-local guard, guarded raw transaction and rollback.

Guarded build evidence: r16 one-shot run `34525784912` passed the deterministic Local Reconciliation contract, production delegation/shell contract, controlled-browser adapter audit, inherited Stage 4 remote-to-local Candidate C contract, all 73 POS20 operations tests and `git diff --check` before creating bot candidate `54f4ca6d618ad9d507cf5325a65af6e00430bcd7`. The earlier run `34525673396` is historical failure evidence only: all r16/Candidate C tests passed, but the POS20 explicit supplemental census had not yet been extended for the two new contracts.

First full candidate POS20 run `34525881072` on historical head `cc15aecef24721ee98d329d3b84d4279ba630864` found one exact product-integration defect expressed in two lanes: `static-app-release-contracts.cjs` rejected generic internal function names `apply`, `freeze` and `base` because they collided with existing shell function names. Deterministic r16 contracts themselves passed. The bounded correction namespaced only r16 private internals (`lr*`) while preserving the public API, Candidate C delegation, provider/storage authority, and behavioral contract. No failed lane from the historical head is reused as evidence.

The corrected pre-publication candidate `703018a341d83c34fe15a43538c5e80ad145fd39` passed complete normal PR POS20 run `34526222298` / #394 on that exact head, including selector, deterministic census, operations, cognitive benchmark, INLINE, STATIC, VISUAL, STORAGE, REMOTE, FULL and the exact-head cognitive seal.

Guarded r16 publication run `34527023577` succeeded from staging head `a12e63f78ab583383d1e7fa271f5afb0b49b9fbe`. Before committing release bytes it required `703018a341d83c34fe15a43538c5e80ad145fd39` as an ancestor, advanced the whole-shell runtime from r15 to r16 with r15 as previous known-good, preserved both Local Reconciliation service-worker assets, created `RELEASE_V1.9.1_R16.md`, and passed the r16 Local Reconciliation suite, production runtime contract, static release contract, release-shell coherence contract, offline hotfix contract, all operations tests and `git diff --check`. It then self-deleted the publication workflow and created Actions-authored publication commit `7103fddc87d6ec591b1cce30a66243545b2936e9`.

The bot-triggered PR POS20 #396 on `7103fddc87d6ec591b1cce30a66243545b2936e9` concluded `action_required` without becoming acceptance evidence. This connector-authored commit establishes the final r16 publication provenance boundary and intentionally changes documentation only. A fresh complete normal PR POS20 must pass on this exact connector-authored head before PR #246 can merge. Earlier candidate or staging lanes must not be combined with it.

Product-integration credit remains unawarded until final publication-head POS20, expected-head merge, Pages deployment, exact-main POS20 and two-pass Release Integration Burn-In all succeed. MDP stays 86.50 until separate post-integration accounting. SSJR remains 0 until genuine production-two-account evidence is accepted.
