#!/usr/bin/env node

process.stderr.write(
  'SSJR_SAFE_EVIDENCE_ASSEMBLY_RETIRED Standalone v1 JSON assembly is retired because browser-exported files do not authenticate production provenance. Use the current actor-attributed candidate capture only as acceptance assistance; genuine production acceptance requires separately established trusted/owner-observed production evidence.\n'
);
process.exit(1);
