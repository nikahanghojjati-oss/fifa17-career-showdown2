# PR260 Final Seal — 2026-09-14

This file is a continuity and exact-head seal for PR #260. It does not substitute stale CI evidence from an earlier SHA.

## Product contract

- One player-facing Showdown experience.
- Daniel is permanently Player One.
- Nik is permanently Player Two.
- New Showdowns preserve the selected 1 / 3 / 5 / 10 season count.
- Reversed historical role mappings fail closed rather than relabeling role-bound history.
- Connect Players routes to the persistent Daniel/Nik pair controls.
- Fresh-browser recovery routes through the established Candidate C / Legacy recovery loader.

## Provider authority hardening completed before this seal

- Pair-code creation writes the creator account `pairLinks/current` witness inside the same Firestore transaction as rivalry/invite creation.
- Pair-code redemption writes the joining account `pairLinks/current` witness inside the same Firestore transaction as rivalry activation/invite redemption.
- Generated Firestore Rules require the exact creator and redeemer witness seams.
- The pair-link document Rules retain registered-device, canonical manager/role, rivalry-membership, CAS/replacement and private account authority.
- Stale creator and stale redeemer tabs cannot create a second active pairing authority.
- One-use capability mutations roll back when their durable account witness cannot commit.

## Installed application / recovery

- `js/persistentNikDanielPair.js` is retained behind the lazy startup boundary but is included in the service-worker shell cache so installed browsers can load the canonical pairing controls after a controlled reload.
- Startup release budget remains protected at the last targeted proof: 162711 raw / 37496 gzip, below the existing 165000 / 37500 ceilings.

## Provider proof immediately preceding this seal

The generated production Rules provider emulator passed all persistent-pair cases after the final witness optimization, including:

- canonical Daniel=Player One and Nik=Player Two enforcement;
- private current-pair access and no list/delete;
- registered-device pair writes;
- active-career replacement denial and terminal-career replacement safety;
- witness-less creator rejection;
- valid atomic creator success;
- stale creator active-pair rejection with capability rollback;
- witness-less redemption rejection;
- valid atomic redemption success;
- stale redeemer double-active rejection and invite rollback.

The targeted product/release contracts in that same hardening transaction also passed before publication.

## Deployment boundary

- Firebase remains Spark-only; billing remains OFF by product policy.
- The permanent Rules deployment workflow remains main-only and Rules-only.
- It now asserts both `cmsPersistentPairCreationWitnessValid(rivalryId)` and `cmsPersistentPairRedemptionWitnessValid(rivalryId, inviteBefore.data.slotId)` in the generated source before publication.
- No Cloud Functions, Cloud Run, Blaze plan, payment method, public discovery, community or ranking surface is introduced.

## Final gate

No merge credit is granted by this document. The exact SHA containing this seal must independently pass the complete current POS20 matrix and a fresh Codex review. If either produces a substantive finding, fix it on a new SHA and restart the exact-head seal. Merge must use the verified expected head SHA only. After merge, verify main, deployment, generated/published Rules and the zero-billing boundary before beginning the physical Daniel/Nik two-device acceptance.
