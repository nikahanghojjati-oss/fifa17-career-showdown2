# PR260 Final Seal — 2026-09-14 — R9

This is the exact-head continuity seal for PR #260 after provider hardening, explicit desktop + phone-width user-facing DOM/routing acceptance, permanent CI ownership of the real persistent-pair routing audit, paired-browser reload authority hardening, and canonical active-Save hydration before career entry. It grants no merge credit by itself and no evidence from an earlier SHA may be combined with this candidate.

## Canonical product

- One player-facing Showdown experience.
- Daniel is permanently Player One; Nik is permanently Player Two.
- Historical saves are accepted only when the `managers` map exists and is exactly Daniel/Player One + Nik/Player Two. Reversed names, arbitrary names, an absent map, or a null map fail closed before role-bound clubs, scores, rounds, transfers or profile IDs can be relabelled.
- A new Showdown preserves the explicit 1 / 3 / 5 / 10 season selection.
- Home CREATE CODE / JOIN cannot silently auto-create a hidden one-season default: without a prepared shell, pairing routes to NEW SHOWDOWN and requires the player to choose the season length and press START A SHOWDOWN first.
- Expired pending invitations retain a visible NEW CODE path, while live pending and active pair authority remain provider-protected.
- Stale registered browsers converge to durable provider truth after both pending-pair and active-pair conflicts via `pairInitialize({force:true})`.
- CONNECT PLAYERS routes to the real persistent Daniel/Nik pair controls on the visible Home shell.
- Fresh-browser recovery routes through the established Candidate C / Legacy recovery loader.
- The legitimate pre-pair shell (`setupPending === true` with no attached rivalry yet) is onboarding, not a Journey Reconnect failure.

## Provider authority

- Pair creation atomically writes the creator account `pairLinks/current` witness with rivalry/invite creation.
- Pair redemption atomically writes the joining account `pairLinks/current` witness with rivalry activation/invite redemption.
- Generated Firestore Rules require both witness seams.
- Pair-link Rules retain registered-device, canonical manager/role, rivalry-membership, CAS/replacement and private account authority.
- One-use mutations roll back if their durable account witness cannot commit.
- Stale creator and redeemer tabs cannot create a second active pair.

## Installed app / recovery / release budget

- `js/persistentNikDanielPair.js` remains lazy from initial boot but is included in the service-worker shell cache for installed browsers.
- OPEN RECOVERY routes through `openOptionalModule("legacy")` so Candidate C and restore UI are loaded before use.
- An ACTIVE remembered pair lazily invokes the established `save-library-cutover` authority gate before deciding whether the provider-linked Save/Profile exists locally. This keeps Save Library Foundation/Persistence/Runtime out of eager startup while preventing a normal paired reload from being misclassified as missing recovery data.
- Continue Career now always calls the canonical Save Library `switchActiveSave(saveId)` authority for the exact provider-linked Save/Profile after verification, even when that Save is already `activeSaveId`. The runtime therefore hydrates the lexical `currentShowdown`, primes stable season identities, and then opens the shared career. The storage selection is not rewritten when already active.
- If the local authority itself cannot be established, pairing fails closed as unavailable rather than falsely claiming that the verified local career copy is absent.
- The protected startup budget remains unchanged at 165000 raw / 37500 gzip. The paired-reload hardening proof measured 162647 raw / 37468 gzip before R9; R9 changes only the lazy persistent-pair runtime plus permanent tests, and the static-release guard passed again before publication. The guard was not loosened.

## User-facing route/DOM acceptance

The permanent browser audit `tests/browser/persistent-pair-user-facing-routing-audit.cjs` exercises the real `productionSharedJourneyEntry` route and real `persistentNikDanielPair` renderer. It has proved that:

- CONNECT PLAYERS is visible and actionable before pairing;
- CONNECT PLAYERS returns to the visible Home shell and mounts the real persistent-pair panel there;
- CREATE CODE, the connection-code input and JOIN are visible;
- CREATE CODE reaches a complete waiting state with COPY CODE, CHECK STATUS and NEW CODE;
- JOIN reaches CAREER READY and exposes CONTINUE CAREER;
- CONTINUE CAREER invokes the intended career-entry route;
- a remembered ACTIVE pair on a cold normal browser state, where Save Library runtime is not yet loaded but the exact provider-linked local Save/Profile exists, lazily activates storage authority and returns to CAREER READY with CONTINUE CAREER rather than false recovery;
- that same cold-reload regression clears the live in-memory Showdown before pressing Continue and proves the already-active canonical Save is explicitly hydrated back into `currentShowdown` before shared entry opens, preserving its exact Save ID, `shared` journey marker and 5-season duration;
- a genuinely fresh browser missing the verified local recovery copy still renders CAREER RECOVERY NEEDED and exposes OPEN RECOVERY;
- OPEN RECOVERY loads and focuses the verified restore input;
- the player connection surface does not expose retired architecture concepts;
- the route emits no unexpected page or console errors.

The same real persistent-pair DOM audit has passed at a 390×844 phone viewport. The broader stability suite separately passed Chromebook 1366×768 and mobile 390×844 journeys, and the paired-first audit preserved a real 5-season selection while preventing league/club selection before pair authority.

## Permanent regression ownership

The routing audit is permanently owned by normal CI:

- `npm run test:persistent-pair-routing` is the dedicated permanent route audit;
- the real persistent-pair routing audit runs in the standard `test:browser` suite;
- the same audit runs in the standard `test:ssjr:browser` suite;
- the cold ACTIVE-pair regression now proves both lazy authority activation and explicit in-memory hydration of an already-active provider-linked Save before Continue Career;
- the genuinely missing-local-copy branch remains separately proven to fail closed into OPEN RECOVERY.

The temporary workflows used only to publish/prove routing, paired-reload authority and R9 active-Save hydration were removed before this R9 seal.

## Deployment boundary

- Firebase remains Spark-only and billing remains OFF by product policy.
- The permanent Rules deployment workflow remains main-only and Rules-only.
- It asserts both `cmsPersistentPairCreationWitnessValid(rivalryId)` and `cmsPersistentPairRedemptionWitnessValid(rivalryId, inviteBefore.data.slotId)` before publication.
- No Cloud Functions, Cloud Run, Blaze plan, payment method, public discovery, community or ranking surface is introduced.

## Exact-head merge gate

The SHA containing this R9 seal must independently pass the complete current POS20 matrix and a fresh Codex review. If either produces a substantive finding, fix it on a new SHA and reseal again. Merge must use the verified expected head SHA only. After merge, verify `main`, GitHub Pages, the standard deployed-site routing/stability smoke, generated/published Firestore Rules, and the zero-billing boundary before beginning the physical Daniel/Nik two-device acceptance.