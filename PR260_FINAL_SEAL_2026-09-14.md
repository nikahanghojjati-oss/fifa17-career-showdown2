# PR260 Final Seal — 2026-09-14 — R5

This is the exact-head continuity seal for PR #260 after the final onboarding-convergence corrections and a dedicated real user-facing DOM/routing acceptance audit. It grants no merge credit by itself and no evidence from an earlier SHA may be combined with this candidate.

## Canonical product

- One player-facing Showdown experience.
- Daniel is permanently Player One; Nik is permanently Player Two.
- Historical saves are accepted only when the `managers` map exists and is exactly Daniel/Player One + Nik/Player Two. Reversed names, arbitrary names, an absent map, or a null map fail closed before role-bound clubs, scores, rounds, transfers or profile IDs can be relabelled.
- A new Showdown preserves the explicit 1 / 3 / 5 / 10 season selection.
- Home CREATE CODE / JOIN cannot silently auto-create the hidden selector's one-season default: without a prepared shell, pairing routes to NEW SHOWDOWN and requires the player to choose the season length and press START A SHOWDOWN first.
- A creator whose pending one-use code has expired retains a visible NEW CODE action. Provider authority rejects replacement while the old invite is still live and permits replacement only after it is expired/non-open.
- Stale registered browsers converge to durable provider truth after both pending-pair and active-pair conflicts via `pairInitialize({force:true})`; neither state requires a manual reload to expose the authoritative controls.
- Connect Players routes to the real persistent Daniel/Nik pair controls on the visible Home shell.
- Fresh-browser recovery routes through the established Candidate C / Legacy recovery loader.
- The legitimate pre-pair shell (`setupPending === true` with no attached rivalry yet) is treated as onboarding, not as a Journey Reconnect failure, so Connect Players does not emit a misleading recovery error.

## Provider authority

- Pair creation atomically writes the creator account `pairLinks/current` witness with rivalry/invite creation.
- Pair redemption atomically writes the joining account `pairLinks/current` witness with rivalry activation/invite redemption.
- Generated Firestore Rules require both witness seams.
- Pair-link Rules retain registered-device, canonical manager/role, rivalry-membership, CAS/replacement and private account authority.
- Stale creator and stale redeemer tabs cannot create a second active pair.
- One-use capability mutations roll back when their durable account witness cannot commit.
- Expired pending replacement remains provider-guarded; active and still-open pending authority remain protected.
- Client stale-state recovery converges back to provider authority after both pending and active conflicts.

## Installed app / recovery / release budget

- `js/persistentNikDanielPair.js` remains lazy from initial boot but is included in the service-worker shell cache for installed browsers.
- OPEN RECOVERY routes through `openOptionalModule("legacy")` so Candidate C and restore UI are loaded before use.
- The protected startup budget remains unchanged at 165000 raw / 37500 gzip.
- The final dedicated routing proof measured 162647 raw / 37468 compressed initial bytes; the guard was not loosened.

## Final user-facing route/DOM acceptance

A permanent browser audit now exercises the real `productionSharedJourneyEntry` route and the real `persistentNikDanielPair` renderer rather than a placeholder panel. It proved that:

- CONNECT PLAYERS is visible and actionable before pairing;
- CONNECT PLAYERS returns to the visible Home shell and mounts `#persistentNikDanielPairPanel` there;
- CREATE CODE, the connection-code input and JOIN are visible on the real unpaired panel;
- CREATE CODE transitions to a complete waiting state with the actual code plus COPY CODE, CHECK STATUS and NEW CODE controls;
- JOIN transitions to CAREER READY and exposes CONTINUE CAREER;
- CONTINUE CAREER invokes the intended career-entry route;
- an active pair on a fresh browser without the verified local recovery copy renders CAREER RECOVERY NEEDED and exposes OPEN RECOVERY;
- OPEN RECOVERY loads/focuses the verified restore input;
- the player connection surface contains no Save Library, Private Remote Joining or Shared Journey architecture language;
- the routing audit emitted no page errors or unexpected console errors.

The existing paired-first browser audit independently passed on the same proof tree and confirmed real Start a Showdown ownership, Daniel/P1 + Nik/P2 canonical mapping, preservation of a 5-season selection, automatic ACTIVE peer return, durable reload lock, and prevention of league/club selection before connection authority.

The full stability audit also passed on Chromebook (1366×768) and mobile (390×844), including clean Home, Create Showdown, league selection/recovery, club confirmation, Showdown Home, reload/history recovery, optional destinations, accessibility scans and responsive containment. It recorded 46 checkpoints and 14 accessibility scans with clean local runtime/assets.

## R5 onboarding convergence

- Journey Reconnect now recognizes the exact prepared pre-pair state and publishes no recovery state until rivalry attachment exists.
- CREATE CODE and JOIN both refresh durable pair authority on `PERSISTENT_PAIR_PENDING_CONFLICT` or `PERSISTENT_PAIR_ACTIVE_CONFLICT`.
- Focused persistent-pair, Journey Reconnect, production runtime, static release, presentation, paired-first and full browser stability proofs passed before this seal.
- The temporary R5 proof and user-routing workflow files were removed before this seal. The permanent routing audit remains as regression coverage.

## Deployment boundary

- Firebase remains Spark-only and billing remains OFF by product policy.
- The permanent Rules deployment workflow remains main-only and Rules-only.
- It asserts both `cmsPersistentPairCreationWitnessValid(rivalryId)` and `cmsPersistentPairRedemptionWitnessValid(rivalryId, inviteBefore.data.slotId)` before publication.
- No Cloud Functions, Cloud Run, Blaze plan, payment method, public discovery, community or ranking surface is introduced.

## Exact-head merge gate

The SHA containing this R5 seal must independently pass the complete current POS20 matrix and a fresh Codex review. If either produces a substantive finding, fix it on a new SHA and reseal again. Merge must use the verified expected head SHA only. After merge, verify `main`, GitHub Pages, generated/published Firestore Rules and the zero-billing boundary before beginning the physical Daniel/Nik two-device acceptance.
