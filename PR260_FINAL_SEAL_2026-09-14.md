# PR260 Final Seal — 2026-09-14 — R3

This is the exact-head continuity seal for PR #260 after the final stale-browser pending-pair review finding. It grants no merge credit by itself and no evidence from an earlier SHA may be combined with this candidate.

## Canonical product

- One player-facing Showdown experience.
- Daniel is permanently Player One; Nik is permanently Player Two.
- Any historical `managers` mapping other than exact Daniel/Player One + Nik/Player Two fails closed before role-bound clubs, scores, history or profile IDs can be relabelled.
- A new Showdown preserves the explicit 1 / 3 / 5 / 10 season selection.
- Home CREATE CODE / JOIN cannot silently auto-create the hidden selector's one-season default: without a prepared shell, the pairing controls route to NEW SHOWDOWN and require the player to choose the season length and press START A SHOWDOWN first.
- A creator whose pending one-use code has expired retains a visible NEW CODE action. The provider transaction remains the authority: a still-live code is rejected as a pending conflict, while an expired/non-open pending invitation can be replaced atomically.
- If another registered browser is stale and tries to create while a durable pending pair already exists, the provider conflict now triggers `pairInitialize({force:true})`; the stale browser re-reads the account's durable current pair instead of rendering an empty waiting state or requiring a page reload.
- Connect Players routes to the persistent Daniel/Nik pair controls.
- Fresh-browser recovery routes through the established Candidate C / Legacy recovery loader.

## Provider authority

- Pair creation atomically writes the creator account `pairLinks/current` witness with rivalry/invite creation.
- Pair redemption atomically writes the joining account `pairLinks/current` witness with rivalry activation/invite redemption.
- Generated Firestore Rules require both witness seams.
- Pair-link Rules retain registered-device, canonical manager/role, rivalry-membership, CAS/replacement and private account authority.
- Stale creator and stale redeemer tabs cannot create a second active pair.
- One-use capability mutations roll back when their durable account witness cannot commit.
- Expired pending replacement remains provider-guarded; active and still-open pending authority remain protected.
- Client stale-state recovery now converges back to provider authority immediately after a pending-code conflict.

## Installed app / recovery / release budget

- `js/persistentNikDanielPair.js` remains lazy from initial boot but is included in the service-worker shell cache for installed browsers.
- OPEN RECOVERY routes through `openOptionalModule("legacy")` so Candidate C and restore UI are loaded before use.
- The protected startup budget remains unchanged at 165000 raw / 37500 gzip; the last targeted proof remained below those ceilings.

## R3 proof immediately preceding this seal

The bounded stale-pending correction passed:

- syntax check for the changed persistent-pair runtime;
- persistent Nik/Daniel product contracts, including an explicit regression assertion that `PERSISTENT_PAIR_PENDING_CONFLICT` reinitializes from durable provider authority;
- static release and final presentation budgets;
- paired-first browser audit served from the local HTTP test server.

Provider and Firestore Rules semantics were not changed by R3; the immediately preceding R2 generated-production Rules emulator had already passed creator/redeemer witness positives and negatives, active/stale conflicts, and pending replacement safety. The temporary R3 workflow was removed before this seal.

## Deployment boundary

- Firebase remains Spark-only and billing remains OFF by product policy.
- The permanent Rules deployment workflow remains main-only and Rules-only.
- It asserts both `cmsPersistentPairCreationWitnessValid(rivalryId)` and `cmsPersistentPairRedemptionWitnessValid(rivalryId, inviteBefore.data.slotId)` before publication.
- No Cloud Functions, Cloud Run, Blaze plan, payment method, public discovery, community or ranking surface is introduced.

## Exact-head merge gate

The SHA containing this R3 seal must independently pass the complete current POS20 matrix and a fresh Codex review. If either produces a substantive finding, fix it on a new SHA and reseal again. Merge must use the verified expected head SHA only. After merge, verify `main`, GitHub Pages, generated/published Firestore Rules and the zero-billing boundary before beginning the physical Daniel/Nik two-device acceptance.
