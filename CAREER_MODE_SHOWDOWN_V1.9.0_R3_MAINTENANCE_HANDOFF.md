# Career Mode Showdown v1.9.0-r3 Maintenance Release Record

Status: RELEASE CANDIDATE / NOT PRODUCTION-PROVEN
Application version: `v1.9.0`
Runtime revision: `1.9.0-r3`
Previous known-good whole shell: `1.9.0-r2`
Remote Joining readiness: `87/100` under fixed model `RJR-1`

## SLE scope clarification

This legacy-named maintenance file is a release-boundary record required by release-authority coherence. It is not the authoritative project/session handoff and does not replace `SESSION_BOOTSTRAP.json`, `00_CURRENT_HANDOFF.md`, the mirrored versioned `START_NEXT_SESSION` artifacts, `NEXT_CHAT_HANDOFF_PROMPT.md`, `NEXT_TASK.md`, `PROJECT_STATE.md`, or the repository-first Smart Lean Efficient (SLE) packaging rules. If the active work environment reaches handoff proximity 100, those authoritative successor artifacts must still be regenerated and mirrored normally.

## Purpose

`1.9.0-r3` is the bounded pairing one-paste hardening candidate that removes avoidable owner friction before the genuine production Private Remote Joining lifecycle is repeated. Player One receives a direct `COPY PAIRING CODE` action, Player Two pastes exactly once, and the exact private pairing capability must remain byte-for-byte identical across Player One generated pairing state, Player One Connected Rivalry, Player Two retained join input, and Player Two Connected Rivalry.

A late registered-device completion is now non-destructive while pairing is creating, joining, open, or paired. Registration metadata may finish initializing, but an older registration promise cannot overwrite the newer live pairing state.

The normal successful flow auto-links the exact paired rivalry into both manager Connected Rivalry contexts. Manual Verify/Reattach remains recovery-only and must not be required to hide a normal-flow auto-link failure during production acceptance.

## Locked safety boundary

Firebase remains Spark and billing remains permanently forbidden. Do not attach Cloud Billing, enable Blaze, add a payment method, activate Cloud Run or Cloud Functions, purchase credits, or select any billing-required provider path. Firestore persistence remains memory-only. App Check enforcement remains OFF.

Canonical browser storage remains exactly `careerModeShowdown.saveLibrary`, `careerModeShowdown.legacyShowdowns`, and `careerModeShowdown.preferences`. Private pairing and Remote Joining capabilities remain exact, non-enumerable private capabilities; no public discovery, listing, lobby, matchmaking, community or rankings authority is added. Remote Joining may not mutate canonical local saves.

Candidate C remains the sole destructive remote-to-local gameplay Apply authority. It preserves immutable confirmed intent, verified backup before mutation, strict exact raw snapshot guards, transaction-owned mutation, stale-state and anti-clobber rejection, ownership-scoped reverse rollback, and exact recovery verification. `1.9.0-r3` does not bypass or broaden Candidate C.

The Installable Offline App and local Career Mode remain independent of Firebase. Provider or Spark quota failure must fail closed while local play remains available. The protected historical rivalry must not be edited, forced, deleted, or used for destructive testing. The shipped `v1.3.0 Recovery & Device Resilience Hardening` baseline remains intact.

## Runtime and browser evidence boundary

The r3 candidate advances the whole-shell asset identity from `1.9.0-r2` to `1.9.0-r3`; `1.9.0-r2` remains the immediate production-proven recovery shell until r3 publication is independently verified.

Deterministic proof requires repeated two-account equality across all four pairing-code surfaces and zero second-paste requirement. Browser proof uses the production Save Library settings geometry and styles and isolates fresh Player One and Player Two Chromium scenarios so stale browser-process state cannot hide lifecycle defects. Stage 5E Remote Joining remains lazy and provider authority is resolved only on explicit Host, Join, Refresh/Read, Revoke or Close operations.

The production Firestore Rules authority remains the exact already-provider-live Stage 5D source. This maintenance candidate does not mutate Security Rules, provider IAM, authentication policy, App Check enforcement, Firebase persistence, billing, session authorization or gameplay authority.

## Publication and recovery rule

This file describes a release candidate, not a production-proven runtime. `1.9.0-r2` remains the previous production-proven whole-shell recovery target until exact-head CI and review, expected-head merge, Pages deployment, exact deployed-byte/runtime verification and post-merge gates prove `v1.9.0 / 1.9.0-r3`.

Never construct a mixed-version rollback. If a concrete r3 regression requires recovery before production promotion is proven, restore the previous whole `1.9.0-r2` shell rather than selectively mixing runtime generations.

RJR remains exactly `87/100` through this hardening, source work, automation, CI, review, merge and deployment mechanics. A score change requires genuine new fixed-domain production capability evidence. After r3 is independently production-live, resume only the interrupted owner two-account acceptance: one Player One Copy, one Player Two paste, automatic exact Connected Rivalry on both managers, then Private Remote Joining Host → Join → Refresh/Read → Close. The full `session_...` capability must never be pasted into chat or retained in evidence.