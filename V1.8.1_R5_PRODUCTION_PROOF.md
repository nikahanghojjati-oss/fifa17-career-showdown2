# v1.8.1-r5 Production Proof

Status: DEPLOYED / PRODUCTION-PROVEN
Recorded: 2026-08-27 ET
Application: `v1.8.1`
Runtime: `1.8.1-r5`
Previous known-good whole-shell recovery runtime: `1.8.1-r4`
Remote Joining readiness: `84/100` under fixed RJR-1

## Exact source and publication boundary

- Pull request: #163 `Harden sustained Connected Rivalry mutation frequency`.
- Capability proof exact head: `e26d37dc598c956e8e7a82e1f9c0b3d919326914`.
- Final exact PR head: `5131cadcd3250b5a515f3f4b6f292f8ee51aab67`.
- Exact-head PR validation: all 14 permanent workflow families succeeded on that unchanged head.
- Submitted reviews before merge: 0.
- Inline review threads before merge: 0.
- Mergeability before merge: true.
- Publication: expected-head squash merge under standing owner authorization.
- Runtime merge / product-main SHA: `c2fbcdc3d57ac6e64dee8ed5681193673ca0cbbf`.
- Live `main` was independently re-read and matched that merge SHA.
- GitHub Pages main-push run `33130452373` — SUCCESS.
- Release Integration Burn-In main-push run `33130452330` — SUCCESS.
- Stability Lane main-push run `33130452363`.
  - `stability-contracts` job `98718484327` — SUCCESS.
  - `chromium-stability` job `98718615594` — SUCCESS.
  - `deployed-site-smoke` job `98719038793` — SUCCESS after the deployed proof chain completed.

## Capability proven

Permanent `tests/contracts/stage4-mutation-rate-limit-contracts.cjs` and `tests/firebase/stage4-mutation-rate-limit-emulator.cjs` execute the real Connected Rivalry mutation runtime against candidate production Security Rules and prove:

- low-revision establishment remains compatible with existing create/CAS/replay behavior;
- revision 2 → 3 anchors authoritative shared-state `updatedAt` to Firestore server time;
- from revision 3 onward a distinct accepted revision requires at least two seconds since the previous authoritative server-time anchor;
- authoritative shared-state `updatedAt` is supplied by Firestore `serverTimestamp()`, so caller-clock skew cannot bypass the boundary;
- a rate-limited distinct mutation allocates no authoritative revision and no idempotency receipt and leaves canonical local Save Library state unchanged;
- exact accepted-result replay remains idempotent during the closed window;
- the same previously denied logical mutation may advance exactly once after cooldown under immutable-base CAS.

Fixed RJR-1 moves exactly once, `83 → 84`, for this distinct sustained mutation-frequency abuse-resistance capability. Source packaging, CI volume, merge, deployment, provider publication, documentation and SLE receive zero duplicate credit.

## Post-merge deployed production evidence

The main Stability Lane verified deployed GitHub Pages after the Pages publication.

The deployed-site verifier reported `89 runtime files match 1.8.1-r5 byte for byte` before the browser preservation audits.

The production App Check audit then passed in enforcement-OFF degraded state. Its success string retains the historical label `deployed r3`; because the same exact job first proved all 89 deployed runtime files are byte-for-byte `1.8.1-r5`, that phrase is stale audit-report wording rather than deployed runtime identity.

The deployed-site smoke proved:
1. every runtime byte matched the expected merged r5 runtime artifact;
2. runtime-error provenance was clean;
3. the production App Check token path succeeded;
4. Home visual audit succeeded;
5. visible Save Library audit succeeded;
6. manager identity linkage audit succeeded;
7. identity-safe Career Analytics audit succeeded;
8. crop-safe licensed football-photo audit succeeded;
9. Candidate A backup export audit succeeded;
10. Candidate B import analysis succeeded;
11. Candidate C atomic restore and critical recovery audit succeeded;
12. install/offline boundary audit succeeded;
13. the complete deployed journey succeeded.

The canonical Chromium Stability job also passed the runtime, Save Library, offline lifecycle and complete integration journey from the merged main source.

## Production identity and rollback

`v1.8.1 / 1.8.1-r5` is therefore production-proven. `1.8.1-r4` is the immediate previous known-good whole-shell recovery runtime.

Recovery remains a whole-runtime-generation operation. Do not partially roll back the rate-limit/server-time changes. Preserve canonical storage, strict exact raw snapshot authority, transaction-owned Candidate C mutation, ownership-scoped rollback, Installable Offline App behavior, Google popup/session-only authentication, memory-only Firestore and App Check enforcement OFF.

## Provider publication boundary

Repository/emulator evidence proves candidate Security Rules behavior but does not prove provider-live Rules publication. Production-provider publication of the strengthened `firestore.spark.rules` remains separately UNVERIFIED. Website byte deployment does not change that fact.

## Uncredited remaining Remote Joining capability

This production proof does not credit or close:

- authenticated third-account / revoked registered-device production negatives, which still require legitimate authenticated production identity/device state;
- two-physical-network behavior;
- Remote Joining-specific real-device token-lifecycle acceptance;
- production abuse acceptance;
- production rollback proof;
- actual Remote Joining host/join/session orchestration;
- final stable Remote Joining release acceptance.

Stage 5 remains locked until the remaining explicit pre-Stage-5 gates are genuinely closed. Do not repeat already-consumed owner pairing/device recovery, unavailable-code proof, destructive Candidate C reconciliation, exact replay, deterministic adverse-provider, deterministic token-lifecycle, structural abuse or sustained mutation-frequency proof merely for confidence.
