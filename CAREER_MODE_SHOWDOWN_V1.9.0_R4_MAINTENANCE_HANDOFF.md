# Career Mode Showdown v1.9.0-r4 Maintenance Release Record

Status: RELEASE CANDIDATE / NOT PRODUCTION-PROVEN
Application version: `v1.9.0`
Runtime revision: `1.9.0-r4`
Previous known-good whole shell: `1.9.0-r3`
Remote Joining readiness: `88/100` under fixed model `RJR-1`

## SLE scope clarification

This legacy-named maintenance file is a release-boundary record required by release-authority coherence. It is not the authoritative project/session handoff and does not replace `SESSION_BOOTSTRAP.json`, `00_CURRENT_HANDOFF.md`, the mirrored versioned `START_NEXT_SESSION` artifacts, `NEXT_CHAT_HANDOFF_PROMPT.md`, `NEXT_TASK.md`, `PROJECT_STATE.md`, or the repository-first Smart Lean Efficient SLE package. At handoff proximity 100 the authoritative successor artifacts must still be regenerated and mirrored normally.

## Purpose

`1.9.0-r4` is the bounded stale Connected Rivalry pointer-precedence repair. Durable pointer A remains fallback authority while the current Private Pairing candidate B is still evaluated. Pending, expired, wrong-account, wrong-device, wrong-manager, profile/save-mismatched or otherwise unauthorized B cannot displace A. A provider-active exact B may replace A only through the existing provider-authorized attachment verification and must then persist automatically.

The normal successful flow remains one Player Two pairing-code paste with zero manual Connected Rivalry Verify/Reattach actions. Pending B may be exposed only as non-authoritative prefill metadata while A remains authoritative. Once B becomes the verified durable pointer, the prefill disappears. Canonical local storage must remain unchanged.

## Locked safety boundary

Firebase remains Spark and billing remains permanently forbidden. Do not attach Cloud Billing, enable Blaze, add a payment method, activate Cloud Run or Cloud Functions, purchase credits, or select a billing-required provider path. Firestore persistence remains memory-only and App Check enforcement remains OFF.

Private pairing and Private Remote Joining remain exact non-enumerable capabilities with no public discovery, listing, lobby, matchmaking, community or rankings authority. Remote Joining may not mutate canonical local saves. Candidate C remains the sole destructive remote-to-local gameplay Apply authority with immutable confirmed intent, verified backup before mutation, strict exact raw snapshot guards, transaction-owned mutation, stale-state and anti-clobber rejection, ownership-scoped reverse rollback and exact recovery verification.

The Installable Offline App and local Career Mode remain independent of Firebase. Provider or Spark quota failure must fail closed while local play remains available. The protected historical rivalry must not be edited, forced, deleted or used for destructive testing. The shipped `v1.3.0 Recovery & Device Resilience Hardening` baseline remains intact.

## Evidence and RJR boundary

The previously supplied production `v1.9.0 / 1.9.0-r3` owner evidence independently moved fixed RJR-1 from `87/100` to `88/100` for exactly one bounded provider-live Remote Joining Host -> Join -> ACTIVE revision 1 cross-role observation -> Close -> CLOSED revision 2 cross-role propagation capability. The separate zero-manual-reattach behavior failed in r3 and earned zero credit.

This r4 source repair, automation, CI, review, merge, deployment and documentation earn zero additional RJR credit. A future RJR change requires genuinely new fixed-domain production capability evidence.

## Publication and recovery rule

This file describes a release candidate, not a production-proven runtime. `1.9.0-r3` remains the previous production-proven whole-shell recovery target until exact-head CI including Chromium, documented final-head review with zero unresolved valid threads, expected-head merge, post-merge workflows, Pages deployment and exact deployed-byte/runtime verification prove `v1.9.0 / 1.9.0-r4`.

Never construct a mixed-version rollback. If a concrete r4 regression requires recovery before promotion is proven, restore the previous whole `1.9.0-r3` shell. After r4 is independently production-live, perform only the minimal owner acceptance needed to prove a fresh one-paste flow automatically converges both managers on the exact current Connected Rivalry with zero manual Verify/Reattach. Never paste or retain a full `session_...` private capability in chat or durable evidence.
