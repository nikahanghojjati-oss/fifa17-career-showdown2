# CURRENT OVERRIDE — v1.8.1-r5 SUSTAINED MUTATION-FREQUENCY HARDENING EVIDENCE-PROVEN — PR #163 — RJR84 — 2026-08-27 ET

Status: production `v1.8.1 / 1.8.1-r4` remains DEPLOYED / PRODUCTION-PROVEN. Candidate `v1.8.1 / 1.8.1-r5` is RELEASE CANDIDATE / CAPABILITY EVIDENCE-PROVEN / PUBLICATION PENDING / NOT PRODUCTION-PROVEN. Stage 4 reconciliation + exact replay + deterministic adverse-provider + token-lifecycle + structural abuse + sustained mutation-frequency safety are proven. Fixed RJR-1 is `84/100`. Stage 5 remains locked.

Application: `v1.8.1`
Production runtime: `1.8.1-r4`
Candidate runtime: `1.8.1-r5`
Current verified live main: `567e2c308ce32cf2c4ef7432e65ffb3a99111ef5` (PR #162 merge)
Current pull request: `#163` on `agent/post-pr162-rjr-successor`
Sustained mutation-frequency proof head: `e26d37dc598c956e8e7a82e1f9c0b3d919326914`
Previous known-good whole-shell recovery runtime for r5: `1.8.1-r4`
Remote Joining readiness: `84/100` under fixed RJR-1

## Current production truth

PR #160 remains the production runtime authority for `v1.8.1 / 1.8.1-r4`; post-merge proof is `V1.8.1_R4_PRODUCTION_PROOF.md`. PR #162 is merged on live main at `567e2c308ce32cf2c4ef7432e65ffb3a99111ef5`; its structural abuse capability is closed and protected. Candidate Firestore Rules publication from the structural-abuse work remains separately provider-unverified.

The current-identity Nik/Gop Connected Rivalry reconciliation remains production-proven. Exact accepted-result idempotency replay remains evidence-proven. Deterministic adverse-provider failure safety remains evidence-proven. Permanent Stage 4 App Check token-lifecycle safety remains evidence-proven while App Check enforcement remains OFF.

## PR #163 sustained mutation-frequency evidence

PR #163 proof head `e26d37dc598c956e8e7a82e1f9c0b3d919326914` passed the permanent Validate Stage 3 Private Pairing workflow. Its permanent deterministic contract and Firestore emulator proof execute the real Connected Rivalry mutation runtime against candidate production Security Rules.

The bounded capability is:

- low-revision establishment behavior through revision 3 remains compatible with already-proven create/CAS/replay behavior;
- revision 2 → 3 establishes an authoritative Firestore server-time anchor;
- at revision 3 and later, a distinct accepted revision requires at least two seconds since the previous authoritative server-time anchor;
- authoritative shared-state `updatedAt` uses Firestore `serverTimestamp()`, so a skewed caller clock cannot bypass the boundary;
- a rate-limited distinct mutation allocates no authoritative revision and no idempotency receipt and does not mutate canonical local Save Library state;
- exact accepted-result replay remains idempotent during the closed window;
- the same previously denied logical mutation may advance exactly once after the window opens under the normal immutable-base CAS contract.

This is one distinct broader abuse-resistance capability. It earns exactly +1 in `real-device-hardening-release`, moving fixed RJR-1 from 83 → 84. No readiness credit is awarded for source edits, shell packaging, CI volume, PR publication, merge, deployment, documentation or provider publication.

## Candidate r5 runtime

`RELEASE_V1.8.1_R5.md` defines the candidate whole shell. `1.8.1-r4` is the immediate previous known-good rollback runtime. The browser Firestore SDK surface adds only `serverTimestamp()` to the existing bounded Spark account/device/pairing/Connected Rivalry write path. Firestore remains memory-only and no new Firebase service is initialized.

Production-provider publication of the strengthened candidate `firestore.spark.rules` remains separately unproven and must not be claimed from repository/emulator evidence or website deployment alone.

## RJR truth

Fixed RJR-1 is `84/100`.

Recent conservative accounting:

- r3 owner account recovery restored two invalidated credits: 76 → 78;
- remote-to-local reconciliation: 78 → 79;
- exact accepted-result replay: 79 → 80;
- deterministic adverse-provider failure safety: 80 → 81;
- deterministic App Check token-lifecycle safety: 81 → 82;
- deterministic structural abuse resistance: 82 → 83;
- deterministic sustained mutation-frequency abuse resistance: 83 → 84.

No subassertion, CI family, PR, merge, deployment, documentation or provider publication earns duplicate credit.

## Remaining explicitly uncredited capability

Authenticated third-account/revoked registered-device production negatives remain uncredited without legitimate authenticated production state. Two-physical-network behavior remains uncredited. Remote Joining specific real-device token-lifecycle acceptance remains uncredited. Production abuse acceptance remains uncredited. Production rollback proof remains uncredited. Actual Remote Joining sessions remain Stage-5-gated. Final stable Remote Joining release acceptance remains uncredited.

Stage 5 remains locked until explicit remaining pre-Stage-5 conditions genuinely close.

## Current bounded work

Environment `we-2026-08-27-post-pr162-rjr-successor-1544` is finishing PR #163 publication. Require one unchanged exact r5 publication head with all 14 permanent workflow families green, inspect/resolve legitimate review findings, confirm non-draft mergeability, expected-head squash merge under standing owner authorization, and verify post-merge deployed `v1.8.1 / 1.8.1-r5` byte equality plus normal Stability/App Check journey. Provider Firestore Rules publication remains a separate evidence boundary.

After publication verification, complete this environment's recursive SLE package, refresh bootstrap/context pointers, run the next-prompt contract, seal WEC at Handoff proximity 100%, and stop before starting another substantial RJR capability.

## Do not repeat

Do not repeat completed r3 recovery, fresh pairing/device proof, unavailable-code proof, stale-preview proof, destructive Candidate C reconciliation, exact replay, deterministic adverse-provider, deterministic token-lifecycle, structural abuse or sustained mutation-frequency emulator proof merely for confidence/CI volume. Do not force, edit or delete historical rivalry `pair_a07108...756fb`.

## Permanent locks

Firebase remains Spark / zero billing. App Check enforcement remains OFF. Firestore persistent cache remains disabled/memory-only. Google Auth remains popup-only `browserSessionPersistence` with no extra scopes. Trusted-runtime IAM remains reviewed but unactivated/unbroadened. Exactly two private managers remain required. Public discovery, community, matchmaking and global rankings remain prohibited.

Canonical local storage remains exactly `careerModeShowdown.saveLibrary`, `careerModeShowdown.legacyShowdowns`, and `careerModeShowdown.preferences`; `activeShowdown` remains non-canonical. Candidate A remains non-mutating export, Candidate B read-only analysis, and Candidate C remains the sole destructive remote-to-local Apply authority. Candidate C rollback remains transaction-owned and its destructive recovery boundary requires a strict exact raw snapshot; immutable intent, backup-before-Apply, stale/anti-clobber rejection, ownership-scoped rollback and exact recovery verification remain protected.

The Installable Offline App remains the local-first startup/recovery baseline; ordinary local use must not depend on Firebase availability. The `v1.3.0 Recovery & Device Resilience Hardening` baseline remains closed and protected; Local Profiles and Save Library remain the shipped local identity/save dependency chain.

## Work Environment Continuity / SLE

The active environment is `we-2026-08-27-post-pr162-rjr-successor-1544`; it initialized fresh from live main and did not inherit predecessor `HANDOFF_NOW`. Handoff proximity is environment-local transition readiness, not project completion and not RJR. At Handoff proximity 100%, publish the complete mirrored Smart Lean Efficient recursive handoff package and seal the WEC as the final branch mutation.

Standing owner merge/deploy authorization remains active after all required tests and publication gates pass. Later explicit owner instructions override it.
