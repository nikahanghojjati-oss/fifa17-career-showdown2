# CURRENT OVERRIDE — v1.8.1-r3 STAGE 4 RECONCILIATION PRODUCTION-PROVEN — EXACT ACCEPTED-RESULT REPLAY PROVEN — 2026-08-25 ET

Status: DEPLOYED / PRODUCTION-PROVEN STAGE 4 RECONCILIATION / EXACT ACCEPTED-RESULT IDEMPOTENCY REPLAY EVIDENCE-PROVEN / STAGE 5 STILL LOCKED.

Application: `v1.8.1`
Production runtime: `1.8.1-r3`
Current runtime merge: `beab9f31cb7f31bf4938f5b0df67394899ef12a0` (PR #151)
Previous known-good whole-shell recovery runtime: `1.8.1-r1`
Remote Joining readiness: `80/100` under fixed RJR-1
Current publication candidate: PR #154 on `agent/stage4-idempotency-replay`
Stage 5: still locked pending the remaining explicit pre-Stage-5 hardening

Historical immediate-predecessor override retained only as provenance: `CURRENT OVERRIDE — v1.8.1-r3 CONNECTED ACCOUNT RECOVERY HOTFIX`. Under that earlier regression checkpoint, RJR-1 was `76/100` while the two previously credited production account capabilities were unavailable. That score/state is superseded by ordinary-owner r3 restoration, production-proven Stage 4 reconciliation, and the current fixed RJR-1 `80/100`; it is not current runtime or capability truth.

## Current production truth

The r2 Connected Account/App Check throttle regression is repaired and owner-proven on ordinary iPhone Safari in r3. Normal Google sign-in and strict authenticated self-account bootstrap are restored, so the two temporarily withdrawn RJR credits are restored from 76 back to 78.

The fresh current-identity Nik/Gop Connected Rivalry is production-proven through remote revision 1. The separate owner reconciliation gate is also production-proven: read-only exact Preview, stale-intent rejection, verified canonical backup before explicit Candidate C Apply, stable Player Two identity, exact local convergence, and unchanged remote revision/hash after local Apply.

Canonical owner proof: `OWNER_PRODUCTION_STAGE4_REMOTE_TO_LOCAL_RECONCILIATION_PROOF_2026-08-25.md`.

The final read-only post-Apply Preview reports that remote revision 1 already matches the identity-safe local gameplay candidate. The pre- and post-Apply remote content hash is identical, proving the local Apply did not publish or mutate remote authority.

Exact accepted-result idempotency replay is now evidence-proven through the permanent Firestore emulator boundary using the real `js/sparkConnectedRivalry.js` mutation runtime and production `firestore.spark.rules`. This proof did not change production runtime bytes, Firestore Security Rules, Firebase provider configuration, billing, canonical local storage, Candidate C authority, App Check enforcement, Google Auth behavior, or trusted-runtime IAM.

The focused replay proof establishes the harder historical case: an authoritative request is accepted at revision 0, a different legitimate mutation advances the same rivalry to revision 1, and the exact original accepted key/fingerprint then returns the original accepted revision 0 and original accepted content hash while the current revision 1 authority remains exactly unchanged. The accepted receipt remains immutable with no duplicate receipt, canonical local Save Library state remains unchanged, same-key/different-fingerprint reuse is rejected, the other owner cannot use the accepting actor's receipt outside its authority, and a genuinely new stale mutation remains rejected by immutable-base CAS.

## RJR truth

Fixed RJR-1 is now `80/100`.

The accounting is deliberately conservative:

- r3 owner account recovery restores exactly the two r2-invalidated credits: 76 → 78;
- the production-proven remote-to-local reconciliation capability adds exactly +1: 78 → 79;
- the evidence-proven exact accepted-result idempotency replay capability adds exactly +1: 79 → 80;
- no duplicate points are awarded for pairing, revision publication, stale-base CAS rejection, two-physical-device coverage, backup architecture, Preview substeps, replay subassertions, CI volume, documentation, or screenshot count.

Remaining explicitly uncredited capability includes third-account/revoked-device production negatives, two-network/adverse-network/token-lifecycle hardening, actual Remote Joining sessions, and final stable connected release acceptance. Exact idempotency replay is no longer an uncredited blocker.

## Immediate engineering boundary

Do not reopen completed r3, pairing, unavailable-code, stale-preview, destructive reconciliation, or exact-replay proofs. After PR #154 fully passes its exact-head publication gates and is published under standing owner authorization, the next real work is the smallest remaining pre-Stage-5 production authorization negative for a third account and/or a revoked device.

Existing emulator coverage already proves both categories, so first determine whether existing deployed/runtime proof machinery can establish the production boundary non-destructively without asking the owner to recreate accounts, pairings, or canonical state. Request only the minimum owner evidence if a genuine production-only identity dependency cannot be proven automatically.

After that authorization boundary is closed, reassess the remaining two-network/adverse-network/token-lifecycle hardening from live evidence. Do not bundle unrelated hardening into a speculative rewrite. Private Remote Joining remains the highest long-term priority and should begin as soon as its explicit prerequisite gates are genuinely closed.

## Permanent locks

Firebase stays Spark / zero billing. App Check enforcement stays OFF. Firestore persistent cache remains disabled/memory-only. Google Auth remains popup-only with `browserSessionPersistence` and no extra scopes. Trusted-runtime IAM remains reviewed but not activated or broadened. Exactly two private managers remain required. No public discovery/community/matchmaking/global rankings are authorized.

The Installable Offline App remains the local-first recovery baseline. The completed v1.3.0 Recovery & Device Resilience Hardening baseline remains intact. Local Profiles and Save Library remain the shipped local identity and multi-save dependency chain. Canonical local storage remains exactly `careerModeShowdown.saveLibrary`, `careerModeShowdown.legacyShowdowns`, and `careerModeShowdown.preferences`; `activeShowdown` remains non-canonical. Candidate A remains non-mutating export, Candidate B read-only analysis, and Candidate C remains the sole destructive remote-to-local Apply authority with strict exact raw snapshot authority, transaction-owned mutation, stale/anti-clobber rejection, ownership-scoped rollback and exact recovery verification.

## Work Environment Continuity

The active environment is `we-2026-08-25-stage4-idempotency-replay`. It independently verified starting live main `b4d0a2bc82f0de16da29c8b9c3f602e3997506ab` before entering the replay lane. Current-session usage remains unknown because no approved current-session usage value is exposed; predecessor usage was not inherited. Every successor must verify live state, initialize a fresh environment-owned WEC with reset counters, make its own assessment, and never fabricate usage.

Standing owner merge/deploy authorization remains active after required tests, exact-head review, and publication gates pass. Later explicit owner instructions override it.
