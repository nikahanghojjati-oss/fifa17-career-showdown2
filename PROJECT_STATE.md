# CURRENT OVERRIDE — v1.8.1-r3 STAGE 4 RECONCILIATION PRODUCTION-PROVEN — 2026-08-25 ET

Status: DEPLOYED / PRODUCTION-PROVEN / STAGE 4 REMOTE-TO-LOCAL RECONCILIATION CLOSED.

Application: `v1.8.1`
Production runtime: `1.8.1-r3`
Current runtime merge: `beab9f31cb7f31bf4938f5b0df67394899ef12a0` (PR #151)
Previous known-good whole-shell recovery runtime: `1.8.1-r1`
Remote Joining readiness: `79/100` under fixed RJR-1
Stage 5: still locked pending the remaining explicit pre-Stage-5 hardening

## Current production truth

The r2 Connected Account/App Check throttle regression is repaired and owner-proven on ordinary iPhone Safari in r3. Normal Google sign-in and strict authenticated self-account bootstrap are restored, so the two temporarily withdrawn RJR credits are restored from 76 back to 78.

The fresh current-identity Nik/Gop Connected Rivalry is production-proven through remote revision 1. The separate owner reconciliation gate is also now production-proven: read-only exact Preview, stale-intent rejection, verified canonical backup before explicit Candidate C Apply, stable Player Two identity, exact local convergence, and unchanged remote revision/hash after local Apply.

Canonical owner proof: `OWNER_PRODUCTION_STAGE4_REMOTE_TO_LOCAL_RECONCILIATION_PROOF_2026-08-25.md`.

The final read-only post-Apply Preview reports that remote revision 1 already matches the identity-safe local gameplay candidate. The pre- and post-Apply remote content hash is identical, proving the local Apply did not publish or mutate remote authority.

## RJR truth

Fixed RJR-1 is now `79/100`.

The accounting is deliberately conservative:

- r3 owner account recovery restores exactly the two r2-invalidated credits: 76 → 78;
- the newly production-proven remote-to-local reconciliation capability adds exactly +1: 78 → 79;
- no duplicate points are awarded for pairing, revision publication, stale-base CAS rejection, two-physical-device coverage, backup architecture, Preview substeps, CI, documentation, or screenshot count.

Remaining explicitly uncredited capability includes exact idempotency replay, third-account/revoked-device production negatives, two-network/adverse-network/token-lifecycle hardening, actual Remote Joining sessions, and final stable connected release acceptance.

## Immediate engineering boundary

Do not reopen completed r3, pairing, unavailable-code, stale-preview, or destructive reconciliation owner proofs. The next real work is the smallest remaining pre-Stage-5 Stage 4 hardening capability, beginning with exact idempotency replay unless fresh live/source analysis proves a narrower dependency.

After that proof, reassess the remaining Stage 5 blockers rather than running a generic hardening program. Private Remote Joining remains the highest long-term priority and should begin as soon as its explicit prerequisite gates are genuinely closed.

## Permanent locks

Firebase stays Spark / zero billing. App Check enforcement stays OFF. Firestore persistent cache remains disabled/memory-only. Google Auth remains popup-only with `browserSessionPersistence` and no extra scopes. Trusted-runtime IAM remains reviewed but not activated or broadened. Exactly two private managers remain required. No public discovery/community/matchmaking/global rankings are authorized.

The Installable Offline App remains the local-first recovery baseline. Canonical local storage remains exactly `careerModeShowdown.saveLibrary`, `careerModeShowdown.legacyShowdowns`, and `careerModeShowdown.preferences`; `activeShowdown` remains non-canonical. Candidate A remains non-mutating export, Candidate B read-only analysis, and Candidate C the sole destructive remote-to-local Apply authority.

## Work Environment Continuity

The closing environment is `we-2026-08-25-stage4-owner-reconciliation`. Its transition decision is predecessor-only. Every successor must verify live state, initialize a fresh environment-owned WEC with reset counters, make its own assessment, and never fabricate usage.

Standing owner merge/deploy authorization remains active after required tests and publication gates pass. Later explicit owner instructions override it.
