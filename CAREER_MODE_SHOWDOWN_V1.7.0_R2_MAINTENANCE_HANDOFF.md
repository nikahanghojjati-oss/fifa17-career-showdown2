# Career Mode Showdown v1.7.0-r2 Maintenance Handoff

Current production runtime: `v1.7.0 / 1.7.0-r2`. Previous known-good whole-shell fallback: `1.6.0-r1`. This maintenance slice fixed public runtime identity coherence only; it did not reopen or expand Stage 4 capability. Subsequent owner-controlled production proof is recorded in `V1.7.0_R2_PRODUCTION_PROOF.md` and `PRODUCTION_STAGE4_CONNECTED_RIVALRY_PROOF_2026-08-24.md`.

The owner observed production initially render `v1.7.0 · Connected Rivalry` and later change to `v1.7.0 · Stable`. Investigation found no intended final-source mutation to Stable, but did prove that changed Stage 4 runtime bytes had reused the same public `1.7.0-r1` asset identity. The fresh r2 namespace removes that ambiguity. The static Home tile now starts as `LOCAL / SAVE LIBRARY`, matching the existing intentional bootstrap identity instead of visibly relabeling itself.

Preserve the Installable Offline App and `1.6.0-r1` as the known-good recovery shell. Preserve transaction-owned rollback semantics, strict exact raw snapshot authority, ownership-scoped rollback, anti-clobber behavior and exact recovery verification. Canonical localStorage remains exactly `careerModeShowdown.saveLibrary`, `careerModeShowdown.legacyShowdowns`, and `careerModeShowdown.preferences`.

Do not republish Firebase Rules. Production Stage 4 Rules remain exact reviewed blob `ecc8ccb2ab50f0f7057ab3170eb080ad9e36025f`. App Check enforcement remains OFF, Firebase Spark / zero billing remains mandatory, persistent Firestore cache remains disabled, and Stage 5 Remote Joining session writes remain denied.

The complete release gate passed before merge/deploy under standing authorization. Deployment then proved byte-exact r2 shell identity; ordinary owner-controlled evidence proved App Check ready/connected/token-observed and the first private Connected Rivalry slice through revision 1 without local Apply or a reported local-save overwrite. RJR-1 is now 77. Remote-to-local reconciliation and Stage 5 remain locked.

Successor capability leverage: the next environment is expected to have materially stronger Work / GPT-5.6 Sol Max capabilities. Use them aggressively but truthfully when available. Prefer direct automation over owner chores: discover and request a Firebase/provider connection if the product exposes one, inspect provider state directly instead of asking for screenshots when authorized, run the repository's own visual/browser/deployed-site tests and inspect screenshots itself, use the repository GitHub CLI bootstrap (`npm run work:gh:bootstrap`) when shell/CLI access exists, and automate exact-head GitHub/CI/deployment evidence collection. Keep every automation bounded by the existing security, zero-billing, canonical-storage, versioning, WEC/SLE and Remote Joining dependency locks. If a stronger capability is unavailable, record that limitation and fall back to the smallest owner-only action rather than inventing access. Stay organized around the single current goal and avoid generic setup repetition or sidequests.
