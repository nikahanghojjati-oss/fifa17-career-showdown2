# Career Mode Showdown v1.3.0 — Production Proof

Date: 2026-08-13 ET
Status: TECHNICALLY PRODUCTION-PROVEN
Application version: `v1.3.0`
Runtime revision: `1.3.0-r1`
Immediate previous known-good whole shell: `1.2.0-r2`
Public site: `https://nikahanghojjati-oss.github.io/fifa17-career-showdown2/`
Release PR: #42
Release candidate source head: `b8d92e9a8a9eec2820c439c0dd2699e9d825a91f`
Immutable release merge commit: `094401b649954656e27e4a92d027e9532e84ccbf`

## What shipped

v1.3.0 — Recovery & Device Resilience Hardening closes the resilience milestone on top of the proven v1.2.0 Installable Offline App architecture without redesigning the product shell or changing gameplay.

Shipped protections include:

- Candidate A export fails closed when strict canonical reads cannot be trusted and never emits a checksum-valid incomplete backup;
- Candidate B remains strictly read-only analysis;
- Candidate C Apply requires strict exact raw snapshot authority, freshness rechecks, complete in-memory planning, transaction-owned mutation and rollback, anti-clobber ownership, exact post-write verification and critical recovery whenever ownership cannot be proven;
- missing Candidate C strict snapshot authority returns `snapshot-unavailable` before destructive planning or transaction entry;
- Service Worker update activation verifies the complete candidate shell, awaits successful `skipWaiting()`, and only then emits `CMS_ACTIVATION_ACCEPTED`;
- a rejected `skipWaiting()` cannot be preceded by a success acknowledgement;
- whole-shell offline recovery preserves coherent current/previous runtime selection and fails closed instead of assembling mixed revisions;
- browser restart while offline continues selecting a complete known-good shell;
- exact opaque bytes under all three canonical localStorage keys remain unchanged through Service Worker/PWA lifecycle transitions;
- reconnect rendering preserves the true pre-offline external-media state;
- existing Service Worker registration is reused/updated rather than redundantly registered;
- Settings rerenders preserve focused controls and dialog focus ownership;
- Candidate B/C proof artifact semantics no longer manufacture secondary missing-artifact failures after a primary browser failure.

## Protected production behavior retained

The release preserves:

- exactly two managers and the established Showdown/scoring rules;
- `js/screens.js` as sole navigation/history/Smart Back authority;
- `js/storage.js` as sole canonical persistence/destructive mutation authority;
- `js/storageTransaction.js` as the raw transaction engine;
- exactly three canonical localStorage keys;
- the r2 iOS installed-app loading composition with bounded width-owned geometry and subject-safe Marco Reus framing;
- Settings-only install/update presentation;
- subject-safe licensed football photography;
- complete whole-runtime cache selection with `1.2.0-r2` as the immediate previous known-good shell;
- existing startup performance ceilings;
- no Service Worker/PWA user-data mutation.

## Pre-merge exact-head proof

PR #40 established the first complete 13/13 normal PR generation at exact candidate head `b8d92e9a8a9eec2820c439c0dd2699e9d825a91f`:

- Stability `31754669948`;
- Candidate C `31754670077`;
- Candidate B `31754670165`;
- Static `31754670066`;
- V1 Visual Immersion `31754670048`;
- Settings `31754669982`;
- Statistics `31754670106`;
- Transfer `31754669986`;
- Home `31754669951`;
- League `31754670005`;
- Season Review `31754670001`;
- Final Polish `31754670010`;
- Licensed Football Visuals `31754670020`.

Release PR #42 independently reran all 13 normal families on the identical candidate SHA and also completed 13/13 green:

- Stability `31755015098`;
- Candidate C `31755015117`;
- Candidate B `31755015129`;
- Static `31755015064`;
- V1 Visual Immersion `31755015116`;
- Settings `31755015065`;
- Statistics `31755015074`;
- Transfer `31755015077`;
- Home `31755015138`;
- League `31755015105`;
- Season Review `31755015083`;
- Final Polish `31755015086`;
- Licensed Football Visuals `31755015092`.

## Merge and Pages proof

Release PR #42 merged to main at:

`094401b649954656e27e4a92d027e9532e84ccbf`

GitHub Pages build `1150007812` completed successfully from that exact merge SHA.

## Deployed Stability proof

Main Stability run:

`31755136265` — SUCCESS

Jobs:

- stability contracts `94629176999` — SUCCESS;
- canonical Chromium Stability `94629218250` — SUCCESS;
- deployed-site-smoke `94629478166` — SUCCESS.

The deployed-site-smoke job passed, in order:

1. Pages wait and exact verification of every runtime byte;
2. deployed runtime error provenance audit;
3. deployed Home visual audit;
4. deployed crop-safe football-photo audit;
5. deployed Candidate A backup export audit;
6. deployed Candidate B import analysis;
7. deployed Candidate C atomic restore/recovery audit;
8. deployed install/offline boundary audit;
9. complete end-to-end public journey.

This establishes that the public Pages runtime matches the release source and that the production boundary preserves the same recovery, visual and gameplay contracts proven pre-merge.

## Release Integration Burn-In

Main Burn-In run:

`31755136240` — SUCCESS

- integration pass 1 job `94629176579` — SUCCESS;
- integration pass 2 job `94629176501` — SUCCESS.

Both complete stateful integration journeys passed independently on release merge SHA `094401b649954656e27e4a92d027e9532e84ccbf`.

## Production conclusion

The required technical release chain is complete:

release candidate proof → release PR → merge → Pages build → exact deployed-byte/provenance verification → deployed resilience/visual/public-journey proof → 2/2 Release Integration Burn-In.

Therefore Career Mode Showdown v1.3.0 runtime `1.3.0-r1` is technically production-proven.

`1.2.0-r2` is now the immediate previous known-good whole-shell recovery target.

This production proof is immutable release evidence. Later documentation-only main commits do not replace the release merge commit as runtime authority.

Owner real-device visual acceptance remains separate from automated technical proof. No new visual redesign was introduced by v1.3.0.
