# PR #114 scope lock — production App Check bootstrap

This candidate is intentionally bounded to the next real Remote Joining prerequisite after PR #113.

Included:

- preserve owner-verified production reCAPTCHA Enterprise / Firebase App Check registration evidence;
- update production Firebase metadata from `appCheck=not-enabled-yet` to provider-verified registration while keeping enforcement and runtime connection off;
- add a dormant exact-origin/exact-app App Check bootstrap that requires controlled public-config injection and token auto-refresh;
- preserve browser Firestore writes as deny-all and grant no trusted mutation authority;
- add permanent contracts for provider state, bootstrap sequencing, no debug path, no premature enforcement and no Firestore connection;
- establish RJR-1 fixed evidence-backed Remote Joining readiness accounting and preserve the existing seven-line owner report;
- carry the compact successor-context system naturally with this substantive engineering PR.

Explicitly excluded:

- loading Firebase/App Check into the production website in this PR;
- committing the concrete Firebase Web API key or reCAPTCHA Enterprise site key into runtime source;
- App Check enforcement;
- trusted-runtime IAM activation;
- browser Firestore writes;
- Stage 3 Registered Devices / Private Pairing;
- Connected Rivalry or Private Remote Joining UI/runtime behavior;
- application or runtime version bump.

Publication gate: all canonical contracts and normal PR workflow families must pass on one exact unchanged final head before this candidate can be considered merge-ready. Merge itself requires separate authority from the draft-PR publication authorization.
