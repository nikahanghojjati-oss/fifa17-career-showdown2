name: TEMP r4 status sync

on:
  push:
    branches:
      - stage5e/r4-stale-pointer-precedence-2026-09-02
    paths:
      - .github/workflows/_temporary-r4-status-sync.yml

permissions:
  contents: write

jobs:
  sync:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: stage5e/r4-stale-pointer-precedence-2026-09-02
          fetch-depth: 0
      - name: Prepend exact current r4 status only
        run: |
          python - <<'PY'
          from pathlib import Path
          records={
          'PROJECT_STATE.md':'''## CURRENT OVERRIDE — V1.9.0-R4 STALE CONNECTED RIVALRY POINTER PRECEDENCE — 2026-09-02 UTC

RELEASE CANDIDATE — NOT YET PRODUCTION-PROVEN. Current bounded candidate is `v1.9.0 / 1.9.0-r4`; production-proven baseline remains `v1.9.0 / 1.9.0-r3` until exact-head CI/review/merge/Pages/deployed-byte gates complete. Fixed RJR-1 is `88/100` after evidence-only recalculation of the already supplied r3 provider-live Host → Join → ACTIVE rev 1 → Close → CLOSED rev 2 lifecycle. The failed r3 zero-manual-reattach behavior earns zero credit, and source, CI, review, merge, deployment and documentation earn zero RJR credit.

The r4 lane fixes only stale Connected Rivalry pointer precedence. Durable pointer A remains fallback authority. A current pairing candidate B is evaluated even when A exists, but B may replace A only after the existing provider-authorized attach path verifies exact active device/account/manager/profile/save/rivalry authority. Pending, expired, mismatched or unauthorized B cannot displace A. Exactly one Player Two paste remains required; automatic Player One and Player Two convergence must require zero Connected Rivalry Verify/Reattach actions and must not mutate canonical localStorage.

Firebase remains Spark and billing is permanently forbidden. Firestore stays memory-only, App Check enforcement OFF, Candidate C sole destructive remote-to-local gameplay Apply authority, and no public discovery/listing/lobby/matchmaking/community/rankings is authorized.

## IMMEDIATE NEXT TASK AFTER FULL STUDY

Finish r4 on one unchanged exact head: all permanent contracts/workflows including the stale-A/current-B Chromium regression, final-head review, zero unresolved valid threads, clean mergeability, expected-head merge, post-merge/Pages success and exact deployed `v1.9.0 / 1.9.0-r4` byte/runtime verification. Then perform only the minimal owner production acceptance needed to prove one fresh Player Two paste causes automatic exact-B convergence on both roles with zero manual Connected Rivalry Verify/Reattach. Recalculate RJR only if that production behavior itself is newly proven.

---

''',
          'NEXT_TASK.md':'''# CURRENT OVERRIDE — V1.9.0-R4 STALE CONNECTED RIVALRY POINTER PRECEDENCE — 2026-09-02 UTC

Authorized release candidate: `v1.9.0 / 1.9.0-r4`. Production-proven baseline remains `v1.9.0 / 1.9.0-r3` until publication/deployment gates close. Fixed RJR-1 is `88/100` from the already supplied provider-live r3 Remote Joining lifecycle only; r4 implementation/release mechanics earn zero readiness credit.

The only product repair is stale Connected Rivalry pointer precedence: preserve durable A unless current pairing B passes the existing exact provider-authorized attach verification, then persist B automatically. Pending, expired, wrong-account, wrong-device, wrong-manager, profile/save-mismatched or otherwise unauthorized B must preserve A. Player Two pastes exactly once. Player One and Player Two must converge on B with zero manual Connected Rivalry Verify/Reattach and unchanged canonical localStorage.

## IMMEDIATE NEXT TASK AFTER FULL STUDY

1. Complete all r4 deterministic, Chromium and full-contract gates on one exact branch head.
2. Require all 14 permanent PR workflow families, final-head review, zero unresolved valid threads and clean mergeability.
3. Expected-head merge only after the exact head is green, then verify all normal post-merge workflows, Pages exact merge SHA and deployed `v1.9.0 / 1.9.0-r4` bytes.
4. Only after production r4 is live, run the minimal owner acceptance: one fresh Player Two paste, automatic exact current Connected Rivalry on both roles, zero manual Verify/Reattach. Never paste a private session capability into chat.
5. Recalculate RJR only from genuinely new production capability evidence.

Billing remains permanently forbidden. Firebase stays Spark, Firestore memory-only and App Check enforcement OFF. No Cloud Billing, Blaze, payment method, Cloud Run, Cloud Functions, public discovery/listing/matchmaking, canonical-save mutation or Candidate C bypass is authorized.

---

''',
          'README.md':'''# CURRENT RELEASE CANDIDATE — v1.9.0-r4 Stale Connected Rivalry Pointer Precedence

Current source candidate: `v1.9.0 / 1.9.0-r4` — not yet production-proven. Previous production-proven whole-shell recovery target: `v1.9.0 / 1.9.0-r3`. This bounded hotfix preserves durable Connected Rivalry pointer A unless the current Private Pairing candidate B passes the existing exact provider-authorized attachment checks, then persists B automatically for the correct account/device/manager/profile/save. Permanent regression proof requires exactly one Player Two paste, automatic Player One/Player Two convergence on B, zero manual Connected Rivalry Verify/Reattach actions, preservation of A for pending/expired/mismatched B, and unchanged canonical localStorage. Firebase remains Spark and billing remains permanently forbidden. Fixed RJR-1 is `88/100` from the already supplied r3 provider-live Remote Joining lifecycle; r4 source, CI and deployment earn zero readiness credit.

''',
          'CHANGELOG.md':'''## v1.9.0-r4 — Stale Connected Rivalry Pointer Precedence — release candidate

Candidate runtime: `1.9.0-r4`
Previous production-proven runtime: `1.9.0-r3`

- evaluates the current Private Pairing candidate even when a durable Connected Rivalry pointer already exists;
- preserves durable pointer A unless pairing B passes the existing exact provider-authorized attachment path, then persists B automatically;
- permanently tests pending, expired and authorization-mismatched B preserving A, plus exact active B replacing A for Player One and Player Two;
- preserves exactly one Player Two paste, zero manual Connected Rivalry Verify/Reattach actions and unchanged canonical localStorage;
- changes no provider rivalry merely to retire a stale local pointer and changes no Firebase Rules, billing, persistence, App Check enforcement, public-discovery boundary or Candidate C authority;
- records the separate evidence-only RJR-1 recalculation from `87/100` to `88/100` for the previously supplied provider-live r3 actual Remote Joining lifecycle; r4 implementation/release work earns zero RJR credit.

'''
          }
          for name,prefix in records.items():
              p=Path(name)
              text=p.read_text()
              if '1.9.0-r4' not in text[:1200]:
                  p.write_text(prefix+text)
          PY
          git diff --check
      - name: Commit status records
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add PROJECT_STATE.md NEXT_TASK.md README.md CHANGELOG.md
          git commit -m "Sync v1.9.0-r4 release status"
          git push origin HEAD:stage5e/r4-stale-pointer-precedence-2026-09-02
