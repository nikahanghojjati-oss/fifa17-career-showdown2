# START NEXT SESSION — v1.4.33 / PR #174 / RJR87 / Zero-Billing Remote Joining Authorized

SLE = Smart Lean Efficient. Work Environment Continuity (WEC) remains mandatory.

This starter is orientation only. Current source, live GitHub/provider/deployment evidence, `REMOTE_JOINING_READINESS.json`, `NEXT_TASK.md`, `PROJECT_STATE.md`, the successor's fresh WEC and later owner instructions win.

## Verified boundary

Repository: `nikahanghojjati-oss/fifa17-career-showdown2`.

Production remains `v1.8.1 / 1.8.1-r5`, production-proven and unchanged. Production Rules remain exact provider-proven `firestore.spark.rules` blob `2b7c0b166ae0aae7ab7a3ce84725b21091262484`.

PR #173 merged as live main `c005f69c8952fbce8a4b842e69641e5467c36f9a` before PR #174. PR #174 is `Prove Stage 5B device credential foundation`.

PR #174 first implementation proof head/tree: `5b092220ce2507c66cf653e510fbaa2c43fb425d` / `2b525677ba2af016929b0bb1706df9c3f40847aa`.

PR #174 pre-seal head `f4689e6b0440e0490875afe74c75b2a218f3dbc6` passed all 14 workflow families. Codex then found one valid P2: candidate Rules checked only `device_id`, so wrong key/version claims could bypass the intended provider boundary.

The correction requires `device_id`, `device_credential_version` and `device_key_sha256` to match an active exact credential document. Direct emulator fixtures cover missing, forged, wrong-key, wrong-version and revoked credentials. The successor must fetch the final PR head rather than infer publication from this starter.

Fixed Remote Joining readiness remains `87/100`. Candidate source, emulators, CI, SLE and publication receive zero capability credit.

## Controlling owner authorization

Read `00_OWNER_ZERO_BILLING_REMOTE_JOINING_AUTHORIZATION.md` and `ZERO_BILLING_REMOTE_JOINING_ARCHITECTURE_DECISION_2026-08-31.md`.

The owner authorized every remaining engineering, IAM, provider, authentication-policy, Security Rules, runtime, deployment, testing, evidence and publication decision necessary for Remote Joining except billing.

Billing must never be activated. Do not link a Cloud Billing account, enable Blaze, attach a payment method or activate any service that requires billing, even when that service advertises a free usage tier. Cloud Run is excluded.

Standing owner authorization permits merge and deploy after every required automated gate passes.

All required tests and gates must pass before publication.

## Architecture decision

Stage 5B's non-extractable P-256 key, one-use challenge, per-sign-in custom-token claims and atomic revocation remain valid dormant research. Its production trusted issuer is not the critical path because Cloud Run requires billing.

PR #174 preserves the underlying Stage 5B device credential contracts, Chromium browser proof and Auth-plus-Firestore emulator evidence.

The production critical path is existing popup-only Google Authentication plus direct Cloud Firestore client operations and exact-path deny-by-default Security Rules on Firebase Spark.

The next bounded product slice is `stage5c-zero-billing-standard-auth-session-adapter`:

1. use standard Google-token `request.auth.uid` as provider identity;
2. preserve exact opaque session IDs, no listing, exactly two rivalry accounts, host-open, peer-join and terminal lifecycle;
3. treat registered device IDs as account-owned audit/revocation metadata, never as provider-bound cryptographic identity;
4. keep Firestore memory-only and local-first fallback;
5. prove deterministic and Auth-plus-Firestore emulator negatives before production publication;
6. keep production Rules publication, runtime host/join UX and provider-live RJR proof as separate gated slices.

Spark quota exhaustion must fail safely. It must never trigger an upgrade or charge.

## Production non-changes

PR #174 does not change production Rules, runtime, Auth configuration, IAM, billing, App Check enforcement, session documents, host/join UX, local Saves, canonical storage, Candidate C, Connected Rivalry or the protected historical rivalry.

Production session Rules remain excluded from PR #174 and are a later separately reviewed gate.

Permanent locks remain: exactly two private managers; exact capability and no listing; only three canonical localStorage keys; Candidate C sole destructive Apply authority; Firestore memory-only; Google popup-only `browserSessionPersistence` with no extra scopes; App Check enforcement remains OFF; no public discovery, community, matchmaking or rankings.

## IMMEDIATE NEXT TASK AFTER FULL STUDY

1. Independently verify live main, PR #174 exact head/state, changed files, all workflow families, reviews, threads and mergeability. The predecessor WEC required HANDOFF_NOW, so PR publication may still be incomplete.

2. If PR #174 is open, finish only its bounded publication. Require all 14 workflow families on the corrected exact head, inspect the Java 21 Auth-plus-Firestore job, request final-head Codex review, resolve every valid thread, expected-head squash merge under standing authorization, then verify all post-merge workflows, Pages, live main and unchanged production Rules/runtime.

3. Validate/archive predecessor WEC `we-2026-08-31-stage5b-device-credential-foundation`. Initialize a fresh unique WEC from observed live main with reset counters and obey its assessment.

4. If that fresh WEC permits new work, begin only `stage5c-zero-billing-standard-auth-session-adapter`. Do not activate billing or Cloud Run and do not resurrect the owner-decision blocker.

Do not repeat consumed pairing, Candidate C, replay, adverse-provider, token-lifecycle, abuse, rate-limit, rollback, provider-Rules or provider-abuse proof merely for confidence. Do not fabricate provider, device, network or session state.

## Recursive SLE and reporting

At Handoff proximity 100%, provide the successor with a short repository-first next-developer prompt that points to this starter and the mirrored handoff before stopping.

Every substantive owner update uses exactly:

Handoff proximity: X%
Remote Joining readiness: ~Y%
Estimated focused sessions to genuine RJR100: ~N–M
Current lane: ...
Concrete dependency completed: ...
Next unlock: ...
Blocker: ...
Sidequest check: ...

At Handoff proximity 100%, complete a byte-identical mirrored SLE handoff and versioned starter, refresh `SESSION_BOOTSTRAP.json` and progressive context, run `npm run work:next-prompt`, make the final WEC seal the last branch mutation, provide the short repository-first prompt and stop before another substantial milestone.

Every successor recursively preserves SLE, WEC, fixed RJR evidence discipline, zero-billing authority, standing gated publication authorization and this exact reporting format.
