# v1.3 r2 Active Handoff

Started: 2026-08-13 ET
Last checkpoint: 2026-08-13 18:15 ET

Working branch: `agent/v13-r2-hardening`
Session-start base authority: `main` at `49c72abaea30c9d3ed1a3b2f1247315d6c7656da`
Production application: v1.2.0 — Installable Offline App
Production runtime: `1.2.0-r2`
Previous known-good runtime: `1.2.0-r1`
Draft audit PR: #40
Latest fully proven audit head before this Markdown checkpoint: `a5db0a823c1b2baa5055579b85cbe297547bb01e`

## Owner instruction and scope

Continue from verified production source without redesign or planning loops. Audit before editing. Compare PR #37 against current r2 main, salvage only evidence-backed hardening, preserve gameplay, canonical persistence, Smart Back, PWA behavior, performance and accepted visuals, and record meaningful checkpoints continuously.

v1.3.0 is Recovery & Device Resilience Hardening. Profiles, Save Library, accounts, cloud runtime/sync/backup, QR pairing, two-device transport, gameplay/scoring changes, framework replacement and broad UI redesign remain out of scope.

External review disposition: retain its useful pressure for fail-closed raw reads, exact byte transactions, interruption tests, ownership checks and rollback proof. Defer stable manager/device IDs, registry persistence, new canonical keys and remote revision machinery until the later profile/cloud dependency chain. Those ideas are strategically useful but wrong for the current exactly-three-key maintenance boundary.

## Production baseline protected

The shipped r2 iOS standalone loading composition and Settings-only install/update hierarchy remain locked. Do not reintroduce viewport-height-driven loading geometry or a floating/sticky global install rail/panel. Current visual gates continue to cover desktop, low-height desktop, narrow mobile and iOS standalone-height archetypes.

Current architecture authority remains:

- `js/screens.js` — navigation/history/Smart Back;
- `js/storage.js` — canonical persistence/destructive mutation;
- `js/storageTransaction.js` — raw transaction engine;
- `js/scoring.js` — scoring;
- `js/analytics.js` — analytics.

Canonical localStorage remains exactly activeShowdown, legacyShowdowns and preferences.

## PR #37 diagnosis

PR #37 / `agent/v13-hardening` remains untrusted at `221212a87cc58712a1ebd9452d7b71cdaa36327d`.

At that head 11 of 13 normal workflow families failed. Candidate C pure transaction/planning contracts passed, then its browser audit timed out because `#mainMenu` never became visible. Home/Static/Visual lanes also showed mixed 1.3.0-r1 / 1.2.0 identity.

Root cause: commit `6ce7fe6fab87031b69e3dc5e98587fd3f78b3558` (`Freeze v1.3 shell identity`) replaced 211 lines of proven production DOM with an alternate ~80-line shell while existing JS/CSS still expected the production structure. Do not merge/deploy that branch or copy its `index.html`, manifest/package identity or premature v1.3 release records.

Useful PR #37 ideas were revalidated behavior-by-behavior rather than file-by-file.

## Evidence-backed runtime fixes landed on clean r2 branch

### Candidate A strict blocked-read behavior

Defect: Candidate A used the lossy raw backup reader, so a thrown canonical `localStorage.getItem` could become null/missing and produce an apparently valid incomplete backup.

Fix `5d57b30e0c3b4538f1e6b25275f5d4934cfd0e6f`: Candidate A now requires `captureCareerModeRawRestoreSnapshot()` and fails closed.

Regression proof `1cfe4424f7ccaa51c9b613f11507e062e0f8f0c4` stays in the existing Candidate A contract owner and proves blocked-read rejection, zero mutation and exact byte preservation.

### Offline/PWA lifecycle

Three independent r2 defects were reproduced:

1. reconnect could repeatedly restore stale pre-offline media status and clobber later legitimate live text;
2. `activationRequested` was armed after the activation message, allowing `controllerchange` to race ahead of reload intent;
3. an existing online Service Worker registration was verified then redundantly registered again.

Fix `10eab7986207c975e735ecc095857a3a2379584e` corrects all three without restoring global install UI.

Focused proof `da030cd4ad0ffbd3ef38b194044508be695524a5` plus suite wiring `a3af788da68437992000b7aad4c7a37b05c88bb6` consolidates repeated offline/reconnect/live-status, activation ordering/cleanup and single-registration ownership in one v1.3 lifecycle contract.

### Settings focus across live offline-state rerender

Root cause found during Settings audit: the Settings overlay replaces its content tree when connectivity changes. If a user was focused on a control, the focused node was removed and keyboard focus could be lost even though the dialog stayed open.

Fix `0f93dd72773c43502dd3a47b5c3ba2b85101caff` captures an equivalent control selector before the offline-state rerender and restores focus after the new tree mounts. It does not change layout or install hierarchy.

Focused mobile DPR2/touch proof `a5db0a823c1b2baa5055579b85cbe297547bb01e` verifies offline→online rerenders preserve focus, focus stays inside the dialog, Tab wraps from the final control to Settings Close, Escape closes and restores the Settings tile, inert is released, and no global install UI exists.

## Whole-shell recovery evidence

`tests/browser/offline-cache-lifecycle-v2-audit.cjs` now proves:

- coherent first shell installation;
- failed cache population leaves the known-good shell intact;
- new shell waits for explicit activation;
- unrelated Cache Storage survives cleanup;
- corrupt current shell falls back to the complete previous shell;
- a second corrupt upgrade again falls back deterministically;
- browser close/reopen while offline selects the known-good shell;
- corrupt previous shell does not displace a healthy current shell;
- when both complete shells are corrupt, offline navigation fails closed instead of mixing or serving a partial runtime.

The both-corrupt extension landed in `a5b413e3f7408db44d87796644902bef203264b8`. No Service Worker production change was needed because the existing whole-shell selector passed the stronger fixture.

Open hypothesis, not yet a landed defect: `verifyCache()` proves presence/HTTP-ok, not byte hashes. Deployment proof is stronger because `scripts/verify-deployment.mjs` SHA-256 compares every deployed runtime file byte-for-byte. A same-key wrong-byte 200 Response therefore remains a resilience hypothesis. Do not add a hash-manifest architecture until a focused fixture demonstrates a meaningful failure path and an integrity source of truth can be maintained coherently.

## CI/test authority repairs

Several current-main contracts had stale literal assumptions exposed only because PR #40 finally ran the complete repository contract suite against current authority. These were corrected semantically, never by rewriting immutable production proof or weakening gates.

- `e2a8cffaa6974d1f82d55c95e0debf1eaa5da138`: r2 hotfix records no longer have to invent a nonexistent v1.2.0 Git tag; primary release records still require their tag, while later runtime hotfixes require exact runtime/app/previous-shell truth.
- `fc35c4956f06db42c27186e0db71ea4cf4180ca1`: `NEXT_TASK.md` truthfully labels r2 as the Installable Offline App production baseline while v1.3 resilience remains current.
- `e8f720761c6906f2d0be1ca37fa67091ef056b7b` and `c1d8580b5e77d9617200c202afffbec5a59d49d4`: preserve deep future cloud identity/revision/conflict/tombstone/privacy/security architecture but remove stale version-number authority; dependency order is semantic.
- `6584e7362b8dea163a3efa3b5a9e9018e94252b4`: release-authority coherence checks production hotfix changelog truth semantically and future candidate previous-known-good identity from its release record rather than hard-coded 1.1.5-r1.

### Specialist artifact failure semantics

Observed defect: Candidate B/C screenshot uploads used `if: always()` with `if-no-files-found: error`. A cancelled or failed browser step before screenshot creation then produced a second artifact error, obscuring the primary failure. PR #37 and later cancelled audit generations reproduced this.

Corrections:

- `7ea77d2c8650723535671822c82bdebda4df9fa8` — Candidate B uploads evidence only after successful browser proof;
- `933c899ce4e2ff675ba6a99bfd180edfe0e53b84` — same for Candidate C;
- `fcd81f079de58b57e66d1bac326323953de13dc8` — CI contract permanently requires `if: success()` plus `if-no-files-found: error` for both specialist lanes.

Evidence is not weakened: a successful specialist browser proof still fails if its screenshot artifact is missing. A primary browser/cancellation failure now stays primary.

## CI orchestration lesson

On pull requests, a Markdown-only handoff commit can still synchronize the PR because earlier code changes are part of the PR diff. First-attempt concurrency then correctly cancels stale heavy work. This repeatedly cancelled otherwise healthy Stability/Candidate runs during early continuous handoff commits.

Classification: orchestration/process interaction, not a product defect. New working rule: record handoff checkpoints between proof generations, not while a heavy proof is actively running. Do not disable stale-run cancellation merely to accommodate documentation writes.

## Full green evidence

At `a5b413e3f7408db44d87796644902bef203264b8`, all 13 normal PR workflow families completed successfully, including canonical Stability, Candidate B, Candidate C, Settings, Static App, V1 Visual Immersion and Licensed Football Visuals.

After the Settings focus fix/proof, head `a5db0a823c1b2baa5055579b85cbe297547bb01e` again completed all 13 normal PR workflow families successfully:

- Stability `31748819954` — success;
- Candidate C `31748820008` — success;
- Candidate B `31748819999` — success;
- Settings `31748819916` — success;
- Static App `31748819959` — success;
- V1 Visual Immersion `31748819972` — success;
- Licensed Football Visuals `31748820020` — success;
- League Confirmation `31748819961` — success;
- Final Polish `31748819919` — success;
- Transfer `31748819920` — success;
- Home Bootstrap `31748820031` — success;
- Season Review `31748819960` — success;
- Statistics `31748819965` — success.

Static/repository contracts report 13 PR workflows / 27 protected multiline executable blocks. Locked `npm ci` remains reproducible.

Current eager budget evidence is 164,563 raw / 37,355 gzip, leaving only 437 raw bytes and 145 gzip bytes below the protected 165,000 / 37,500 ceilings. Do not add eager code casually and do not raise the limits.

## Remaining audit findings and hypotheses

### Candidate C strict-authority degraded path

`js/restore.js` still falls back to `captureCareerModeRawBackupInputs()` only when `captureCareerModeRawRestoreSnapshot()` is entirely absent. Normal production includes the strict authority and all Candidate C proof is green, but in a partial/mixed runtime failure this fallback is lossy and contradicts the locked rule that Candidate C must use a strict exact raw snapshot or perform no write.

Preferred correction: remove only the loose Apply-stage fallback so missing strict authority returns `snapshot-unavailable`, prove the loose reader is never consulted and the transaction is never called, and leave `js/storage.js` / `js/storageTransaction.js` semantics untouched. The UI has a similar review-stage fallback; even if retained temporarily it cannot be allowed to authorize mutation.

### Residual/version literals

Read-only scan found historical fallback literals such as v1.2.1 in `js/settings.js` and 1.1.4-r1 in `js/optionalModules.js`. In normal runtime both are superseded by authoritative `APP_VERSION` / revision meta and current gates are green. Treat them as cleanup candidates only if reachability or a mixed-runtime fixture proves they can surface; do not churn runtime bytes for cosmetic string cleanup during resilience hardening.

### Repository cleanup

Many root-level historical audits/handoffs remain. They are evidence, not automatically dead files. Do not delete historical production/recovery context simply to reduce file count. Cleanup should target files proven unused, misleading as current authority, or duplicated without evidence value.

## Tooling events

Several large connector writes were rejected by the safety layer before GitHub mutation, including early direct edits to Stability/Candidate C and one workflow attempt. Later smaller authoritative edits succeeded. A local public `git clone` attempt also failed because this runtime cannot resolve github.com; it changed nothing. GitHub connector state is the repository authority.

## Exact next legal action

1. Let the CI generation triggered by this handoff checkpoint finish before another branch write; classify any red lane before changing product source.
2. Close the Candidate C degraded strict-authority fallback with the smallest canonical Apply-stage correction and focused no-write proof if the connector permits a normal file edit. Do not create a parallel restore authority.
3. Continue read-only audit of same-key wrong-byte cache corruption, Smart Back/lazy listener ownership, Settings pointer/touch/reduced-motion behavior and true unused-file residue. Fix only reproduced defects.
4. Keep PR #40 draft. It is not merge/deploy ready while changed runtime bytes still carry production `1.2.0-r2` identity.
5. Freeze v1.3 identity only after hardening scope is mature. Promotion must update runtime/release/cache/document authority coherently in one controlled batch with `1.2.0-r2` as immediate previous known-good whole shell and no shell redesign.
