# v1.3 r2 Active Handoff

Started: 2026-08-13 ET

Working branch: `agent/v13-r2-hardening`
Base authority: session-start `main` at `49c72abaea30c9d3ed1a3b2f1247315d6c7656da`
Production: v1.2.0 / `1.2.0-r2`
Previous known-good: `1.2.0-r1`
Draft CI/audit PR: #40

## Owner instruction

Continue from verified production source without redesign. Audit before editing. Compare PR #37 to current main. Salvage only evidence-backed hardening. Preserve gameplay, canonical data, navigation, PWA, performance and accepted visuals. Keep tests strong, investigate root causes and residual defects, and maintain continuous repository handoff.

External review disposition: retain its pressure for fail-closed raw reads, exact transactions, interruption and rollback proof. Defer manager/device IDs, save registry, new canonical keys and cloud-style revisions because they are outside v1.3 and conflict with the current exactly-three-key maintenance boundary.

## PR #37 diagnosis

Audit input: PR #37 `agent/v13-hardening` at `221212a87cc58712a1ebd9452d7b71cdaa36327d`. Fresh comparison is 26 commits ahead and 68 behind current main.

At the exact PR head, 11 of 13 normal workflow families failed. Representative evidence:

- Home Bootstrap: shell revision was `1.3.0-r1` while `APP_VERSION` still resolved to 1.2.0 identity.
- Static App: package identity was 1.3.0 while application identity was still 1.2.0.
- V1 Visual Immersion: same release/cache identity incoherence.
- Candidate C pure transaction/planning contracts passed, then the browser audit timed out because `#mainMenu` stayed hidden. The subsequent missing-screenshot artifact failure was secondary.

Root cause is the branch-wide shell/release-coherence regression, not Candidate C. Commit `6ce7fe6fab87031b69e3dc5e98587fd3f78b3558` changed only `index.html` but replaced 211 lines of proven production DOM with an alternate 80-line shell while existing CSS/JS still expected the production structure. Do not merge, deploy or continue from that shell.

## PR #37 changed-file classification

Useful after revalidation:

- `js/backup.js`: blocked canonical reads should fail Candidate A closed.
- `tests/contracts/backup-contracts.cjs`: blocked-read/no-mutation proof belongs in the existing Candidate A owner.
- selected `js/offlineApp.js` lifecycle hunks only: preserve true pre-offline media state, arm update reload intent before worker activation can emit `controllerchange`, reuse/update an existing Service Worker registration instead of a redundant second registration.
- v1.3 offline/live-reconnect contract ideas: useful, but consolidate them rather than keeping duplicate specialist files.
- semantic cloud/dependency assertions: useful conceptually because current `CLOUD_STORAGE_FOUNDATION.md` still contains stale old milestone numbering.

Defensive but not yet a reproduced r2 product defect:

- `service-worker.js` empty previous-revision sentinel guard and its focused contract. r2 has a real previous revision (`1.2.0-r1`), so this is not the first runtime fix.

Stale/conflicting or evidence-only:

- `index.html`: shell regression; reject wholesale.
- `manifest.webmanifest`: invalid branch release-identity work; do not copy.
- `package.json` / `package-lock.json`: mixed invalid release identity and test wiring; do not copy wholesale. Protect lockfile reproducibility.
- `RELEASE_V1.3.0.md` and `CAREER_MODE_SHOWDOWN_V1.3.0_MAINTENANCE_HANDOFF.md`: premature records for an invalid candidate; historical evidence only.
- PR #37 versions of `00_CURRENT_HANDOFF.md`, `PROJECT_STATE.md`, `NEXT_TASK.md`: superseded by current main.
- `release-authority-v2.cjs` and the one-line delegated `release-authority-coherence.cjs`: useful experiment but tied to stale exact roadmap phrases and an assumed `r1` candidate revision. Do not replace the current authoritative release contract with it.
- PR #37 whole `js/offlineApp.js`: stale because r2 later removed the rejected floating global install UI. Only isolated lifecycle behavior may be salvaged.
- PR #37 cloud document rewrite/consolidation: not safe to copy; one revision incorrectly called `1.2.0-r1` production and the next collapsed a detailed future contract into a summary that points at a historical commit. Correct current prose directly instead.
- `tests/support/run-contract-suite.cjs`: test-only wiring; change only if needed to preserve one-owner regression evidence.

## First reproduced/evidence-backed r2 defects and fixes

1. Candidate A blocked-read ambiguity. `captureCareerModeRawBackupInputs()` uses lossy `readStorageValue()`, which converts a thrown `localStorage.getItem` into `null`. `backup.js` then treats the unread key as missing and can create an apparently valid incomplete backup. `captureCareerModeRawRestoreSnapshot()` already provides the exact fail-closed authority, so no new persistence authority is needed.

   Fix commit `5d57b30e0c3b4538f1e6b25275f5d4934cfd0e6f`: Candidate A now requires the strict exact raw restore snapshot and aborts if any canonical read is blocked.

2. Offline media reconnect stale-state clobber. r2 retained `menuMediaStatusBeforeOffline` after reconnect and restored it on every later online render without proving the offline override was still active.

3. Update activation race. r2 set `activationRequested=true` only after awaiting `CMS_ACTIVATE_UPDATE`; the waiting worker can call `skipWaiting()` and cause `controllerchange` before that continuation.

4. Redundant Service Worker registration. When an existing registration was found while online, r2 verified it but fell through to `navigator.serviceWorker.register()` again.

   Fix commit `10eab7986207c975e735ecc095857a3a2379584e`: reconnect restores only the actual offline override then clears its snapshot; update intent is armed before the activation message and cleared on rejection/no-waiting paths; existing registrations update in place and return before the new-registration path. No install UI or shell DOM was reintroduced.

Regression proof:

- `1cfe4424f7ccaa51c9b613f11507e062e0f8f0c4`: Candidate A blocked-read/no-mutation/raw-byte preservation evidence remains inside `backup-contracts.cjs`.
- `da030cd4ad0ffbd3ef38b194044508be695524a5`: one consolidated `v13-offline-lifecycle-contracts.cjs` owns repeated-offline/reconnect/live-status behavior, activation ordering/cleanup and single-registration ownership.
- `a3af788da68437992000b7aad4c7a37b05c88bb6`: the new offline lifecycle contract is wired once into the existing repository contract suite.

## Draft PR #40 and CI classification

PR #40 is deliberately a draft CI/audit harness. It remains explicitly non-mergeable while changed runtime bytes still carry production `1.2.0-r2` identity; coherent v1.3 identity must be frozen only after the hardening audit is mature, with r2 as the immediate previous whole-shell recovery target.

First PR #40 CI exposed a latent current-main contract defect before new contracts ran. `static-app-release-contracts.cjs` required the r2 hotfix record to claim a nonexistent `v1.2.0` release tag. GitHub ref lookup for `refs/tags/v1.2.0` returned 404. History traced the defect to `12d239d6873e9362c7f32f2513daf003ad2cb009` (`Compact static release contracts and support r2`).

Correction `e2a8cffaa6974d1f82d55c95e0debf1eaa5da138`: primary r1 records still require their tag; later runtime-hotfix records require exact runtime, application version and immediate previous whole-shell recovery target. Immutable r2 release evidence was not edited.

The next Stability contract failure was stale current-task wording: the contract still looked for Installable Offline App in `NEXT_TASK.md` even though v1.3 resilience is current. Direct contract edits were rejected twice by the connector safety layer before mutation. Truthful compatibility clarification `fc35c4956f06db42c27186e0db71ea4cf4180ca1` now labels the shipped r2 section `v1.2.0-r2 Installable Offline App production baseline that must be preserved` while leaving `Next milestone: v1.3.0 — Recovery & Device Resilience Hardening` authoritative.

The next repository-contract failure correctly exposed stale cloud/roadmap authority. Current `POST_V1_ROADMAP_EXECUTION.md` already marks v1.3 resilience current and Local Profiles future/version pending, but `CLOUD_STORAGE_FOUNDATION.md` and its contract still treated the historical v1.8/v1.9 numbering as current authority.

Corrections:

- `e8f720761c6906f2d0be1ca37fa67091ef056b7b`: preserve the detailed future identity/revision/conflict/tombstone/privacy/security architecture while making v1.3 resilience current, Local Profiles later, and Cloud Readiness → opt-in Cloud Backup semantic rather than bound to retired version numbers.
- `c1d8580b5e77d9617200c202afffbec5a59d49d4`: cloud contract now protects the semantic dependency chain and current no-cloud boundary rather than stale numeric labels.

The next failure came from `release-authority-coherence.cjs`, which assumed production changelog content must literally begin `# v<version> —`. Current production truth correctly begins with the document title and then `## v1.2.0 runtime hotfix r2 — production`. History showed this literal assumption came from the August 13 compaction pass.

Correction `6584e7362b8dea163a3efa3b5a9e9018e94252b4`: release authority remains strict about runtime publication, rollback ownership, exact snapshots, current roadmap, cloud boundary and workflow topology, but production hotfix changelogs are checked semantically. Future release candidates now derive their previous known-good runtime from their release record instead of the obsolete hard-coded `1.1.5-r1` fallback.

At `6584e7362b8dea163a3efa3b5a9e9018e94252b4`:

- Static App run `31748265322` is fully green: JavaScript syntax, dynamic release architecture, complete repository contracts and permanent workflow topology all passed.
- repository contract suite reports 21 contract files green;
- permanent topology reports 13 PR workflows / 27 multiline executable blocks;
- Candidate A strict blocked-read proof passes;
- v1.3 offline lifecycle proof passes;
- cloud semantic dependency proof passes;
- release authority coherence passes;
- Candidate C at the previous `c1d8580...` head completed green and is rerunning at the current head;
- V1 Visual Immersion, Settings, Season Review, Transfer, Home Bootstrap, League Confirmation, Statistics and Final Polish are green at the current head;
- Stability contract stage is green and its Chromium runtime/offline/complete-journey stage is currently in progress.

Locked dependency install remains reproducible. Stability `npm ci` passed. Static budget evidence at current runtime source is 164,563 eager raw / 37,355 eager gzip. With ceilings 165,000 / 37,500, remaining headroom is only 437 raw bytes and 145 gzip bytes. Do not add eager runtime code casually and do not raise ceilings to hide this pressure.

## Additional audit findings not yet landed

Candidate C degraded-authority gap: `js/restore.js` currently falls back to `captureCareerModeRawBackupInputs()` if `captureCareerModeRawRestoreSnapshot()` is entirely unavailable. The normal production path has strict authority and is green, but this degraded path is fail-open because the fallback reader is lossy. The correct fix is to fail `snapshot-unavailable` when strict authority is absent and add a no-loose-fallback contract. A narrow source update attempt was rejected by the connector safety layer before GitHub mutation. Do not introduce a wrapper or parallel restore authority to bypass tooling; retry only if the canonical file can be edited safely.

Specialist artifact-failure noise: Candidate B and Candidate C workflows upload screenshot evidence with `if: always()` plus `if-no-files-found: error`. PR #37 demonstrated the consequence: a genuine browser failure before screenshot capture also produced a second artifact failure. Correct semantics are to require the artifact after successful browser proof while leaving the original browser failure primary. An attempted Candidate B workflow edit was rejected by the connector safety layer before mutation. Record this as a CI ownership/configuration defect, not a product defect; do not weaken evidence requirements.

Service Worker corruption audit: current whole-shell selection verifies every required Response and chooses forced → current → previous only when a complete shell exists. Existing lifecycle browser proof covers failed population, no automatic activation, current corruption fallback to previous, repeated update corruption fallback, unrelated-cache preservation and offline restart. Audit is continuing for previous-only corruption and both-shell corruption so partial/mixed shell behavior cannot silently appear.

## Testing decision

Do not carry PR #37's duplicate `v13-offline-hardening` plus `v13-live-reconnect` files as separate permanent owners. One focused v1.3 offline lifecycle contract owns these related cases. Candidate A blocked-read proof stays in its existing `backup-contracts.cjs` owner.

Any red workflow is classified before production code changes. Distinguish product defect, test/runtime defect, infrastructure flake and CI ownership/configuration defect. Do not lower visual or performance thresholds to obtain green CI.

## Tooling events

The first attempted long handoff-file creation was rejected by the connector safety layer before GitHub mutation; the shorter retry succeeded. Later attempted edits to `stability-contracts.cjs`, the Candidate C strict-fallback path, and Candidate B artifact upload semantics were also safety-blocked before mutation. These are tooling events, not repository changes or product failures.

GitHub connector writes are authoritative in this environment; authenticated `gh` CLI is unavailable.

## Exact next action

1. Let current PR #40 Stability/Candidate B/C specialist runs finish and classify any failure before changing source.
2. Extend or otherwise prove the whole-shell corruption boundary for healthy-current/corrupt-previous and both-corrupt states without changing Service Worker behavior unless a real defect is reproduced.
3. Continue Candidate C strict-authority and CI artifact-semantics corrections only if they can be landed inside existing authorities without tooling workarounds.
4. Audit Settings focus/pointer/touch/reduced-motion, Smart Back/lazy listener ownership, low-height/mobile/DPR2/accessibility, dependency residue/dead files and performance headroom.
5. Do not freeze v1.3 release identity until the hardening audit is mature. When identity is eventually promoted, update the entire runtime/release/cache/document authority coherently in one controlled batch with `1.2.0-r2` as immediate previous whole-shell recovery target and no shell redesign.
