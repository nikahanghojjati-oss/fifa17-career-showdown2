# Career Mode Showdown v1.2.0 Release Record

Release date: 2026-08-13
Release tag: `v1.2.0`
Runtime asset revision: `1.2.0-r1`
Status: PRODUCTION-PROVEN — merged, deployed to GitHub Pages, exact-byte verified, and exhaustively validated at the public boundary.

## Immutable production evidence

- runtime merge SHA: `e5acd4ae524f181242df3114b35fd2e812cd8f3b`;
- GitHub Pages deployment: `5891182853`;
- Pages build/deployment run: `31716786499`;
- Stability run: `31716787806`, attempt 1;
- deployed-site-smoke job: `94503946791`;
- Release Integration Burn-In: `31716787876`;
- Burn-In pass 1 job: `94503420385` — complete stateful journey passed;
- Burn-In pass 2 job: `94503420339` — complete stateful journey passed.

The deployed Stability boundary verified every runtime byte from the merged revision before running runtime-error provenance, Home visual, crop-safe licensed-football visual, Candidate A backup, Candidate B read-only analysis, Candidate C atomic restore/recovery, install/offline, and complete public-journey audits. Every step passed.

This is technical production proof. It does not fabricate a separate owner visual-acceptance statement.

## Scope

v1.2.0 is the Installable Offline App milestone from the approved post-v1 roadmap. It adds an installable local-first application shell without changing competition rules, persistence authority, Candidate A/B/C data-safety contracts, accepted football presentation, or protected startup budgets.

The release includes:

- a Web App Manifest and original Showdown 17 install artwork;
- a version-owned first-party application-shell cache;
- atomic Service Worker installation that discards an incomplete new cache rather than replacing the active known-good runtime;
- no automatic install-time activation;
- explicit user-controlled Update Ready activation at safe application boundaries;
- Candidate C transaction/recovery busy-state protection around update activation;
- whole-runtime cache selection so a corrupted revision never silently borrows files from another revision;
- worker-owned verified connectivity rather than trusting `navigator.onLine` alone;
- explicit offline degradation for external YouTube media while the local tracker remains usable;
- Chromebook/Android installation guidance and browser-appropriate fallback guidance elsewhere;
- local and deployed proof for online install, installed-offline boot, exact three-key data preservation, optional Candidate A/B/C loading, online recovery, failed cache population, explicit update activation, corruption recovery, unrelated-cache preservation, and two consecutive upgrade/recovery cycles.

## Protected invariants

v1.2.0 does not alter:

- exactly two managers;
- Showdown lengths `[1,3,5,10]`;
- same selected league with different permanent clubs;
- the maximum-11 season scoring system or 0–0-only tiebreak;
- League confirmation or Club rivalry confirmation checkpoints;
- the Transfer Challenge state machine;
- the Season Review persistence boundary;
- Statistics, Legacy, and Trophy calculations;
- centralized Smart Back/navigation ownership in `js/screens.js`;
- the three canonical localStorage keys or `js/storage.js` persistence authority;
- Candidate A non-mutating backup format v1;
- Candidate B strictly read-only import analysis;
- Candidate C immutable confirmed intent, strict exact raw snapshot, last-moment prewrite checks, transaction-owned mutation/rollback, byte-for-byte verification, and anti-clobber semantics;
- the protected Marco Reus and accepted licensed football-photo presentation;
- eager startup ceilings of 165,000 raw bytes and 37,500 gzip bytes;
- local-first operation.

## Release quality evidence

The frozen candidate passed all 13 normal PR workflow families together before merge. Main then passed both focused Release Integration Burn-In journeys. GitHub Pages deployed the merge revision and deployed Stability passed exact-byte coherence plus the exhaustive public boundary.

The release candidate measured 164,563 eager raw code bytes and 37,355 eager gzip bytes, preserving the existing limits rather than increasing them.

## Historical rollback authority

v1.1.5 / `1.1.5-r1` remains immutable historical rollback and regression evidence at runtime SHA `ff755a9863abc843ae9aac45178428e3a104fc65`. It is no longer the current production release.

## Next legal development step

The immediate post-release work is v1.3.0 Recovery & Device Resilience Hardening. The older roadmap label that assigned v1.3.0 directly to Local Profiles and Save Library is superseded as a current task by the later v1.2 maintenance handoff. Profile/save-library work remains a future dependency but must not be started under the stale v1.3 label until its version assignment is explicitly reconciled.
