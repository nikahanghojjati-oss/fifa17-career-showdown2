# SUCCESSOR HANDOFF PR215 — GITHUB CLI BOOTSTRAP SAFE TRANSFER — SLE V1.4.61 — 2026-09-07

SLE = Smart Lean Efficient. This handoff records a safe red-CI transfer after the a63 recursive anti-spiral circuit breaker. It is orientation only; current source and live GitHub/provider/deployment evidence win.

## Authority snapshot

Repository: `nikahanghojjati-oss/fifa17-career-showdown2`
Branch: `fix/pr214-postmerge-dual-screen-contract`
Recorded main: `420d0dd21480c660d4fe139ba011eca1ccb987a9`
Active PR: #215, open at the last live check, with no reviews, issue comments or inline review comments.
Production-proven authority: `v1.9.1 / 1.9.1-r5`.
Release candidate: `v1.9.1 / 1.9.1-r6`, not yet accepted production authority.
RJR-1: `100/100` frozen.
SSJR-1.1: `0/100`.
MDP-1: `39.00/100`; a63 contribution `+0.00`.
Closing WEC: `we-2026-09-07-pr215-r6-publication-a63`.

## Exact CI evidence, never combine across heads

The last substantive exact head inspected before sealing was `71d4b630bcbffac7c2291aeef8a8efe6b4e24f5a`. All 15 current permanent workflow families completed on that exact head: 12 green / 3 red / 0 pending. Red: Validate Stability Lane, Validate Static App, Validate Stage 5F Authenticated Negatives.

Stability run `34162087837`, job `101865812226`, reached the complete repository contract suite. Before the failure, it passed the current-authority contract, WEC, HTR-1, SHP-2, owner-reporting, SLE packaging, current next-developer prompt, interruption-resilience and forward-progress anti-loop contracts. It then failed `tests/contracts/github-cli-bootstrap-contracts.cjs:28` because `00_DEVELOPER_START_HERE.md` did not match literal `/GitHub CLI bootstrap/i`. The document currently says to use the connected GitHub app/connector first and, only if native `gh` is genuinely required and unavailable, use the repository-owned bootstrap. The successor must inspect the surrounding contract and bootstrap authority together before changing either side.

The first a63 correction head `84cf92278b1c7eb8f441c032de824363aec01137` had the same three shared repository-suite families red, but its exact failure was earlier: `work-environment-interruption-resilience-contracts.cjs:47` required the last published implementation-authority environment/main provenance in `NEXT_TASK.md`. The second bounded correction restored only `we-2026-09-06-ssjr-production-two-account-a55` and `ae89974dfc91753d4a57d83c0f2ca449fc7eacd6`; on `71d4b630...` that interruption-resilience contract passed. Do not reopen it without a reproduced regression.

Because the second a63 correction/validation cycle was still red, the owner-directed SHP-2 recursive anti-spiral breaker fired. No third correction was attempted. The continuity/SNS seal commit advances the branch beyond `71d4b630...`; its own CI is a new evidence generation and must be resolved independently by the successor.

## What a63 changed successfully

A63 separated immutable historical V1.4.56/a58 and PR210/209/207/205/203 provenance from current V1.4.60/a62 successor authority. Current WEC ancestry now follows the versioned successor capsule, literal SHP zero reset is enforced for a fresh successor, sealed lifecycle accepts independently derived handoff decisions, and default next-developer prompt generation follows the additive current capsule instead of stale root historical routing. `NEXT_CHAT_HANDOFF_PROMPT.md` was brought to V1.4.60. These exact contract surfaces passed on `71d4b630...`.

A63 also restored only the two `NEXT_TASK.md` historical implementation-authority provenance markers required by interruption resilience. Product runtime, provider Rules, billing configuration, security authority and SSJR/MDP scoring were not changed.

## First safe action

Validate the a63 archive and ensure status/archive identity, independently resolve live main and exact PR215 head/checks/reviews/comments/threads, then initialize a fresh unique WEC (`a64` only if unused) with literal SHP-2 `0%`, no inherited counters and no inherited transition decision. Never use HTR-1 as session pressure.

Then inspect only the exact current shared repository-suite assertion around `tests/contracts/github-cli-bootstrap-contracts.cjs:28` together with current `00_DEVELOPER_START_HERE.md` GitHub-access/bootstrap wording. Decide the smallest objective authority correction. Do not blindly add a phrase or weaken the test; preserve the connector-first policy, repository-owned fallback, credential-safety rules and current package routing.

## Publication gates after the blocker is corrected

All 15 permanent workflow families must be green on one exact reviewed head. Immediately refresh PR head, reviews, comments and unresolved threads. Only then expected-head squash merge PR215. After merge, verify exact-main Stability including Chromium and deployed-site smoke, Pages/deployed coherent `v1.9.1 / 1.9.1-r6`, and Release Integration Burn-In. Only after the whole boundary is clean may the four Shared Setup product-integration stages add `+2.50` MDP, moving `39.00 -> 41.50`; SSJR remains unchanged. Only then say `YOUR TESTING TIME` and conduct the irreducible genuine two-account Shared Setup acceptance.

## Permanent locks

Billing permanently OFF; Firebase Spark only; never Blaze, Cloud Billing, a billing account, Cloud Run or Cloud Functions. App Check enforcement OFF. Firestore browser persistence memory-only. Google Auth popup-only `browserSessionPersistence`, no extra scopes. Exactly two private managers. Connected Rivalry pairing plus exact ACTIVE before league or club authority. Candidate C sole destructive remote-to-local gameplay Apply authority with transaction-owned strict exact raw-snapshot rollback. Preserve `careerModeShowdown.saveLibrary`, `careerModeShowdown.legacyShowdowns`, `careerModeShowdown.preferences`, and `SSJR-DUAL-FULL-SCREEN-1`. No public discovery/listing/lobby/matchmaking/community/rankings/global leaderboards. No durable raw private capability/account/device/rivalry/session/pairing identifiers.

## Recursive transfer rules

SHP-2 resets to zero only in a fresh environment and uses monotonic observable proxies. HTR-1 is separate repository transfer readiness. Generate a tiny VTLS SNS after every substantial state change. If the successor itself reaches a repeated failed correction/validation plus red/context-damage trigger, generate a fresh full SNS and transfer rather than loop. Every final transition requires mirrored starter/deep handoff, current routing, sealed WEC archive and a downloadable owner SNS.

Immediate next task after full study: resolve the exact GitHub CLI bootstrap authority-contract failure in one bounded current-authority batch, then demand one exact all-green 15-family PR215 head before publication.
