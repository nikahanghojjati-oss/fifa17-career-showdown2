# NEXT TASK — Career Mode Showdown

## CURRENT SUCCESSOR AUTHORITY — POST-PR #99 REMOTE-JOINING UNBLOCK CLOSEOUT — 2026-08-19 ET

Current verified source and live GitHub override every historical record. This file is the sole primary owner of the current implementation authorization boundary.

Status: BOUNDED POST-PR #99 CURRENT-AUTHORITY CLOSEOUT / PREPARE_HANDOFF / NON-RUNTIME / NON-PROVISIONING / PRODUCTION FIREBASE DISCONNECTED.

Current branch: `agent/post-pr99-remote-joining-unblock`.
Current environment: `we-2026-08-19-post-pr99-remote-joining-unblock`.
Starting independently verified live main: `0f61225b267e8334467a6d868d36c7ce58dd54a0`.
Current WEC decision: `PREPARE_HANDOFF`.
Usage: unavailable and not estimated.
Authorized product candidate: none.

The exact full pre-closeout `NEXT_TASK.md` is preserved byte-for-byte at `authority-history/NEXT_TASK_POST_PR99_PRE_CLOSEOUT_FULL.md` with blob SHA `aeb826fdde2ea9e6b0ce7c886d2dc1a419158601`. Earlier protected authority history also remains preserved in repository history and `authority-history/NEXT_TASK_PRE_PR98_TRANSITION_FULL.md`. Historical material is provenance only and never overrides this current section.

## Verified publication boundary

PR #99 `Reconcile post-PR98 successor continuity` is DONE / MERGED / PROVEN.

Exact PR #99 head: `fc9dcc9cb7d298beb8fc07f4ed8caf2470394da3`.
Squash merge / independently verified live main: `0f61225b267e8334467a6d868d36c7ce58dd54a0`.
PR #99 changed exactly five files. All 13 normal pull-request workflow families succeeded on the unchanged exact head. Submitted reviews and inline review threads were empty.

The predecessor `HANDOFF_AT_CHECKPOINT` belonged only to `we-2026-08-19-post-pr98-stage2-selection`; it is fulfilled and is not inherited by this successor.

## Current Remote Joining dependency state

Stage 1 — Cloud / synchronization readiness — DONE / MERGED / PROTECTED through Phase 1F.

Stage 2 — Private account / authentication / authorization — ACTIVE prerequisite lane.

Stages 2A through 2I — DONE / MERGED / PROVEN at their protected dormant/non-production boundaries.

Stage 2 as a whole remains incomplete. No later Stage 2 prerequisite is selected by this closeout.

Stage 3 Registered Devices / Private Pairing remains BLOCKED until every actually required Stage 2 prerequisite is DONE / MERGED / PROVEN.
Stage 4 Connected Rivalry remains BLOCKED behind Stage 3 and earlier prerequisites.
Stage 5 Private Remote Joining remains PRIORITIZED LONG-TERM / DEPENDENCY-GATED / NOT YET IMPLEMENTATION-AUTHORIZED.

Required dependency direction remains:

Cloud / synchronization readiness
→ private account / authentication / authorization
→ paired-device / private-session capability
→ Connected Rivalry
→ Private Remote Joining
→ end-to-end hardening / stable release.

After this bounded closeout is merged and independently verified, reassess WEC. If that fresh reassessment permits continued engineering, reconstruct the remaining Stage 2 dependency graph from current verified source and select exactly one smallest necessary unblocked prerequisite that materially reduces the distance to Stage 3 and Private Remote Joining. If Stage 2 is actually complete, prove that completion and unlock Stage 3 instead of inventing artificial Stage 2 work.

## Protected trust and production locks

Stage 2I remains DONE / MERGED / PROVEN / PRODUCTION DORMANT / NON-PROVISIONING / PRODUCTION FIREBASE DISCONNECTED.

For every future protected browser-to-trusted-service request, preserve this order:

1. Stage 2H production-origin defense in depth.
2. Transient `X-Firebase-AppCheck` token.
3. Trusted Firebase Admin App Check verification.
4. Exact expected production Firebase Web App identity verification.
5. Exact two-entry production project audience verification.
6. Stage 2F revocation-aware `verifyIdToken(idToken, true)`.
7. Architecture `accountId` derived only from verified Firebase UID.
8. Exact operation-specific Career Mode Showdown application authorization.
9. Only then execute the explicitly reviewed trusted operation under Stage 2H IAM.

App Check is attestation only. It grants no user identity, application authorization, device authority, pairing authority, rivalry authority, session entitlement, gameplay authority or IAM authority.

Stage 2H reviewed runtime permissions remain exactly:

```text
firebaseauth.users.get
datastore.databases.get
datastore.entities.get
datastore.entities.create
```

Do not silently broaden this role. Optional App Check limited-use token consumption remains separately reviewed and outside the default Stage 2H role.

Every application-client Firestore create/update/delete remains denied. The Phase 1D / Phase 1F idempotency-receipt finding is not repaired by loosening Security Rules. Privileged shared mutation requires a trusted mutation gateway or a separately reviewed protocol/schema redesign.

Production Firebase, Firebase Web App, reCAPTCHA Enterprise, App Check enforcement, Cloud Run, service accounts, IAM bindings/custom roles, billing, production users/data/provider configuration and production Security Rules remain disconnected/unprovisioned unless later separately authorized current source explicitly changes that state.

## Storage, recovery, identity and private-product locks

Canonical browser storage remains exactly:

1. `careerModeShowdown.saveLibrary`
2. `careerModeShowdown.legacyShowdowns`
3. `careerModeShowdown.preferences`

Do not restore `careerModeShowdown.activeShowdown` as a permanent fourth canonical key.

Candidate A remains non-mutating export. Candidate B remains read-only analysis. Candidate C remains the sole destructive import Apply authority with strict exact raw snapshot authority, exact preconditions, last-moment raw guards, transaction-owned mutation, ownership-scoped reverse rollback, anti-clobber protection, exact post-write verification, byte-for-byte rollback verification, corrupt-byte preservation, retry/idempotence and critical recovery intact.

Firebase Auth UID maps only to architecture `accountId`. It remains distinct from `profileId`, `saveId`, `seasonId`, `deviceId`, `installationId`, `rivalryId`, `sessionId` and `inviteId`.
Exactly two manager slots remain authoritative. A disabled account does not silently relinquish entitlement, and a surviving manager never gains accidental unrestricted sole shared mutation authority.

Local Profiles and Save Library remain completed shipped dependencies.
Public discovery, public profiles, public matchmaking, public invitation directories, community systems, global leaderboards and public rankings remain eliminated.

## Production/version baseline

Application milestone remains `v1.4.0` — Product Deepening.
Package remains `1.4.0`.
Current production Installable Offline App runtime remains `1.4.0-r1`.
Previous whole shell remains `1.3.0-r2`.
Completed resilience baseline remains v1.3.0 — Recovery & Device Resilience Hardening.

This closeout changes no production runtime behavior and therefore receives no semantic application version bump.

## Current authorized task

Complete only this one post-PR #99 authority closeout:

1. preserve PR #99 post-seal publication facts and the exact pre-closeout authority archive;
2. replace stale predecessor implementation authorization with this fresh successor boundary;
3. update only permanent contracts that otherwise freeze the completed predecessor checkpoint;
4. preserve all Stage 2F/2H/2I, Firestore, production-isolation, storage, recovery, identity, two-owner, gameplay and private-product locks;
5. do not risk rewriting append-only `WORK_ENVIRONMENT_HISTORY.md` merely for cleanliness when the connector lacks a safe additions-only patch route;
6. run the complete normal validation gate on one coherent exact head;
7. require clean submitted reviews, inline review threads and mergeability;
8. publish with expected-head protection and independently verify live main;
9. close the continuity/history lane;
10. reassess WEC before the distinct next Stage 2 engineering milestone.

Do not select or implement another Stage 2 prerequisite in this environment before this closeout is fully published and WEC is reassessed.
Do not begin Stage 3, Connected Rivalry or Private Remote Joining in this closeout.
Do not provision production cloud resources in this closeout.
Do not create another history-only milestone merely because this closeout produced history.

## IMMEDIATE NEXT TASK AFTER FULL STUDY

Finish and publish this bounded post-PR #99 current-authority closeout. Then immediately reassess WEC. If continuation is permitted, return directly to the Remote Joining roadmap and select exactly one smallest remaining Stage 2 prerequisite from current verified source; do not start another continuity/history sidequest without a concrete failing gate or safety contradiction.
