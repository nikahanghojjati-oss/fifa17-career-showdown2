Open the live repository `nikahanghojjati-oss/fifa17-career-showdown2` and read `START_NEXT_SESSION_V1.4.52_PR207_POSTMERGE_GREEN_SSJR_PRODUCTION_TWO_ACCOUNT_NEXT.md` first. Treat the handoff as orientation only. Read `00_BUILD_FIRST_PRODUCT_POLICY.md` and `00_HANDOFF_PROXIMITY_STAGE_GATES.md` early: target roughly 75% actual product implementation / 25% validation-maintenance-continuity, use targeted checks and atomic multi-file Git tree/commit batching before publication, and never use the old heuristic Handoff proximity. Under V2, unresolved terminal failures cap proximity at 70%, pending publication CI is 85%, post-publication green is 98%, 99% requires a fully sealed package with no repository mutation remaining, and 100% means generate SNS immediately and stop. Independently verify current `main` beginning from `791b5f9ad48e8d6d5623fd7271300f7266cfae1e` / tree `eed8b9c21bcb814a36d96ff43af93bf129a3766c`, PR #207 final reviewed head `c6bf6b2cb0d492f2da727b8591e8fb7f118e3db6`, all 15 exact-head workflow families and all 15 post-merge workflow families, including Stability `34041920689` attempt 2 on unchanged `1.9.1-r3` bytes; verify zero unresolved PR207 review threads, PR #205 strict validator authority, production authority PR #203 at `v1.9.1 / 1.9.1-r3`, live Spark Rules release `cloud.firestore` ruleset `73b4435e-85a8-49f9-92ef-8ffe3ce0f91c`, frozen RJR-1 100/100, fixed SSJR-1.1 0/100, and closing WEC `we-2026-09-06-ssjr-production-shared-setup-a51`. Validate/archive that WEC, initialize a fresh unique WEC, then execute IMMEDIATE NEXT TASK AFTER FULL STUDY: obtain genuine production-two-account Shared Setup observations on r3, automating every feasible non-private step first. Use two legitimate private manager accounts and two distinct registered browser identities. Exact Connected Rivalry pairing plus exact ACTIVE must precede league/clubs. Pipe each private observation only through stdin to `npm run record:ssjr-production-shared-setup`, retain only the privacy-safe output, then validate both outputs with `npm run validate:ssjr-production-shared-setup -- <player-one-evidence.json> <player-two-evidence.json>`. Prove identical authoritative final `SHOWDOWN_CONFIRMED` revision 6, reload/reconnect/fresh-session continuity without redraw, all required adverse/modified-client denials, and unchanged canonical local saves. CI must not fabricate the two real-account observations and the validator must not be weakened. Do not start transfer/results/scoring first. Once Shared Setup evidence is genuinely credited, return immediately to product-building and begin the next authorized career-start / transfer-results-season-scoring capability rather than extending the proof lane. The prior RJR two-device/two-independent-network acceptance is consumed unless a proven regression invalidates it. Billing must remain permanently OFF and Firebase must remain Spark. Keep App Check enforcement OFF, Firestore browser persistence memory-only, Google Auth popup-only `browserSessionPersistence`, exactly two private managers, and Candidate C as the sole destructive remote-to-local Apply authority. Report every substantive checkpoint using the exact eight-line SSJR block, including `Estimated focused sessions to genuine SSJR100` (currently ~5–10).
