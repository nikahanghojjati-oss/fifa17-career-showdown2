# START NEXT SESSION — V1.4.29 — PR #171 / RJR86 / Provider Abuse Acceptance

Open the live repository `nikahanghojjati-oss/fifa17-career-showdown2` first. Treat this starter and every handoff artifact as orientation only; current source, live GitHub/provider/deployment evidence and later owner instructions win.

SLE = Smart Lean Efficient. Work Environment Continuity (WEC) is mandatory. Never inherit the predecessor transition decision. Independently validate/archive the predecessor WEC, initialize a fresh unique WEC with reset per-environment counters and the independently observed live-main SHA, run its own assessment, then obey that decision before substantive work.

## Entry boundary

Pre-publication live main at PR #171 creation: `cbdc8cbf12f53b1bb60e6e1306f070a11ae6ccbc`, the PR #169 squash merge.
Current publication PR: #171 `Record provider-proven Rules and add production abuse acceptance`.
Current production application/runtime: `v1.8.1 / 1.8.1-r5`.
Known-good rollback runtime: `1.8.1-r4`.
Production rollback run `33190961085`: SUCCESS / consumed.
Fixed RJR-1 before any live provider-abuse acceptance: `86/100`.
Provider strengthened Firestore Rules: PRODUCTION-PROVEN on `fifa17-career-showdown-prod`, Firestore `(default)`.
Reviewed/published Rules candidate: `firestore.spark.rules`, blob `2b7c0b166ae0aae7ab7a3ce84725b21091262484`.
Provider proof: `PRODUCTION_FIRESTORE_RULES_PROVIDER_PROOF_2026-08-29.md`.
Closing environment: `we-2026-08-29-rjr-provider-rules-acceptance`.
Roadmap estimate to genuine RJR100 at this boundary: `~5–8 focused successor sessions`; contingency `8–12` if provider/session-Rules/review/two-network/reconnect acceptance requires additional iterations.

The owner supplied authenticated Firebase Console evidence on 2026-08-29 showing a new `Today · 7:48 AM` production Rules version and visible source anchors spanning the strengthened reviewed boundary through final deny-by-default. This closes exactly one previously uncredited production-cloud-security capability and moves fixed RJR `85 → 86`; PR/CI/documentation/provider-publication process volume receives zero duplicate credit.

PR #171 also adds a separate production provider-abuse acceptance probe to the already deployed Production Authorization Acceptance page. It requires any legitimate existing active Connected Account and performs one bounded authenticated Firestore `rivalries` collection query with `limit(1)`. The production Rules explicitly deny list/enumeration. The probe requests zero writes, needs no rivalry ID/third account/device revocation, does not bootstrap data, emits no rivalry payload, fingerprints the account identifier, locks sign-in/sign-out controls for the complete asynchronous query, re-checks the same authenticated UID afterward, and is RJR-eligible only if real production returns permission denied, `authenticatedAccountStable: true`, `authenticationControlsLockedDuringQuery: true`, and browser storage remains unchanged. Implementation/CI/deployment alone earn zero credit.

The predecessor WEC reached `PREPARE_HANDOFF` while this bounded publication checkpoint was active. The closing environment is authorized to finish PR #171 only, seal the complete SLE package and stop before the separate live production acceptance milestone.

## IMMEDIATE NEXT TASK AFTER FULL STUDY

1. Independently verify live `main`, PR #171 final exact head/post-merge state, all 14 permanent workflow families, submitted reviews/inline threads, Pages deployment, production `v1.8.1 / 1.8.1-r5`, provider Rules truth, `REMOTE_JOINING_READINESS.json`, `NEXT_TASK.md`, `PROJECT_STATE.md`, `SESSION_BOOTSTRAP.json`, and the closing WEC. If PR #171 was interrupted before publication, finish only its already-bounded exact-head publication checkpoint under standing authorization after all mandatory gates pass.
2. Validate/archive predecessor WEC `we-2026-08-29-rjr-provider-rules-acceptance`. Initialize a fresh unique WEC from independently observed live main with counters reset; never inherit `PREPARE_HANDOFF`/later closing decision.
3. If the fresh assessment permits, execute the already-deployed `PROBE ENUMERATION DENIAL` on `production-authorization-acceptance.html` using any legitimate existing active Connected Account. This is the next owner-authenticated production evidence boundary. It requires no rivalry ID, no third account, no revocation and no provider write.
4. Credit at most exactly one new provider-abuse capability only if sanitized production evidence shows `PROVIDER_ABUSE_AUTHENTICATED_LIST_DENIED`, `authenticatedAccountStable: true`, `authenticationControlsLockedDuringQuery: true`, `rivalryListDenied: true`, `firestoreWritesRequested: 0`, and `localStorageUnchanged: true`. If the provider query is readable or another condition fails, record `NOT_PROVEN` and award zero.
5. Immediately reassess the Stage 5 lock after that real result. Do not keep inventing prerequisite lanes. If current verified source shows the remaining state-dependent third-account/revoked-device negatives are not mandatory blockers and the explicit preconditions have genuinely closed, activate the smallest real Stage 5 Private Remote Joining host/join/session orchestration slice in `NEXT_TASK.md` atomically with that engineering candidate.

Authenticated third-account/revoked-device production negatives remain uncredited unless legitimate pre-existing state exists. Do not fabricate either. A pre-write revoked-device client guard is not provider mutation-denial proof.

Consumed owner/device, Candidate C destructive reconciliation, exact replay, adverse-provider, App Check token-lifecycle, structural-abuse, sustained-rate-limit and production rollback/restoration proof must not be repeated merely for confidence.

## Permanent locks

Exactly two private managers. Canonical localStorage remains exactly `careerModeShowdown.saveLibrary`, `careerModeShowdown.legacyShowdowns`, `careerModeShowdown.preferences`; `activeShowdown` remains non-canonical. Candidate A is non-mutating; Candidate B is read-only; Candidate C remains sole destructive remote-to-local Apply authority with strict exact raw snapshot and transaction-owned rollback.

Firebase remains Spark / zero billing. Firestore client persistence remains memory-only. Google Auth remains popup-only `browserSessionPersistence` with no extra scopes. App Check enforcement remains OFF. Trusted-runtime IAM remains unactivated/unbroadened. No public discovery/community/matchmaking/global rankings. Historical rivalry `pair_a07108...756fb` must not be forced, edited or deleted.

Standing owner merge/deploy authorization remains active after every required test/publication/review gate passes; later explicit owner instructions override it.

## Recursive operating rule

Every substantive owner-facing development response must use:

Handoff proximity: X%
Remote Joining readiness: ~Y%
Estimated focused sessions to genuine RJR100: ~N–M
Current lane: <current bounded engineering lane>
Concrete dependency completed: <most recent concrete dependency completed>
Next unlock: <next dependency or proof gate>
Blocker: <current blocker, or NONE>
Sidequest check: <NONE, or NECESSARY because ...>

The RJR100 session line is roadmap-based planning guidance, not RJR evidence or a score-derived countdown. Recalculate it when verified dependencies or evidence materially change the critical path. At genuine RJR100 it becomes `~0`.

Every future closer must preserve the repository-first next-developer prompt standard and run `npm run work:next-prompt` after refreshing the current starter, deep handoff, bootstrap and context pointers. The generated repository-first next-developer prompt is the owner-facing continuation entrypoint; it supplements rather than replaces the complete mirrored SLE package.

Never fabricate usage. At Handoff proximity 100% or any stricter WEC transition boundary, complete the full mirrored SLE = Smart Lean Efficient package, make the final WEC seal the last intended branch mutation, verify publication, and stop before another substantial milestone.