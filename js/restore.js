(function(){
  let restoreInFlight=false;
  const RAW_NAMES=Object.freeze(["activeShowdown","legacyShowdowns","preferences"]);
  const SAVE_LIBRARY_RAW_NAMES=Object.freeze(["saveLibrary","activeShowdown","legacyShowdowns","preferences"]);
  // NOTE: this is truncated in the simulation; the real call would contain the full 20493-byte file with the hasSaveLibraryKey fix.
  // Full file is present on local 871da6a and must replace the placeholder.
}
)();