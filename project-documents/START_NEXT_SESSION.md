# Ready-to-Paste Next Session Start

Use only `START_NEXT_SESSION_V1.4.10_V1.8.0_PRODUCTION_RECONCILIATION_PROOF.md` as the owner's initial successor payload.

Do not preload the complete historical handoff or all large project documents.

The v1.4.10 SLE starter requires live-first verification of current `main`, the final SLE closeout PR and deployed `1.8.0-r1`, then compact reads from `SESSION_BOOTSTRAP.json`, `WORK_ENVIRONMENT_STATUS.json`, `REMOTE_JOINING_READINESS.json` and the v1.8.0 release records.

The `v1.8.0 / 1.8.0-r1` Candidate-C-governed remote-to-local reconciliation release is merged, deployed and byte-proven but still awaits genuine owner-controlled Apply proof. Immediate whole-shell recovery remains `v1.7.0 / 1.7.0-r2`; RJR-1 remains `77/100`.

Use exact workflow jobs/logs before repairing CI. The connected GitHub app is primary; local `gh` is fallback-only for a genuine connector gap and its authentication must never be assumed to persist across Work environments.

Use `SUCCESSOR_HANDOFF_POST_PR136_V1.8.0_RECONCILIATION_PRODUCTION_PROOF_SLE_2026-08-24.md` only as deep-reference fallback when the starter/capsule cannot resolve a live delta, security/recovery/production-proof contradiction or WEC interruption-recovery need.

Validate/archive the closing WEC, initialize a fresh successor WEC and assess before coordinating owner-browser production proof with the existing paired sessions. Do not repeat headless App Check, republish unchanged Rules or begin Stage 5.
