# START NEXT SESSION

Use only `START_NEXT_SESSION_V1.4.18_R3_FIREAUTH_PRODUCTION_PROOF.md` as the normal owner entrypoint. SLE = Smart Lean Efficient. The deep fallback is `SUCCESSOR_HANDOFF_POST_PR147_R3_FIREAUTH_PRODUCTION_PROOF_SLE_2026-08-25.md` and should be loaded only when the compact starter plus live GitHub delta cannot resolve the task.

Current production runtime is `v1.8.1 / 1.8.1-r3`. Runtime release merge is `857727586d548a96fca3ad63c394bf8f0b9e3b90`; current verified main at predecessor handoff is `449c5d07f79ede8814eab2f6cbbe6656f10b973b` after PR #147's proof-only correction. Official Remote Joining readiness remains `76/100` under RJR-1 until ordinary owner production evidence restores the two account capabilities invalidated by the r2 regression.

IMMEDIATE NEXT TASK AFTER FULL STUDY: initialize a fresh successor WEC, then investigate exact production Stability job `97928827439` where the r3 App Check boundary now passes but the Settings install audit stops on `fireauth is not defined`. Capture stack/source provenance before changing behavior. Keep every first-party pageerror fatal; classify only an exactly proven external Firebase Auth iframe/provider error narrowly, or fix product runtime if the error is first-party/behavioral. Require all applicable tests and publication gates before merge/deploy, then require the complete downstream deployed-site smoke before asking the owner to test.

After technical production proof, owner acceptance is Connected Account available → Google popup sign-in → `Private account ready`. After that, resume the pre-regression original-rivalry test: Nik copies the complete saved original Pair ID, Gop pastes that exact full ID and attaches / `VERIFY / REATTACH` exactly once, then refreshes shared state and observes the same original rivalry at the expected revision. Do not create a replacement pairing, Publish, Preview or Candidate C Apply. The separate all-zero unavailable-code fixture remains unconsumed until this recovery is stable.

App Check enforcement remains OFF. Firebase remains Spark / zero billing. Firestore remains memory-only. Stage 5 remains locked.

Owner-facing checkpoint template remains:
Handoff proximity: X%
Remote Joining readiness: ~Y%
Current lane: ...
Concrete dependency completed: ...
Next unlock: ...
Blocker: ...
Sidequest check: ...
