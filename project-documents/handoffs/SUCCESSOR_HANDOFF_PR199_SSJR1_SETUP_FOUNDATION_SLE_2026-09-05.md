# Complete successor handoff — PR #199 SSJR-1 setup foundation

SLE = Smart Lean Efficient. Recorded 2026-09-05T22:10:22Z. This is the complete deep-reference envelope, not the normal owner entrypoint. Use `START_NEXT_SESSION_V1.4.46_PR199_SSJR1_SETUP_FOUNDATION.md` and the freshly generated repository-first next-developer prompt first. Root/project mirrors are byte-identical. Treat every recorded boundary as orientation only; current source and live evidence win.

## Repository and publication boundary

Repository `nikahanghojjati-oss/fifa17-career-showdown2`; public site https://nikahanghojjati-oss.github.io/fifa17-career-showdown2/ . PR #199 is the current foundation candidate on `ssjr/authoritative-setup-foundation-2026-09-05`, based on verified PR198 main `39ffe88d61dcda973df03a18e0266fcfe4cf5638`. Initial remote head `18e08bb96b7a6da7f9cb845aef03c79e8cd91f8e` and tree `3c206c63d780fbff415c884d683ae77caa633019` exactly match the locally staged tested tree. The final sealed head is intentionally not self-referenced. Fetch PR199 live, including exact head/tree, all reviews/threads, workflow runs and merge/post-merge evidence. At this source seal the final gate is pending; the closing developer may finish it without another source mutation and publish a factual closure comment. A successor must consume that live outcome before deciding publication remains undone.

Standing authority `00_OWNER_STANDING_MERGE_DEPLOY_AUTHORIZATION.md` covers every current/future PR through project completion after all gates. Require every current permanent workflow family green on the same exact reviewed PR head, no unresolved review blockers, mergeability, expected-head protected squash merge and complete post-merge evidence. The initial head has 15 workflows scheduled; it is not substitute proof for a later seal. After merge inspect all 15 main-push workflow families, Pages, Stability including deployed bytes/App Check/full journey and the release integration burn-in. Run `npm run verify:deployment` against the merged source. Never use the last good runtime's proof as proof of a changed candidate.

Production v1.9.1 / 1.9.1-r2 remains loaded; previous whole-shell rollback 1.9.1-r1. Latest runtime PR194 merge `11bb681527a9b78884baf0c384350c90493dc9bd` remains historical runtime provenance. No existing runtime JS, shell, service worker, CSS, data, workflow YAML, Rules, provider, IAM or billing changes are included. The new setup module is dormant. Newly served candidate source is not active gameplay.

## Fixed readiness and owner instruction

SSJR-1 is a separate fixed 100-point model with 20 whole capabilities, domain weights 10/25/20/20/15/5/5, and one explicit baseline of 0/100 at PR198 main. Definition: `SHARED_SHOWDOWN_JOURNEY_MODEL.json`. Ledger: `SHARED_SHOWDOWN_JOURNEY_READINESS.json`. Canonical model SHA-256: `246aa4572600bc00e14b505e0de15308373fdb57575015a60092d84935697f4e`.

RJR-1 remains COMPLETE/FROZEN at 100/100. Its accepted Chromebook/Home WiFi plus iPhone/cellular run used two physical devices and independent networks; its final stable acceptance is consumed. Do not repeat or re-credit without a proven regression. PR, CI, review, merge, deployment, WEC, SLE/SNS, documentation and candidate tests earn zero readiness points. SSJR0 does not erase RJR100: the prerequisite transport is complete, but no whole production Shared Showdown capability has its required evidence yet. `npm run ssjr:assess` reconciles fixed weights, evidence layers, dependencies and regression cascades.

The initial planning estimate is ~8–14 focused sessions to genuine SSJR100, based on provider/Rules, paired-first setup UI, transfer/results, scoring/progression, recovery/closure and final acceptance stages. It is a forecast, never score arithmetic. Report changes with concrete evidence.

The later owner instruction is canonical in `authority-history/OWNER_SSJR_REPORTING_AND_PAIRING_ORDER_2026-09-05.md` and the current override in AGENTS.md. Replace the estimate label with `Estimated focused sessions to genuine SSJR100`. The requested final product must support two remote managers and require manager pairing plus ACTIVE before league or club selection. Necessary implementation changes are authorized. Automate every feasible test before asking for unavoidable physical/account actions. No present owner action is required.

## Completed foundation and limits

`js/sharedShowdownSetup.js` exposes `CareerModeSharedShowdownSetup`/CommonJS `createProtocol`, apply, verifyState, prepareDraw and confirmationHash. It is candidate-provider-neutral, productionEnabled false, canonicalStorageMutation false and billingRequired false. The caller-supplied authority object is a simulation contract, never authentication. The future provider adapter must derive and validate authority inside the same transaction that commits setup.

Authority requires an active exact Connected Rivalry with two distinct legitimate account slots, bound profile/Save per role, active account/entitlement and actor device, and the exact same unexpired ACTIVE two-member private session. Setup hashes stable rivalry/manager/profile/Save binding, not the short-lived session capability. Session defaults are 15 minutes, maximum 30: a new same-pair ACTIVE session must resume the existing setup; a new session host cannot steal the original coordinator or reset confirmed setup. No raw IDs or capability are persisted by the candidate state or evidence.

The opening session host fixes coordinatorRole. Only that role commits one League Wheel result, distinct same-league clubs and a valid total length (1/3/5/10). Both roles independently acknowledge an identical immutable configuration hash; either may confirm first. Two confirmations produce SHOWDOWN_CONFIRMED. There is no reroll, reset or silent rebase. Every command has a bounded operation ID and baseRevision; identical accepted retries return current state with original accepted revision, while altered reuse and stale competing commands are rejected. At most six accepted mutations/receipts exist. Schema, phase, receipt order, catalog, binding and content checks fail closed.

Inputs/catalog are copied and frozen before async digest work. Random preparation uses secure bounded rejection sampling and two clubs without replacement, done once before retry callbacks. The provider must validate each transition and pin the canonical catalog; a hash is not authorization. This does not promise trustless fairness against a modified coordinator choosing a different valid random outcome. Joint entropy would require a separate product decision. Do not weaken concurrency protection or mislabel secure client randomness as server-verifiable fairness.

The app's existing shared transport and whole-Save publication do not implement these new shared lifecycle states. Do not piggyback authoritative setup on unrestricted whole-Save publication. The candidate performs no remote-to-local Apply; preserve Candidate C as the sole destructive authority. The shared UI/local pre-draw Save shell, authoritative transfer/results, scoring/progression and final closure are still unimplemented. SSJR stays 0/100.

## Validation and correction history

Local validation before the SLE seal: `npm run test:ssjr` PASS, `npm run test:ssjr:browser` PASS, and the complete `npm run test:contracts` PASS (89 suite files after explicit static release, plus three post-suite acceptance contracts). The setup contract exercises 65 accept/reject cases, including exact manager/profile/Save/session authority, all four lengths, stale commits, duplicate operation replay, conflicting concurrent commits, confirmation tampering, terminal setup, later same-pair sessions, frozen snapshots and async mutation. The browser audit uses two isolated Chromium contexts and real WebCrypto; both converge on identical setup while all three canonical local bytes remain exact and no page errors occur. These are local candidate contexts, not production UI, a live provider, two physical devices or independent networks. The audit is appended to the existing browser suite and both contracts to the permanent suite.

Independently verified predecessor evidence: PR #198 exact head `165b21a1e9a269fae87efa06ebd1df89cfc48e04`, merge `39ffe88d61dcda973df03a18e0266fcfe4cf5638`, 15/15 PR and 15/15 main-push families, no review threads, Stability `33990982219`, Burn-In `33990982204`, closure comment `5554721311`. Direct deployment verification matched all 100 original runtime files byte for byte at 1.9.1-r2. The dormant new module will add one served JS file after Pages publication without being loaded by the app. Actual provider/billing control-plane configuration was not separately queried or mutated in this environment; do not relabel source/prior proof as a fresh control-plane observation.

The full suite initially found generic helper names `fail`/`canonical` colliding with the namespace audit; renaming to `ssjFail`/`ssjCanonical` fixed it without weakening the gate. Current-authority contracts contained stale PR198/current-task assertions and exact required routing phrases. Only current-task assertions were updated; all historical RJR proof, security/recovery and source routing requirements remain. Preserve historical starters/handoffs v1.4.37/v1.4.40/v1.4.43 byte-stable; never edit them into current evidence. Old graph/model current pointers were refreshed for SSJR because they incorrectly still directed work toward completed PR196 physical acceptance; historical facts remain separately retained.

One context compaction occurred. Tool routing corrections: unsupported connector tags collection (git works), safe-open web failure (direct deployment verifier works), repeated guessed local helper paths (use `rg --files`), connector payload shape (`structuredContent` is the returned payload, not a nested result), and oversized shell JSON output (read bounded chunks internally and compare resulting Git tree SHA). No credentials were copied or requested. GitHub CLI 2.100.0 was checksum verified and installed but unauthenticated; connector-first reads/writes cover publication. Re-run the repository bootstrap in a fresh environment as tools/auth may not survive.

## IMMEDIATE NEXT TASK AFTER FULL STUDY

Independently verify PR #199 final exact head/tree/state, all 15 workflow families, reviews/threads, merge and post-merge evidence. If publication is unfinished, complete that exact-head gate first. Once publication is proven, immediately implement the Spark-compatible exact-path Shared Setup transaction adapter and candidate Firestore Rules, with direct modified-client emulator negatives. Derive authority from protected provider records in the same transaction; never trust caller-supplied JavaScript authority or a client-computed hash. Enforce paired managers and an ACTIVE session before any league or club commitment. Update NEXT_TASK.md in that substantive implementation candidate, with no separate continuity-only PR.

Ordered implementation gates after completed PR199 publication:

1. Fresh successor WEC and source study: hydrate the setup module, fixed model/ledger, runtime account/entitlement/device/Connected Rivalry/private-session contracts, `data/clubs.js`, provider Rules and existing emulator tests. Read the baseline study for exact source mappings. Do not redo accepted RJR physical proof.
2. Design exact-path authoritative setup document/adapter on Spark. Bind immutable manager/profile/Save slots and original coordinator; validate current actor/device/entitlement, exact rivalry and ACTIVE session on every read/mutation. Separate stable journey identity from expiring transport sessions. Transactions must preserve one outcome under contention; rejected stale intent must not auto-rebase. Prepare random commands once outside retried callbacks.
3. Enforce equivalent Rules semantics for direct modified clients. Reject unpaired/pre-ACTIVE/expired/closed/wrong-rivalry/third-account/revoked-device and forged role/catalog/hash/state transitions; no listing/discovery or permissive whole-document overwrite. Use direct emulator negatives, including bypass of JavaScript guards and concurrent write tests. Source hashes alone cannot provide security.
4. Only after provider enforcement is proven, implement the shared paired-first entry screens. A local pre-draw profile/Save identity shell may support binding; league/clubs cannot be required first. Both managers must see the same committed setup, confirmations and useful reconnect/conflict recovery. Preserve existing local-only behavior and Candidate C authority. Reuse existing zero-billing Rules publication and runtime versioning gates for actual activation when needed.
5. Continue dependency-gated through local career starts, transfer challenge, per-manager results, authoritative season commit, canonical score/history and multi-season progression, same-pair recovery, final reconciliation and terminal completion/close without resurrection. Automate simulated browser/provider paths and production test-account proof; reserve genuine physical two-person acceptance for the final fully functional journey. Reconcile only whole fixed SSJR capabilities with required evidence; invalidate dependent credit on a proven regression.

Do not begin the distinct provider milestone in the closing environment. Fresh work should update current task authority in the same substantive implementation PR, without an unrelated docs-only prerequisite. The initial ~8–14-session estimate is not a promise or an authorization to stop at a partial game.

## Security, recovery and control-plane locks

Billing must never be activated. Firebase remains Spark. Never link Cloud Billing, enable Blaze, add a payment method, use purchased credits, Cloud Run, Cloud Functions or another billing-required service. App Check enforcement remains OFF. Firestore browser persistence remains memory-only. Google Auth remains popup-only browserSessionPersistence with no extra scopes. Exactly two private managers; no public discovery/listing/lobby/matchmaking/community/rankings/global leaderboards. Never request, expose, quote, paste, log or durably retain raw private capabilities or raw account/device/rivalry/session authority IDs. Never destructively test the protected historical rivalry.

Canonical localStorage remains exactly `careerModeShowdown.saveLibrary`, `careerModeShowdown.legacyShowdowns`, `careerModeShowdown.preferences`. Candidate A is non-mutating export, Candidate B is read-only analysis, and Candidate C is the sole destructive remote-to-local gameplay Apply authority with transaction-owned strict exact raw-snapshot backup/rollback. Preserve the Installable Offline App, Recovery & Device Resilience, Local Profiles and Save Library.

Permanent control-plane authority: `00_FIREBASE_PERMANENT_ZERO_BILLING_CONTROL_PLANE.md` and `HANDOFF_FIREBASE_CONTROL_PLANE_PERMANENT_ACCESS_ADDENDUM_2026-09-01.md`. The only authorized production Firestore Rules publication route is `.github/workflows/deploy-firestore-rules-zero-billing.yml` using its existing protected service account secret after applicable gates. Do not activate Rules, IAM or billing for this candidate. Do not ask the owner to redo completed access/bootstrap/production RJR acceptance. Sensitive observations must use established sanitized proof outputs; never log private values while testing rejects.

## Closing WEC, recursion and owner delivery

Closing environment `we-2026-09-05-ssjr-setup-foundation-28cf84`, entered at `39ffe88d61dcda973df03a18e0266fcfe4cf5638`, has observed context compaction and accumulated implementation/publication evidence. The final status and byte-identical own archive preserve actual counters and decision. Before modification a successor must run `npm run work:continuity:validate`, inspect final live closure, archive facts append-only, initialize a fresh unique WEC with reset signals and startingMainSha, then run `npm run work:assess`. Never inherit the predecessor transition decision. Unknown usage remains null/unavailable and is never inferred from file/message counts.

At every substantive checkpoint show:

```text
Handoff proximity: X%
Shared Showdown Journey readiness: Y/100
Estimated focused sessions to genuine SSJR100: ~N–M
Current lane: ...
Concrete dependency completed: ...
Next unlock: ...
Blocker: ...
Sidequest check: NONE
```

Explain which concrete dependency the action removes or which fixed capability its evidence proves. SSJR100 requires a production-proven supported playable journey for both remote managers. Handoff proximity is separate from product readiness and may remain unchanged. At 100%, HANDOFF_AT_CHECKPOINT, HANDOFF_NOW or equivalent final boundary, SLE = Smart Lean Efficient requires the full mirrored handoff, new versioned mirrored starter, current capsule/context pointers, archived/finalized WEC, exact successor task, normal contract/publication gates and a clean stop before another substantial milestone. Final WEC seal is the last intended branch mutation; any later fix requires resealing and new exact-head gates.

This is recursive and permanent. Every successor must generate a fresh repository-first next-developer prompt from `npm run work:next-prompt`, identifying its current starter, live verification, fresh WEC and IMMEDIATE NEXT TASK AFTER FULL STUDY. Give the owner that short prompt and keep the compact starter available as fallback. Do not surface this full handoff by default or burden the owner with multiple continuity files. Current source and live evidence remain authoritative.
