# SUCCESSOR HANDOFF — PLAYER TWO ZERO PROOF → STAGE 4 OWNER RECONCILIATION

SLE = Smart Lean Efficient. This rule is mandatory for this successor and every later successor. A plain chat prompt is not a complete project handoff.

## Live-first rule

Repository: `nikahanghojjati-oss/fifa17-career-showdown2`

Public site: `https://nikahanghojjati-oss.github.io/fifa17-career-showdown2/`

At predecessor seal, independently verified live `main` is `beab9f31cb7f31bf4938f5b0df67394899ef12a0`. Production application/runtime is `v1.8.1 / 1.8.1-r3`. PR #151 is merged and the post-merge Stability production journey is successful. Treat every identifier in this handoff as orientation until independently revalidated against live GitHub, current source, current CI and the deployed site.

Standing owner authorization remains in force: after all required tests and publication gates pass, merge and deploy without repeatedly asking the owner for approval. A later explicit owner instruction may narrow or revoke that authorization.

## Current fixed readiness authority

Remote Joining readiness authority is `REMOTE_JOINING_READINESS.json`, model `RJR-1`, fixed denominator 100.

Current Remote Joining readiness: `78/100`.

The r2 Connected Account regression temporarily invalidated two previously earned capabilities, reducing 78 → 76. Owner ordinary iPhone Safari production evidence on repaired r3 then re-proved the normal Google-authenticated identity path and strict self-account bootstrap (`Private account ready`), restoring exactly those two credits to 78. The fresh-pair and Player Two unavailable-code proof below earns zero duplicate RJR credit.

Do not move RJR for PR count, green CI alone, WEC/SLE work, screenshots themselves, documentation, or repeated proof of an already-credited capability.

## Closed evidence in this environment

### 1. Connected Account recovery

Owner iPhone Safari production evidence proves both the normal signed-out `SIGN IN WITH GOOGLE` state and the authenticated `Private account ready` state on repaired production r3. This closes the temporary r2 account regression gate.

### 2. Historical rivalry recovery conclusion

The historical original Nik/Gop Firestore rivalry still exists and is active, recognition fingerprint `pair_a07108...756fb`. The original local `profileId` / `saveId` identities were deleted when local browser data was wiped. Current production correctly refuses to attach replacement local identities to that historical rivalry and explicitly leaves local saves unchanged. Do not force, edit, delete or rewrite that historical rivalry merely to satisfy a proof script.

### 3. Fresh current-identity positive proof

The owner created one fresh private pairing using the current stable Player One / Nik identity in normal Chromebook Chrome and joined it from Player Two / Gop in Incognito Chrome. Both isolated browser-storage contexts attach to the same fresh rivalry, privacy-safe recognition fingerprint `pair_8f24ae...3528c`.

Before publication both sides correctly observed no authoritative shared state. Player One published exactly once. The production UI reported revision 0 and that the Local Save Library remained unchanged. Player One refreshed revision 0. Player Two refreshed the same rivalry and visibly observed `REMOTE OBSERVED: Revision 0` with `LOCAL COMMIT: Not applied this session` and no local Save overwrite.

This re-proves already-credited positive pairing/Connected-Rivalry capabilities using the owner's current replacement local identities. It does not add RJR points.

### 4. One-shot Player Two unavailable-code regression

After the fresh rivalry was stable at revision 0, Player Two / Gop submitted the authorized non-secret fixture `pair_` plus 64 zero hex digits exactly once through the Private Pairing JOIN field.

The deployed UI returned bounded non-enumerating unavailable/expired/used guidance, did not expose raw permission-denied text, explicitly said local saves were not changed, and preserved:

- `PLAYER TWO · GOP` selection;
- the same fresh Connected Rivalry pointer;
- `REMOTE OBSERVED: Revision 0`;
- Player Two's local target;
- `LOCAL COMMIT: Not applied this session`.

The one-shot zero fixture is consumed for owner evidence. Do not repeat it merely for duplication. Canonical evidence record: `OWNER_PRODUCTION_PLAYER_TWO_UNAVAILABLE_CODE_PROOF_2026-08-25.md`.

## IMMEDIATE NEXT TASK AFTER FULL STUDY

Resume the distinct Stage 4 owner remote-to-local reconciliation proof on the fresh current-identity rivalry. This is the next real product milestone and is intentionally separate from pairing and the zero-code UX proof.

Before asking the owner to perform any destructive Apply, independently study current live source plus at least:

- `START_NEXT_SESSION_V1.4.10_V1.8.0_PRODUCTION_RECONCILIATION_PROOF.md`
- `START_NEXT_SESSION_V1.4.8_STAGE4_RECONCILIATION.md`
- `PRODUCTION_STAGE4_CONNECTED_RIVALRY_PROOF_2026-08-24.md`
- `CAREER_MODE_SHOWDOWN_CANDIDATE_C_IMPLEMENTATION_LOG_2026-08-12.md`
- `SAVE_LIBRARY_AUTHORITY_RECONCILIATION_ACTIVE_HANDOFF.md`
- `js/sparkConnectedRivalry.js`
- the canonical Save Library / Candidate C recovery authority

Then derive the smallest safe owner sequence that proves the existing Stage 4 reconciliation behavior rather than inventing a new workflow.

Required proof characteristics are:

1. reverify the same fresh current-identity rivalry and exact observed remote authority before reconciliation;
2. `PREVIEW REMOTE → LOCAL` is non-mutating and does not alter canonical local storage;
3. preview intent is bound to exact observed remote revision/hash and exact local target identity;
4. if remote authority changes after preview, the stale intent is rejected rather than silently rebased;
5. the actual local Apply requires exact confirmation and a verified exact backup;
6. Candidate C remains the sole destructive local Apply authority;
7. Apply preserves stable manager identity and unrelated Saves;
8. successful Apply converges the intended local target to the exact remote authority;
9. local Apply must not itself mutate the remote authoritative document;
10. evidence clearly distinguishes observed remote state from committed local state and proves rollback/recovery ownership boundaries.

Do not use the historical deleted-local-identity rivalry for this proof. Do not repeat the zero-code fixture. Do not begin Stage 5 sessions until Stage 4 reconciliation and the remaining explicit Stage 5 prerequisites are production-proven.

If the current product fails one of these bounded requirements, stop on first failure, preserve evidence, diagnose the smallest genuine product defect, then repair through the normal exact-head test/review/merge/deploy process. Do not create a documentation or security sidequest.

## Critical locks

Canonical browser storage remains exactly:

- `careerModeShowdown.saveLibrary`
- `careerModeShowdown.legacyShowdowns`
- `careerModeShowdown.preferences`

`activeShowdown` is non-canonical.

Candidate A is non-mutating export. Candidate B is read-only analysis. Candidate C is the sole destructive import/apply authority.

Firestore browser persistence remains memory-only. Firebase remains Spark / zero billing. Google Auth remains popup-only with `browserSessionPersistence`. App Check enforcement remains OFF. Trusted-runtime IAM remains reviewed but not activated or broadened. Exactly two private managers remain required. Public discovery, community, matchmaking and global rankings remain prohibited.

Do not expose full account IDs, device IDs, identity hashes or full active rivalry IDs in repository evidence.

## WEC and successor discipline

The predecessor environment reached a new-milestone boundary after closing account recovery, fresh-current-identity Connected Rivalry continuity, revision-0 cross-manager observation and the one-shot Player Two unavailable-code regression. Therefore the predecessor must seal rather than begin the distinct destructive reconciliation proof.

Initialize a fresh successor WEC only after independently validating this predecessor seal. Do not inherit the predecessor's transition decision as your own. Usage remains unavailable unless an approved current-session value is explicitly supplied; never fabricate it.

At Handoff proximity 100%, repeat the full SLE = Smart Lean Efficient mirrored packaging protocol and stop before the next substantial milestone.

## Required owner-facing checkpoint format

Handoff proximity: X%
Remote Joining readiness: ~Y%
Current lane: ...
Concrete dependency completed: ...
Next unlock: ...
Blocker: ...
Sidequest check: ...
