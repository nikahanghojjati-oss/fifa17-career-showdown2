# SUCCESSOR HANDOFF — POST PR #158 ADVERSE-PROVIDER SAFETY PROVEN / RJR 81 / WEC 100

SLE = Smart Lean Efficient.

Repository: `nikahanghojjati-oss/fifa17-career-showdown2`
Public site: `https://nikahanghojjati-oss.github.io/fifa17-career-showdown2/`
Closing environment: `we-2026-08-26-private-remote-joining-session-boundary-5c7a`

This handoff is orientation only. The successor must independently fetch current source, live GitHub, provider state and deployed evidence. Those sources plus later owner instructions override any stale recorded statement here. The successor must validate the inherited closing WEC, archive its final facts, create a fresh unique WEC with reset counters, and only then assess its own environment. Never inherit this predecessor's `HANDOFF_NOW` decision as the successor's starting decision.

## 1. Exact verified live boundary

Current live `main` is `5eecb482e94263fce7ce9041cdc4f3c7f3a86d21`, the squash merge of PR #158 `Prove Stage 4 adverse-network local-save safety`.

PR #158 final exact head was `72435c180deb744418f93fe8155f280973c3b76a`. All 14 permanent pull-request workflow families succeeded on that unchanged head. The PR had one automated Codex COMMENTED review finding: the initial source assertion for publish-error recovery messaging was too greedy and could match a later handler. The assertion was corrected to slice the actual refresh/publish handler boundaries, the reply recorded the correction, and the single thread was resolved/outdated. There were zero unresolved review threads before merge and the PR was mergeable.

PR #158 was squash-merged by expected-head guard as `5eecb482e94263fce7ce9041cdc4f3c7f3a86d21`. Exactly 15 post-merge push/deployment runs were created and all 15 completed successfully. GitHub Pages run `33019945003` succeeded. Stability run `33019945012` succeeded through repository contracts, Chromium Stability and deployed-site smoke. Deployed smoke verified runtime bytes, runtime error provenance, production App Check token path, Home, Save Library, manager identity linkage, identity-safe analytics, football photo crops, Candidate A, Candidate B, Candidate C restore/recovery, install/offline boundary and the complete deployed journey. Zero post-merge failures were observed.

Production remains application `v1.8.1`, runtime `1.8.1-r3`. Runtime release lineage remains PR #151 merge `beab9f31cb7f31bf4938f5b0df67394899ef12a0`. PR #158 changed no runtime bytes, Firestore Security Rules, Firebase provider configuration, billing, canonical local storage, Google Auth policy, App Check enforcement or trusted-runtime IAM.

## 2. Remote Joining readiness authority

Fixed RJR-1 is live at `81/100` from `REMOTE_JOINING_READINESS.json`.

Current domains are:
- deterministic-sync-recovery: 20/20
- identity-auth-trust: 18/20
- production-cloud-security: 18/20
- devices-pairing-connected-rivalry-remote-join: 20/30
- real-device-hardening-release: 5/10

The evidence chain is conservative: repaired r3 restored exactly the two r2-invalidated account credits to 78; production-proven Stage 4 remote-to-local reconciliation added exactly +1 to 79; exact accepted-result idempotency replay added exactly +1 to 80; deterministic adverse-provider failure safety adds exactly +1 to 81. Documentation, PR count, CI volume, repeated subassertions and already-consumed owner/device proof never earn points.

Do not credit two-physical-network behavior, token lifecycle, authenticated third-account/revoked registered-device production negatives, actual Remote Joining sessions, abuse hardening or final connected release acceptance until those capabilities have their own valid evidence.

## 3. PR #158 capability now permanently closed

Permanent test: `tests/contracts/stage4-adverse-network-contracts.cjs`, wired into `tests/support/run-contract-suite.cjs`.

It executes the real `js/sparkConnectedRivalry.js` boundary. Attach/read provider unavailability returns bounded provider errors. The stronger publish case passes legitimate active registered-device and exactly-two-owner rivalry preflight, builds the immutable local projection, then experiences provider failure before remote mutation commit. The canonical local Save Library fixture remains byte-for-byte unchanged. Retry/error UI preserves local-first recovery messaging. This proves deterministic provider-loss safety without pretending it is a real two-network field test.

## 4. PR #157 production-negative audit remains closed

PR #157 exact head `2e482dfdf8e6a313fb47e890e27761801fa94746` merged as `ac1a2d7cb30d35e82609b67efa9c8bb820ee9883`, after 14/14 exact-head workflow families and 15/15 post-merge runs were green.

Its purpose was to determine whether GitHub execution could validly production-prove an authenticated third-account denial and/or revoked registered-device denial without asking the owner to recreate state. Two attempted synthetic production probes are explicitly non-evidence. The first failed before Firebase because of a Node module-form error. The second obtained HTTP 403 from an unauthenticated synthetic request but also violated independent required payload and paired-parent predicates, so the rejection could not be attributed specifically to authentication. Both temporary artifacts were removed.

The correct result is dependency discovery, not proof: a real authenticated third-account negative needs a legitimate third Firebase identity; a revoked-device negative needs authenticated owner identity plus a real registered device whose server-side revoked state participates. Do not fabricate substitutes and do not ask the owner to recreate already-consumed setup merely for duplication. Request only the minimum legitimate owner evidence if/when this production-only gate must finally close.

## 5. Important correction history from PR #158

The new product proof itself passed early. Moving live authority from RJR 80 to 81 exposed stale continuity/release assertions that still froze earlier checkpoint wording. Those were corrected without weakening product/security rules. Corrections preserved the original environment starting main, historical trusted shared mutation gateway marker, completed Stage 1 authority, superseded r3 recovery heading, superseded RJR 76 and 79 checkpoints, canonical `Remote Joining readiness` label, precise exact accepted-result replay terminology plus historical shorthand, exact r3 runtime lineage, blocked production-negative boundary, and the permanent 14-workflow-family provenance.

The one Codex test-quality finding was also fixed rather than ignored. These corrections explain the unusually long PR #158 commit history; the squash merge leaves a clean final diff. Do not reopen them as product bugs unless current live evidence shows an actual regression.

## 6. Owner/device/destructive proof already consumed — do not repeat

Do not repeat owner pairing, owner-device recovery, destructive Candidate C Apply, Stage 4 remote-to-local reconciliation, exact accepted-result replay, or deterministic adverse-provider proof merely for confidence or CI volume.

Do not use the consumed `pair_` plus 64-zero unavailable-code fixture merely for duplication. Do not force/edit/delete historical rivalry `pair_a07108...756fb`; its original local profile/save identities were deleted. Existing emulator coverage already includes registered third-account denial, unregistered/spoofed device denial, revoked-device publication denial, expired/deleted pair denial and direct remote-save write denial. Re-running those alone is not new production evidence.

## 7. Permanent authority locks

Exactly two private managers. Canonical browser storage remains exactly `careerModeShowdown.saveLibrary`, `careerModeShowdown.legacyShowdowns`, `careerModeShowdown.preferences`; `activeShowdown` remains non-canonical. Candidate A is non-mutating export, Candidate B read-only analysis, Candidate C the sole destructive explicit Apply authority with exact snapshot, stale/anti-clobber, rollback-ownership and recovery protections.

Firebase remains Spark / zero billing. Firestore persistent cache remains disabled/memory-only. Google Auth remains popup-only with `browserSessionPersistence` and no extra scopes. App Check enforcement remains OFF. Trusted-runtime IAM remains reviewed but unactivated/unbroadened; its exact locked permissions remain `firebaseauth.users.get`, `datastore.databases.get`, `datastore.entities.get`, `datastore.entities.create`. No public discovery, public community/profile surfaces, matchmaking or global rankings. Local-first startup/recovery must remain usable when Firebase is unavailable.

Standing owner merge/deploy authorization remains active through project completion: after all required tests, exact-head review and publication/deployment gates pass, merge/deploy without repeatedly asking the owner. Later explicit owner instructions may narrow or override this authority.

## 8. IMMEDIATE NEXT TASK AFTER FULL STUDY

Do not begin Stage 5 host/join/session orchestration yet. The next automatically provable explicit pre-Stage-5 capability is token-lifecycle hardening, but PR #159 publication is a mandatory predecessor gate if it remains open.

Ordered execution:
1. Independently verify live `main`, current PR/deployment state, `REMOTE_JOINING_READINESS.json`, `NEXT_TASK.md`, this starter/handoff, and the closing WEC.
2. Validate and archive the inherited WEC; initialize a fresh unique successor WEC with reset counters before assessing. Never inherit this predecessor's `HANDOFF_NOW` decision.
3. If PR #159 is still open and the fresh WEC permits continuation, first complete read-only validation of its immutable WEC-100 head, publish it under standing authorization only if the required gates remain clean, then verify post-merge `main` and deployment state. Do not begin token-lifecycle product work while PR #159 remains unpublished.
4. Read the deployed r3 lifecycle path, especially `js/productionFirebaseRuntime.js`, `js/sparkConnectedAccount.js`, `tests/contracts/production-app-check-runtime-contracts.cjs`, browser App Check audit coverage, and the Connected Rivalry safety boundary.
5. Characterize what is already proven: App Check auto-refresh is configured, initial token observation/expiry may be exposed, enforcement remains OFF, Auth uses popup-only session persistence, local-first recovery remains the fallback.
6. Select the smallest deterministic provider-compatible later expiry/refresh transition that is genuinely unproven. Prefer focused proof over a long production TTL wait or speculative scheduler.
7. Prove both successful and failed lifecycle transition handling cannot corrupt Connected Account state, Connected Rivalry state or canonical local saves. Preserve bounded recovery/error behavior.
8. Do not add persistent Firestore cache, extra Auth scopes, billing, broader IAM, new storage authority or App Check enforcement merely to make the test easier.
9. Award RJR only if a fixed-domain capability is genuinely closed. Keep two-physical-network behavior and authenticated production third-account/revoked-device negatives separately uncredited.
10. Reassess remaining explicit pre-Stage-5 gates after token-lifecycle proof. Stage 5 becomes the next real product milestone only when those gates are genuinely closed.

## 9. Closing WEC

Closing environment: `we-2026-08-26-private-remote-joining-session-boundary-5c7a`.
Starting independently verified main for that environment: `1af7d7243ba3849d3ec2f4bd236f1ba7e4c0efeb`.
The environment completed two major bounded phases: PR #157 production-negative dependency audit and PR #158 deterministic adverse-provider safety/publication. Usage remained unavailable; no percentage was invented or inherited. Final transition decision is `HANDOFF_NOW` for this predecessor only. The successor starts fresh.

## 10. SLE recursion and repository-first next-developer prompt

SLE = Smart Lean Efficient remains mandatory for every future handoff. This full handoff and its project mirror must remain byte-identical; likewise the compact starter and its mirror. Future closers must refresh `SESSION_BOOTSTRAP.json`, context pointers and WEC, use `npm run work:next-prompt` when available, generate a fresh repository-first next-developer prompt, and stop at Handoff proximity 100% before beginning another substantial milestone.

Repository-first next-developer prompt standard: tell the next developer to open the live repository, read the newest `START_NEXT_SESSION_...md`, independently verify current `main`/PRs/deployment/provider state and live authority files, treat handoff material as orientation only, initialize a fresh WEC, then execute `IMMEDIATE NEXT TASK AFTER FULL STUDY` without repeating consumed owner proof.

Owner-facing progress format:
Handoff proximity: X%
Remote Joining readiness: ~Y%
Current lane: ...
Concrete dependency completed: ...
Next unlock: ...
Blocker: ...
Sidequest check: ...
