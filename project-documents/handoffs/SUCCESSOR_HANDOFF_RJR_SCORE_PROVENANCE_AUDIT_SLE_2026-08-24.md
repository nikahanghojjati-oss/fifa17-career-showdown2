# FIFA 17 Career Mode Showdown — SLE Successor Handoff — RJR Score Provenance Audit

Recorded: 2026-08-24 ET
Repository: `nikahanghojjati-oss/fifa17-career-showdown2`
Public site: `https://nikahanghojjati-oss.github.io/fifa17-career-showdown2/`
SLE = Smart Lean Efficient.

## 1. Mission and current truth

Investigate, recalculate and explain the owner-reported RJR sequence `81 -> 77 -> 78` without guessing. The exact 81 source is not in current source or reachable Git history and was not recovered from available prior-context search. Preserve it as a real owner-reported discrepancy whose provenance is unresolved.

Official numerical authority remains fixed model `RJR-1` in `REMOTE_JOINING_READINESS.json`, currently `78/100`. This handoff does not authorize a numerical change. It authorizes an evidence/provenance audit, then directs the successor back to the queued deployed Player Two regression check.

## 2. Exact repository and production boundary

- PR #139 merged at expected-head squash/live main `9219879389b727fba1f9095aa2bf1a0299449222`; exact tree `320522bbf183199ad8175aec7d0904ef4b15aabe` equals the sealed branch tree.
- All 14 PR workflow families succeeded with clean reviews and zero unresolved threads. Pages run `32797673625` succeeded.
- Production runtime remains `v1.8.1 / 1.8.1-r1` from PR #138 runtime merge `ca0cb6ce8628c5f993669c08ff33e8f64c634870`. Independent verification matched all 89 public runtime files.
- Fourteen of fifteen PR #139 push workflows succeeded. Stability failed only at the already classified reCAPTCHA Enterprise `403 initial-throttle` after contracts, Chromium, deployed bytes and runtime provenance passed. No retry, provider mutation, debug provider, enforcement change or unchanged Rules publication is authorized.
- Production Firestore Rules remain exact blob `ecc8ccb2ab50f0f7057ab3170eb080ad9e36025f`.

## 3. Exact score evidence

### Owner-reported 81

The owner states that a prior work environment reported 81. Before creating the audit, searches of current `HEAD` and reachable Git history for exact `81/100`, `81 / 100`, RJR near 81 and Remote Joining readiness near 81 returned no match. Available prior-context retrieval also did not locate an exact 81 statement/calculation. This is a provenance gap, not evidence that the report did not occur.

The RJR-1 authority-history file says the fixed model was created after denominator drift between prerequisite completion and true end-to-end readiness and reconstructed the baseline near 58. This makes a pre-RJR, intuition-based or differently weighted 81 a plausible explanation, but it is not proven.

### Fixed-ledger 77

`REMOTE_JOINING_READINESS.json` records:

- 63 to 69 for production registered-device/private-pairing capability;
- 69 to 72 for exact entitled attach, revision-0 publication and cross-manager read;
- 72 to 73 for live stale-base rejection;
- 73 to 74 for bounded iPhone cross-surface hardening;
- 74 to 77 for safe stale recovery, monotonic revision-1 publication and cross-manager revision-1 convergence.

`PRODUCTION_STAGE4_CONNECTED_RIVALRY_PROOF_2026-08-24.md` independently records the same 69 to 72 to 74 to 77 capability sequence.

### Fixed-ledger 78

`REMOTE_JOINING_READINESS.json` event `production-stage4-two-physical-device-hardening-proof` and `PRODUCTION_TWO_PHYSICAL_DEVICE_CONNECTED_RIVALRY_PROOF_2026-08-24.md` both record exactly `77 -> 78`. The sole +1 credits fresh private pairing, exact manager attachment, revision-0/revision-1 publication, live stale rejection, recovery and convergence across a Chromebook and iPhone. Source, tests, CI, merge, deployment, screenshots and SLE earn zero points.

Current domain sum is `20 + 18 + 18 + 17 + 5 = 78`.

## 4. Why no definitive 81 to 77 explanation exists yet

RJR-1's decrease rule permits a reduction only when previously credited evidence is invalidated or a proven capability regression removes credit. No such 4-point invalidation is recorded in the fixed ledger. Its model-change rule requires a new version, explicit rationale and a historical backcast for any weight/denominator change.

Therefore the successor must not describe 81 to 77 as a legitimate capability loss, a known recalculation or an error until it finds the 81 artifact and compares like with like.

## 5. IMMEDIATE NEXT TASK AFTER FULL STUDY

Bootstrap and verify first:

1. Fetch live main, PR #139, relevant workflow evidence and public Pages. Current live evidence overrides this handoff.
2. Verify main `9219879389b727fba1f9095aa2bf1a0299449222`, production `1.8.1-r1`, fixed RJR 78 and unchanged Rules blob.
3. Validate/archive the inherited WEC, initialize a fresh unique successor WEC and assess it.

Then complete only the score-provenance audit:

1. Read `RJR_SCORE_PROVENANCE_AUDIT_81_77_78_2026-08-24.md`, `REMOTE_JOINING_READINESS.json`, `authority-history/REMOTE_JOINING_READINESS_MODEL_RJR1_2026-08-20.md` and the Stage 3/Stage 4/two-physical-device proof records.
2. Search the prior chat/export, available Library artifacts, older branches/tags/handoffs, issues and PR material for the exact 81 statement. Record the artifact identifier, timestamp, wording, model/version, denominator, weights and credited capabilities.
3. Backcast those exact inputs through RJR-1 and create a checkpoint comparison table.
4. If 81 was comparable RJR-1, identify the exact invalidated points or regression required for 77. If none exists, report that 77 conflicts with RJR-1's decrease rule and propose a tested ledger/reporting correction.
5. If 81 was a different or intuition-based measure, label it non-comparable, explain the denominator drift and show its RJR-1 backcast. Do not call the transition a capability decrease.
6. Recalculate all five domains and explain every delta. Modify the official ledger only after exact comparable evidence supports the correction and all RJR/SLE contracts pass.
7. Publish the evidence-backed explanation briefly for the owner.

After this mandatory correctness gate, continue real product work directly: run exactly one non-mutating Player Two / Gop syntactically valid but practically nonexistent-code Private Pairing regression on the existing signed-in sessions. Require selector persistence, privacy-safe guidance and no local/remote mutation. Then reassess before the separate Candidate C owner Apply proof. Do not repeat account, device, pairing or rivalry setup without concrete regression evidence.

## 6. Permanent locks

- Official RJR remains 78 until comparable evidence justifies a tested correction.
- Canonical storage is exactly `careerModeShowdown.saveLibrary`, `careerModeShowdown.legacyShowdowns` and `careerModeShowdown.preferences`; `activeShowdown` is non-canonical.
- Candidate A is non-mutating, Candidate B is read-only and Candidate C is the sole destructive local Apply authority with immutable exact intent, backup first, strict preconditions, transaction ownership, stale/anti-clobber rejection, ownership-scoped rollback and exact recovery verification.
- Exactly two stable private managers and direct private-rivalry authorization only.
- Firebase Spark / zero billing; no Functions or Storage initialization.
- App Check enforcement remains OFF. Firestore remains memory-only. Authentication remains popup-only `browserSessionPersistence` with no extra scopes.
- No public discovery, profiles, community, matchmaking, rankings or global leaderboard. Stage 5 remains locked.

## 7. WEC, publication authority and recursive SLE

Standing owner authorization permits merge/deploy only after all required tests and current publication gates pass. This correction is documentation/context only and must not alter runtime, Rules, workflows, provider state or the official RJR score without completed comparable evidence.

Every substantive owner-facing response must end exactly:

Handoff proximity: X%
Remote Joining readiness: ~Y%
Current lane: ...
Concrete dependency completed: ...
Next unlock: ...
Blocker: ...
Sidequest check: ...

At Handoff proximity 100%, or any WEC transition boundary, create the complete Smart Lean Efficient package with byte-identical root/project mirrors, refreshed capsule/context pointers, exact live/deployment/RJR evidence, explicit next task and the final WEC seal as the last branch mutation. Then stop before another substantial milestone. Every successor inherits this recursive rule unless the owner explicitly changes it.
