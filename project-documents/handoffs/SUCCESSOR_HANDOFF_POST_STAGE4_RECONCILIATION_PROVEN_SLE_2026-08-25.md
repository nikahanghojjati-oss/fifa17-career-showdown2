# SUCCESSOR HANDOFF — STAGE 4 RECONCILIATION PRODUCTION-PROVEN

SLE = Smart Lean Efficient. This rule is mandatory for this successor and every later successor. A plain chat-only prompt or one unmirrored handoff is not a complete project transition.

## Live-first authority

Repository: `nikahanghojjati-oss/fifa17-career-showdown2`

Public site: `https://nikahanghojjati-oss.github.io/fifa17-career-showdown2/`

At packaging start, independently verified live `main` was `beab9f31cb7f31bf4938f5b0df67394899ef12a0`, the squash merge of PR #151. Its exact reviewed head was `664a6ba0013d83d20ef88efba85e694a85f072c8`; all 14 permanent PR workflow families passed before publication and the post-merge production Stability/deployment journey succeeded.

Application/runtime remains `v1.8.1 / 1.8.1-r3`. This reconciliation proof package changes evidence/current authority only; it does not change runtime bytes, Firebase provider configuration, Firestore Rules, billing, App Check enforcement, canonical storage, authentication persistence/scopes, or Stage 5 runtime behavior.

Publication PR for this closeout package: PR #153, `agent/stage4-owner-reconciliation-proof` → `main`. The branch continues through mandatory mirrored SLE packaging and final WEC seal, so do not reuse an earlier intermediate head SHA as the final sealed head. The successor must independently fetch PR #153, its final head, workflows, reviews/threads, merge state and resulting live main. Current GitHub/source always wins over this orientation.

Standing owner authorization remains in force: after all required tests and current publication gates pass, merge and deploy without repeatedly asking the owner. A later explicit owner instruction overrides it.

## Fixed Remote Joining readiness

Authority: `REMOTE_JOINING_READINESS.json`, model `RJR-1`, fixed denominator 100.

Current evidence-backed score: `79/100`.

The score history relevant to this checkpoint is:

- 78 → 76: r2 production App Check throttle regression invalidated exactly two previously credited account capabilities: usable Google-authenticated identity and strict self-account bootstrap.
- 76 → 78: ordinary-owner iPhone Safari evidence on repaired r3 re-proved normal Google sign-in and `Private account ready`, restoring exactly those two withdrawn credits.
- 78 → 79: this environment production-proved the previously explicit uncredited remote-to-local reconciliation capability. Exactly +1 is awarded for that bounded capability. Preview, stale rejection, backup, convergence and screenshots are required subproofs and do not receive separate duplicate points.

Do not move RJR for PR count, green CI alone, WEC/SLE work, documentation, repeated proof, or visible action count.

## Production evidence closed in this environment

### 1. Fresh current-identity rivalry baseline

Player One / Nik in normal Chromebook Chrome and Player Two / Gop in Incognito Chrome used the same fresh current-identity private rivalry, privacy-safe fingerprint `pair_8f24ae...3528c`.

The historical `pair_a07108...756fb` rivalry still exists remotely but its original local profile/save identities were deleted. Production correctly refuses replacement-local-identity reattachment. Do not force, edit or delete it for testing.

The authorized Player Two unavailable-code fixture (`pair_` plus 64 zero hex digits) was consumed exactly once in the predecessor and must not be repeated merely for duplication.

### 2. Non-mutating revision-0 Preview

Player Two observed remote revision 0 and generated `PREVIEW REMOTE → LOCAL`. Production explicitly reported that the remote was observed only and had not changed the local Save. The Preview was bound to the exact remote revision/content hash and exact Player Two local target; confirmation remained unchecked and Apply disabled.

### 3. Deliberate stale-intent rejection

Player One refreshed the same rivalry at revision 0 and published exactly once, advancing authoritative remote state to revision 1 without changing Player One's Local Save Library.

The untouched Player Two revision-0 Preview was then explicitly confirmed and `BACK UP + APPLY EXACT REVISION` was exercised exactly once. Production rejected it because the remote changed after Preview. Reviewed revision 0 was not applied, `LOCAL COMMIT` remained unapplied, the target remained stable and no backup-completed note appeared. This matches source ordering: fresh remote verification happens before backup creation and Candidate C mutation.

### 4. Fresh revision-1 baseline

Player Two refreshed to remote revision 1 without local overwrite. The real local state contained exactly one Save (`TEWE`) and two stable Local Profiles (`ghyu`, `GOP`), both linked to that Save. No artificial unrelated second Save was created.

Fresh revision-1 Preview hash:

`sha256:22bc1bea2833533a978ddfb0a6092b8279d40109234606da762d14cc359ccf3d`

The Preview remained bound to Player Two and the same local target.

### 5. Verified backup-first Candidate C Apply

After exact explicit confirmation, the owner pressed `BACK UP + APPLY EXACT REVISION` once. Production reported successful local commit at revision 1 only after verified backup and exact Candidate C transaction.

Chrome download history showed `career-mode-showdown-backup-2026-08-26T00-24-06Z.json`, and the owner supplied the actual JSON. It is backup format v2, app `1.8.1`, runtime `1.8.1-r3`, one Save, two Local Profiles, no warnings and no recovery payload.

Backup checksum:

`c2d3a4054e6aad5ff007dbe6f04340d2bbfc641f4ce60f4b34e3a827b5f834e4`

The checksum was independently recalculated using the production canonical backup algorithm and matched the embedded value exactly.

Do not commit the owner's raw backup into the repository merely for evidence; it contains full local identity identifiers. The privacy-safe proof record is sufficient.

### 6. Post-Apply convergence and remote immutability

After successful Apply, Player Two refreshed shared state once without publishing. Production still showed remote revision 1, the same local target and `LOCAL COMMIT: Revision 1`.

The post-Apply Save Library still contained the single active `TEWE` Save and the Manager Identities still showed the same `ghyu` and `GOP` profiles linked to it.

A final read-only Preview still showed remote revision 1 with the exact same pre-Apply hash:

`sha256:22bc1bea2833533a978ddfb0a6092b8279d40109234606da762d14cc359ccf3d`

The UI reported that remote revision 1 already matches the identity-safe local gameplay candidate. This proves exact local convergence and, because no Player Two publish occurred and the authoritative remote revision/hash are unchanged, proves local Candidate C Apply did not mutate remote authority.

Canonical evidence: `OWNER_PRODUCTION_STAGE4_REMOTE_TO_LOCAL_RECONCILIATION_PROOF_2026-08-25.md`.

## What is DONE versus still uncredited

DONE / production-proven for this gate:

- non-mutating exact Preview;
- immutable remote revision/hash and local-target binding;
- stale Preview rejection before backup/local mutation;
- explicit confirmation;
- verified exact backup before destructive Apply;
- Candidate C-only destructive local Apply;
- stable Player Two identity and real Save Library structure;
- exact local convergence;
- zero remote-authority mutation from local Apply;
- visible distinction between observed remote and committed local state.

Still explicitly uncredited in RJR-1:

- exact idempotency replay in production;
- third-account/revoked-device production negatives;
- two-network/adverse-network/token-lifecycle hardening;
- actual Stage 5 Private Remote Joining sessions;
- final stable connected release acceptance and rollback/failure proof where still required.

The real owner state had no unrelated second Save, so unrelated-Save preservation was not artificially re-proven in production. Permanent Candidate C contracts remain the evidence for that mutation boundary.

## IMMEDIATE NEXT TASK AFTER FULL STUDY

Do not start with a generic hardening program and do not repeat closed owner setup. The next successor should first finish the smallest remaining explicit pre-Stage-5 Stage 4 capability.

Recommended first lane: exact idempotency replay production proof, because RJR-1 still names it explicitly as uncredited and it directly protects authoritative Connected Rivalry mutation.

Ordered execution:

1. Start from the newest compact starter and `SESSION_BOOTSTRAP.json`; independently verify live main, PR #153 final state, current runtime identity and current RJR authority.
2. Initialize a fresh unique WEC with all per-environment counters reset. Do not inherit this environment's transition decision.
3. Load only the idempotency-relevant Stage 4 mutation receipt/client/Rules/permanent-contract source needed to understand the exact accepted-result replay contract.
4. Determine the smallest safe real-production proof that an exact replay returns the original accepted outcome without another authoritative revision and without local Save mutation. Prefer automated/deployed proof where it can establish the production boundary without making the owner repeat setup.
5. If a real product defect appears, repair the smallest defect through the normal exact-head test/review/merge/deploy path. Do not create a documentation/security/provider sidequest.
6. After exact replay is closed, reassess live remaining Stage 5 blockers. Known gaps include third-account/revoked-device production negatives and adverse-network/token-lifecycle behavior. Take them one capability at a time.
7. Begin Stage 5 Private Remote Joining host/join/session orchestration only when the fresh evidence-led assessment proves the remaining explicit pre-Stage-5 gates are closed.

Private Remote Joining remains the highest long-term project priority; prerequisite ordering exists to reach it safely, not to postpone it indefinitely.

## Permanent locks

- exactly two private managers;
- canonical localStorage exactly `careerModeShowdown.saveLibrary`, `careerModeShowdown.legacyShowdowns`, `careerModeShowdown.preferences`;
- `activeShowdown` non-canonical;
- Candidate A non-mutating export;
- Candidate B read-only analysis;
- Candidate C sole destructive local Apply authority;
- Firebase Spark / zero billing;
- Firestore memory-only persistence;
- Google Auth popup-only `browserSessionPersistence`, no extra scopes;
- App Check enforcement OFF;
- trusted-runtime IAM reviewed but not activated or broadened;
- no public discovery, community, public matchmaking, public invitation directory or global rankings;
- display names never authorize;
- local-first startup/recovery must not depend on Firebase availability.

## WEC / SLE transition discipline

Closing environment: `we-2026-08-25-stage4-owner-reconciliation`.

Usage remains `null/unavailable`; no approved current-session usage percentage was supplied. Never fabricate it.

The closing environment's handoff decision is predecessor-only. The successor validates the predecessor facts, archives them as orientation, initializes a fresh environment identity and makes its own WEC decision.

SLE = Smart Lean Efficient must be recursively preserved. At every future Handoff proximity 100%, generate the complete root handoff plus byte-identical `project-documents/handoffs/` mirror, a new versioned compact root starter plus byte-identical `project-documents/session-starts/` mirror, refresh `SESSION_BOOTSTRAP.json` and any genuinely changed context pointers, run applicable packaging/publication gates, seal WEC, and stop before the next substantial milestone.

## Required owner-facing format

Handoff proximity: X%
Remote Joining readiness: X/100
Current lane: ...
Concrete dependency completed: ...
Next unlock: ...
Blocker: ...
Sidequest check: ...
