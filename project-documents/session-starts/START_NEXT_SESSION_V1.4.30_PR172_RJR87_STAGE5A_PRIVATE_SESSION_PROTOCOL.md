# START NEXT SESSION — v1.4.30 / PR #172 / RJR87 / Stage 5A Private Session Protocol

SLE = Smart Lean Efficient. This compact repository-first starter is orientation only. Current source, live GitHub/provider/deployment evidence, the fresh successor WEC, and later explicit owner instructions win.

## Live boundary to verify

- Repository: `nikahanghojjati-oss/fifa17-career-showdown2`.
- Public site: `https://nikahanghojjati-oss.github.io/fifa17-career-showdown2/`.
- Production application/runtime: `v1.8.1 / 1.8.1-r5`.
- PR #171 exact final head: `d5c8549924244ee177065559043e0697d0c810c3`.
- PR #171 squash merge / pre-PR #172 live main: `1d945ba47c89c305575ef72cc26672fc3e0743ff`.
- Exact PR #171 / merge tree: `d8e5ef517457693ff7dd095db0777b8f366593c4`.
- PR #171: 14/14 permanent exact-head workflow families successful; final-head Codex review found no major issue; all eight inline review threads resolved.
- Post-merge: all 14 normal push validation families plus Pages run `33264211554` successful.
- Production acceptance document and both acceptance-only modules independently matched live main byte-for-byte through cache-bypassing retrieval.
- Provider Rules proof: `PRODUCTION_FIRESTORE_RULES_PROVIDER_PROOF_2026-08-29.md`; reviewed blob `2b7c0b166ae0aae7ab7a3ce84725b21091262484`.
- Provider-abuse proof: `PRODUCTION_PROVIDER_ABUSE_ACCEPTANCE_PROOF_2026-08-29.md`.
- Fixed Remote Joining readiness: `87/100` under RJR-1.
- Closing WEC: `we-2026-08-29-provider-abuse-production-acceptance`, final decision `HANDOFF_AT_CHECKPOINT`.
- Publication checkpoint: PR #172 `Record production provider abuse acceptance and unlock Stage 5A` on `rjr/provider-abuse-acceptance-2026-08-29`.

Independently verify PR #172 final exact head, all 14 permanent workflows, reviews/threads/mergeability, squash merge, post-merge push validation, Pages publication, live main, unchanged production runtime, current ledger, and closing WEC. Never trust recorded candidate facts over live state.

## Production evidence and RJR reconciliation

At `2026-08-29T18:22:57.861Z`, a legitimate existing active Connected Account ran deployed `PROBE ENUMERATION DENIAL`. The sanitized production result was `PASS / PROVIDER_ABUSE_AUTHENTICATED_LIST_DENIED` with:

- `authenticatedAccountStable: true`;
- `authenticationControlsLockedDuringQuery: true`;
- `queryLimit: 1`;
- `rivalryListDenied: true`;
- `providerErrorCode: permission-denied`;
- `firestoreWritesRequested: 0`;
- `localStorageUnchanged: true`;
- both provider-abuse and RJR evidence-candidate flags true.

The account fingerprint is intentionally omitted. No account, device, pairing, rivalry, shared state, session, revocation, provider write, local Save, or canonical storage state was created or mutated.

Fixed RJR advances exactly `86 -> 87`. Only `real-device-hardening-release` moves `8/10 -> 9/10`; `production-cloud-security` remains capped at `20/20`. Implementation, PR/CI/deployment, browser authentication process, service-worker repairs, documentation, and repeated result subfields receive zero duplicate credit.

Current domains: deterministic sync/recovery `20/20`; identity/auth/trust `18/20`; production cloud/security `20/20`; devices/pairing/Connected Rivalry/actual Remote Joining `20/30`; real-device hardening/release `9/10`.

## Stage 5 activation

The exact production denial closes the last explicit non-state-dependent Stage 4 / production-security prerequisite. Stage 5 is no longer locked. Third-account and revoked-device production negatives remain state-dependent evidence opportunities; nonexistent state must not be fabricated and does not block the next engineering slice.

Current production `firestore.spark.rules` remains unchanged: exact entitled session `get` is allowed only for an existing member, while session `list/create/update/delete` stays denied. This checkpoint contains no Stage 5 runtime and no production session Rules mutation.

## IMMEDIATE NEXT TASK AFTER FULL STUDY

1. Validate/archive closing WEC `we-2026-08-29-provider-abuse-production-acceptance` and append its closure additions-only.
2. Initialize a fresh unique WEC from independently observed live main with every per-environment counter reset; run `npm run work:assess` and obey that fresh decision.
3. If permitted, start Stage 5A as real product work: implement a separate private-session protocol/client module plus deterministic emulator contracts and candidate minimum session Rules. Do not add sessions to `js/sparkConnectedRivalry.js`.
4. Use the reserved `rivalries/{rivalryId}/sessions/{sessionId}` schema and exact fields. Require an active paired rivalry with exactly two entitled accounts; current active account/device checks on every operation; opaque exact-path private capability and no listing; host-only `open` creation; peer-only atomic `open -> active` join; immutable rivalry/host/membership authority; bounded expiry/revoke/close; deterministic idempotent retry; fail-closed provider loss.
5. Permanently cover authorized host/join, same-account/third-account denial, inactive/revoked fixture denial, expiry/replay denial, no session/rivalry discovery, unchanged canonical local storage, and adverse-provider failure. Emulator identities are test mechanisms only and earn no production RJR credit.
6. Keep the client memory-only and change no local Save, Candidate C authority, Auth persistence, App Check enforcement, IAM, billing, historical rivalry, or production Rules.
7. Only after the protocol/client/candidate Rules/emulator proof and exact-head review are clean may a later separately reviewed checkpoint publish the minimum production session Rules and expose host/join UX.

Do not insert a generic prerequisite lane. Do not repeat consumed owner/device, Candidate C reconciliation, exact replay, generic adverse-provider, App Check token lifecycle, structural abuse, sustained mutation-frequency, rollback/restoration, provider Rules, or provider-abuse proof merely for confidence.

## Permanent locks

Exactly two private managers. Canonical browser storage remains exactly `careerModeShowdown.saveLibrary`, `careerModeShowdown.legacyShowdowns`, and `careerModeShowdown.preferences`; `activeShowdown` is non-canonical. Candidate A is non-mutating, Candidate B read-only, and Candidate C remains sole destructive remote-to-local Apply authority with transaction-owned strict exact raw snapshot rollback.

Firebase remains Spark / zero billing. Firestore client persistence remains memory-only. Google Auth remains popup-only `browserSessionPersistence` with no extra scopes. App Check enforcement remains OFF. Trusted-runtime IAM remains unactivated/unbroadened. No public discovery, profiles, community, matchmaking, session listing, global rankings, or leaderboards. Historical rivalry `pair_a07108...756fb` remains untouched.

Standing owner merge/deploy authorization remains active only after all required tests, exact-head workflows, final-head review, resolved-thread, mergeability, deployment, and live-verification gates pass. Later explicit owner instructions override it.

## Recursive operating rule

Every substantive owner-facing response uses exactly:

Handoff proximity: X%
Remote Joining readiness: ~Y%
Estimated focused sessions to genuine RJR100: ~N–M
Current lane: <current bounded engineering lane>
Concrete dependency completed: <most recent concrete dependency completed>
Next unlock: <next dependency or proof gate>
Blocker: <current blocker, or NONE>
Sidequest check: <NONE, or NECESSARY because ...>

The session estimate is roadmap-based guidance, not RJR evidence or a score-derived countdown. Current expected critical path from fixed RJR87 is approximately `4–7` focused successor sessions; contingency `7–11` if session Rules, review, two-network acceptance, reconnect/token hardening, or legitimate bugs require extra iterations.

At Handoff proximity 100% or a stricter transition boundary, refresh the complete mirrored SLE package, current pointers, and fresh repository-first next-developer prompt; run `npm run work:next-prompt`; make the final WEC seal the last intended branch mutation; validate/publish the unchanged exact head; give the owner the short prompt; and stop before another substantial milestone. This rule is recursive and permanent until the owner changes it.

## Repository-first next-developer prompt

After refreshing the current starter, full handoff, bootstrap, and context pointers, run `npm run work:next-prompt`. Use the generated short repository-first prompt as the owner-facing continuation entrypoint. It supplements rather than replaces the byte-identical starter and full SLE mirrors.
