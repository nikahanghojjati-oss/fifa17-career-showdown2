# START NEXT SESSION V1.4.21 — PR #154 MERGED / POST-MERGE GREEN / EXACT REPLAY PROVEN

SLE = Smart Lean Efficient. This rule is mandatory for this successor and every later successor. The owner normally needs to provide only this compact starter first; the complete deep handoff is repository-owned.

## LIVE STATE

Repository: `nikahanghojjati-oss/fifa17-career-showdown2`

Public site: `https://nikahanghojjati-oss.github.io/fifa17-career-showdown2/`

Published product remains `v1.8.1 / 1.8.1-r3`. PR #154, `Prove exact accepted-result idempotency replay`, finished on exact head `317121cd3298f2d452b079c06af53b21470be57b`. All 14 permanent PR workflow families succeeded on that unchanged head; PR #154 was mergeable with zero submitted reviews and zero inline review threads. It squash-merged to `main` as `141a7937a9e4cf580178c857dc6267e464f918aa`. The resulting 15 post-merge workflow/deployment runs all succeeded, including GitHub Pages and the deployed-site Stability journey. PR #154 did not change production runtime bytes, Firestore Security Rules, Firebase provider configuration, billing, canonical local storage, Google Auth behavior, App Check enforcement, trusted-runtime IAM or Stage 5 session behavior.

Remote Joining readiness is `80/100` under fixed RJR-1. Exact accepted-result idempotency replay earned exactly +1 from the prior production-reconciliation checkpoint 79 → 80. Do not award duplicate credit for replay subassertions, CI volume, documentation, PR count or handoff work.

Exact replay is now closed: the permanent Firebase emulator proof uses the real `js/sparkConnectedRivalry.js` mutation runtime plus production `firestore.spark.rules`, accepts revision 0, advances authority with a different legitimate mutation to revision 1, and then replays the exact original key/fingerprint. Replay returns the original accepted revision/hash while current revision 1, the accepted receipt set and the tracked canonical local Save snapshot remain unchanged. Same-key/different-fingerprint reuse is rejected; cross-owner receipt authority is denied; a genuinely new stale mutation remains CAS-rejected.

This handoff is being sealed from branch `agent/post-pr154-sle-handoff-seal`. Treat every SHA and PR number here as orientation: independently fetch live `main`, current handoff-seal publication state, checks/reviews/threads, runtime identity and `REMOTE_JOINING_READINESS.json` before acting. Current source and live GitHub always win.

## IMMEDIATE NEXT TASK AFTER FULL STUDY

1. Validate the predecessor publication facts, then initialize a fresh unique WEC with counters reset. Do not inherit the predecessor transition decision and never fabricate usage.
2. Start the smallest remaining explicit pre-Stage-5 production authorization negative: third-account and/or revoked-device denial.
3. Existing permanent emulator coverage already proves both boundaries: a registered third account is denied private-rivalry attachment with `permission-denied`, and a revoked Player Two device is denied authoritative publication with `CONNECTED_RIVALRY_DEVICE_REVOKED` while authority stays unchanged.
4. First determine whether existing deployed/runtime proof machinery can prove one of those negatives safely without recreating owner accounts, pairings or destructive state. The production client exposes device-revocation machinery, but the predecessor found no normal UI control invoking it; confirm live source before relying on that observation.
5. If genuine production-only identity setup is unavoidable, ask the owner only for the minimum evidence/action needed for that one negative boundary. Do not bundle unrelated setup.
6. After the production-negative boundary closes, reassess two-network/adverse-network and token-lifecycle hardening. Begin Stage 5 Private Remote Joining host/join/session orchestration as soon as fresh evidence proves the remaining explicit pre-Stage-5 gates are closed.

Do not reopen or repeat the successful destructive Candidate C remote-to-local Apply, consumed unavailable-code fixture, historical deleted-identity rivalry recovery, r2/r3 Connected Account incident, Stage 4 reconciliation or exact-replay proof merely for duplication.

## PERMANENT LOCKS

Exactly two private managers. Canonical browser storage remains exactly `careerModeShowdown.saveLibrary`, `careerModeShowdown.legacyShowdowns`, `careerModeShowdown.preferences`; `activeShowdown` is non-canonical. Candidate A remains non-mutating export, Candidate B read-only import analysis, Candidate C sole destructive Apply authority. Firebase remains Spark / zero billing. Firestore remains memory-only. Google Auth remains popup-only `browserSessionPersistence` with no extra scopes. App Check enforcement remains OFF. Trusted-runtime IAM remains reviewed but unactivated/unbroadened. No public discovery, community, matchmaking, public invitation directory or global rankings. Local-first startup/recovery must remain usable without Firebase.

Standing owner authorization remains active: after all required tests, exact-head review/thread gates and deployment gates pass, merge/deploy without repeatedly asking for approval. A later explicit owner instruction overrides it.

## DEEP REFERENCE

Canonical full handoff: `SUCCESSOR_HANDOFF_POST_PR154_EXACT_REPLAY_PROVEN_SLE_2026-08-25.md`

Stable compact mirror: `project-documents/START_NEXT_SESSION.md`

Machine bootstrap: `SESSION_BOOTSTRAP.json`

Context graph: `SESSION_CONTEXT_GRAPH.json`

Current execution authority: `NEXT_TASK.md`

Current product state: `PROJECT_STATE.md`

RJR authority: `REMOTE_JOINING_READINESS.json`

Closing WEC: `WORK_ENVIRONMENT_STATUS.json`

At Handoff proximity 100%, recursively create the complete mirrored SLE package, refresh live pointers, seal WEC and stop before the next substantial milestone.

Owner-facing checkpoint template:
Handoff proximity: X%
Remote Joining readiness: ~Y%
Current lane: ...
Concrete dependency completed: ...
Next unlock: ...
Blocker: ...
Sidequest check: ...
