# START NEXT SESSION — v1.4.9 — v1.8.0 Reconciliation Candidate Publication

Use this file as the only initial owner payload. SLE = Smart Lean Efficient. Load the compact capsule first, verify live state, and expand to the complete handoff only if a real contradiction or recovery question requires it.

Repository: `nikahanghojjati-oss/fifa17-career-showdown2`
Public site: `https://nikahanghojjati-oss.github.io/fifa17-career-showdown2/`
Branch: `agent/stage4-remote-local-reconciliation`

## Exact completed boundary

- Verified live `main` at predecessor entry: `065222416dbd65e4b7886eaebf9a3f375f7c60a8`, PR #135 merge.
- Local engineering checkpoint commit: `42b89dfd4859d1655d231d2c04406e66545e45b4`.
- Verify the final transition-seal branch HEAD live; it contains only SLE/WEC packaging after the engineering checkpoint.
- Source candidate: `v1.8.0 — Safe Remote Reconciliation` / `1.8.0-r1`, release candidate and not production-proven.
- Production and immediate whole-shell recovery authority: production-proven `v1.7.0 — Connected Rivalry` / `1.7.0-r2`.
- RJR-1 remains exactly `77/100`; source, tests, PRs, CI, merge or deployment alone do not increase it.
- Production Firestore Rules remain exact blob `ecc8ccb2ab50f0f7057ab3170eb080ad9e36025f`; the candidate contains no Rules change and they must not be republished.
- No v1.8.0 PR, merge, deployment or production reconciliation proof exists yet.

## Delivered candidate

Remote refresh and preview remain non-mutating. Explicit confirmed local Apply is routed only through Candidate C. Immutable intent binds the exact observed remote revision/hash to the exact local Save/profile/role. Canonical backup completes before mutation. Exact local raw guards and a second remote guard survive asynchronous boundaries. Candidate C retains transaction-owned mutation, stale-state and anti-clobber rejection, ownership-scoped rollback and exact recovery verification.

The Settings UI distinguishes remote observed revision from local committed revision and requires an exact confirmation before Apply. Local Save identity, profile references, existing local season IDs, unrelated Saves, Legacy Showdowns and Preferences are preserved.

The two valid PR #135 review corrections are included: current starter/handoff templates use exact `Remote Joining readiness: ~Y%`, and production-proven candidate status is legal only when candidate and production application/runtime identities match.

Local gates passed: focused release/SLE/reconciliation contracts, all 69 repository contracts, protected workflow blocks and the rendered Stage 4 reconciliation browser audit.

Publication is authorized only after all required tests and gates pass on the exact unchanged PR head.

## IMMEDIATE NEXT TASK AFTER FULL STUDY

1. Verify live `main`, the branch final HEAD, changed files, current PR state, package/runtime identity and WEC. Treat this starter as orientation only.
2. Validate/archive closing environment `we-2026-08-24-stage4-remote-local-reconciliation`; its `HANDOFF_NOW` decision is not inherited. Initialize a fresh unique WEC, reset counters and assess.
3. If the fresh WEC permits, publish the exact sealed branch and open one PR to `main`. Do not alter the validated candidate unless a demonstrated gate failure requires a focused fix.
4. Require all 14 permanent pull-request workflow families on the exact unchanged head, inspect exact failed job logs before any repair, and require clean reviews, inline threads and mergeability.
5. Do not republish unchanged Firestore Rules. Under standing owner authorization, merge only the exact validated head and verify the GitHub Pages deployment and `1.8.0-r1` shell.
6. After deployment, run genuine production proof with the existing paired managers: non-mutating preview first, explicit exact confirmation, backup-before-Apply, local commit identity preservation and cross-manager remote/local state distinction. Do not repeat Firebase, App Check, account, device or pairing setup absent regression evidence.
7. Change RJR-1 only for genuine newly proven fixed-domain production capability. Exact replay/negative authorization, two-physical-device/network hardening and Stage 5 remain incomplete.

## Locked boundaries

- Canonical storage is exactly `careerModeShowdown.saveLibrary`, `careerModeShowdown.legacyShowdowns` and `careerModeShowdown.preferences`; `activeShowdown` is non-canonical.
- Candidate A is non-mutating export. Candidate B is read-only analysis. Candidate C is the sole destructive local Apply authority.
- Exactly two managers, private authorization and Firebase Spark / zero billing remain locked. App Check enforcement remains OFF; Firestore remains memory-only and authentication remains popup-only `browserSessionPersistence` with no extra scopes.
- No public discovery, community, matchmaking, rankings or global leaderboard.
- Stage 5 host/join sessions, presence and lobbies remain locked.

## Mandatory owner reporting

Every substantive response must end with exactly:

Handoff proximity: X%
Remote Joining readiness: ~Y%
Current lane: ...
Concrete dependency completed: ...
Next unlock: ...
Blocker: ...
Sidequest check: ...

At 100%, create the complete Smart Lean Efficient package with byte-identical root/project mirrors, refresh the capsule/context pointers, seal the WEC and stop before the next substantial milestone.

Deep fallback only: `SUCCESSOR_HANDOFF_V1.8.0_RECONCILIATION_CANDIDATE_TO_PR_SLE_2026-08-24.md`.
