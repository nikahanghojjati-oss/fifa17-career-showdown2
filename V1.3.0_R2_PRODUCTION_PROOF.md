# Career Mode Showdown v1.3.0 Runtime Maintenance r2 Production Proof

Date: 2026-08-15 ET
Application version: `v1.3.0`
Production runtime revision: `1.3.0-r2`
Immediate previous known-good whole shell: `1.3.0-r1`
Status: merged, deployed, exact-byte verified and technically production-proven
Public site: `https://nikahanghojjati-oss.github.io/fifa17-career-showdown2/`

## Release identity

Exact production base: `eee3b0c62be4d023b7d83fb22447d37db8a8b9b6`
Implementation checkpoint: `e21e6656a6e19283856ccd6b8c8ad7748265c813`
Frozen candidate head: `cfedec8dccde51a7a9932a1bd3a92cc91514e579`
Release PR: #61 — `agent/local-profile-display-label-edit`
Runtime merge: `67095a02188ebd246da0d0f2cd61158b8e9e504e`
Immediate recovery predecessor: `1.3.0-r1`

The merge has exact parents `eee3b0c...` and `cfedec8...`. Its tree is byte-for-byte identical to the validated PR head. GitHub accepted the merge only with expected-head SHA protection.

The application milestone remains v1.3.0. Runtime revision r2 is required because the bounded feature changes JavaScript and CSS that must reach installed clients through one coherent verified shell. It does not assign a second feature version or alter Service Worker behavior.

## Shipped behavior

- A user can edit only one exact Local Profile `displayName` inside Save Library / Settings.
- Outer whitespace is trimmed and whitespace-only input is rejected before any write.
- Saving the current label is a write-free no-op.
- Stable `profile_*`, `save_*` and `season_*` identities remain unchanged.
- Saved Showdown, in-memory Showdown and Legacy manager labels remain unchanged historical presentation.
- Same visible Local Profile labels remain legal and never become identity authority.
- Explicit identity-link presentation and Identity-Safe Career Analytics / Trophy Room presentation revisioning consume the updated label without changing identity.
- Keyboard disclosure, native validation, exact-profile focus restoration, Chromebook/mobile containment and reduced motion remain protected.

The candidate also corrected two real defects exposed by its required visual proof: a collapsed Save Library CSS Grid item caused by hidden-overflow automatic-minimum behavior, and a 4.19:1 Save-fact label contrast failure. `overflow:clip`, permanent vertical-containment assertions and the approximately 5.01:1 label color correction preserve the intended visual hierarchy.

## Pre-merge proof

PR #61 contained exactly 23 files, 651 additions and 111 deletions across the bounded runtime/UI, whole-shell identity, deterministic/browser proof and authority records.

All 13 normal pull-request workflow families passed exact unchanged head `cfedec8dccde51a7a9932a1bd3a92cc91514e579`:

- Candidate B Import Analysis `31894571685`;
- Candidate C Atomic Restore `31894571676`;
- Final Polish `31894571690`;
- Home Bootstrap `31894571687`;
- League Confirmation `31894571672`;
- Licensed Football Visuals `31894571695`;
- Season Review `31894571673`;
- Settings Workstream `31894571671`;
- Stability Lane `31894571670`;
- Static App `31894571678`;
- Statistics Workstream `31894571682`;
- Transfer Workstream `31894571664`;
- V1 Visual Immersion `31894571679`.

The independent promotion gate verified exact unchanged base and head, a clean merge state, mergeability, zero submitted reviews, zero comments and zero unresolved review threads before the PR was marked ready and merged.

## Exact-merge production proof

All 15 push/deployment runs on merge `67095a02188ebd246da0d0f2cd61158b8e9e504e` succeeded with zero failures and zero cancellations.

Key evidence:

- GitHub Pages run `31894832195` — success;
- GitHub Pages deployment `5922244376` — success on exact merge SHA;
- latest Pages build `1153293091` — `built` on exact merge SHA;
- Release Integration Burn-In `31894832592` — success;
- Candidate B Import Analysis `31894832632` — success;
- Candidate C Atomic Restore `31894832804` — success;
- Licensed Football Visuals `31894832763` — success;
- Stability Lane `31894832637` — success;
- Stability contracts job `95036434681` — success;
- canonical Chromium Stability job `95036465423` — success;
- deployed-site-smoke job `95036682319` — success.

The remaining permanent Home, League, Final Polish, Season, Settings, Static, Statistics, Transfer and V1 Visual Immersion production runs also succeeded on that exact merge.

## Independent deployed verification

The repository deployment verifier confirmed all 71 runtime files under `index.html`, `css`, `js`, `data` and `assets` match local `1.3.0-r2` source byte for byte at the public Pages URL.

Separate trusted HTTPS checks confirmed:

- deployed `service-worker.js` exactly matches the merge, SHA-256 `f05083a1e28979cc6ea9e9c4a602a93ab970a8fe56b96971657532dbe1682139`;
- deployed `manifest.webmanifest` exactly matches the merge, SHA-256 `6add0d1323d3f3eec848877dac4b74d383bb47d882cd7e236577c0a6f6bfe4a1`;
- Service Worker current revision is `1.3.0-r2` and immediate previous revision is `1.3.0-r1`.

An independent public browser journey opened the live site through the cloud browser at runtime `1.3.0-r2`, created isolated test data, opened Save Library, edited one Local Profile label and proved:

- the exact `profile_*` ID remained unchanged;
- the exact `save_*` ID remained unchanged;
- the saved Showdown manager label remained unchanged;
- the Local Profile and explicit identity-link presentation updated coherently;
- whitespace-only input returned `Enter a display label for this Local Profile.` and preserved the valid label.

The production Stability deployed-site-smoke independently passed the complete public Pages journey, recovery/import/export, offline boundary, runtime provenance, visuals and accessibility gates.

## Protected architecture and product invariants

`js/storage.js` remains public raw browser-storage authority. `js/storageTransaction.js` remains raw transaction authority. `js/saveLibraryRuntime.js` remains Save Library product mutation authority. `js/analytics.js` remains derived read-only Analytics authority. UI and Analytics do not directly own canonical browser storage.

Candidate A remains non-mutating export. Candidate B remains read-only analysis. Candidate C remains the only destructive import Apply stage and retains strict exact raw snapshots, transaction-owned mutation/rollback, ownership-scoped anti-clobber protection and exact byte verification.

Gameplay, scoring, Smart Back, accepted FIFA 17-inspired presentation, licensed imagery, startup timing and performance ceilings remain unchanged. Exactly two managers, Showdown lengths 1/3/5/10, same selected league, different permanent clubs, the 11-point maximum and the 0–0-only tiebreaker remain authoritative.

## Visual acceptance boundary

Automated technical production proof is complete. It does not fabricate owner visual acceptance. Any later owner screenshot, visual complaint or acceptance statement remains separate evidence and must be handled on its own merits.

## Authorization boundary

Local Profile display-label editing is closed as a production-proven candidate. Its completion does not authorize profile merge/delete, generic profile CRUD, standalone profile creation, backup-envelope redesign, broader Analytics 2.0, Legacy/Achievements, optional content, cloud/network work, gameplay changes or a second roadmap candidate.
