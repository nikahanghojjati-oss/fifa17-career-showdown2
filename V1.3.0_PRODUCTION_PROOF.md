# Career Mode Showdown v1.3.0 Production Proof

Date: 2026-08-13 ET
Application version: `v1.3.0`
Production runtime revision: `1.3.0-r1`
Previous known-good runtime: `1.2.0-r2`
Status: technically production-proven
Public site: `https://nikahanghojjati-oss.github.io/fifa17-career-showdown2/`

## Release identity

Frozen candidate head: `b8d92e9a8a9eec2820c439c0dd2699e9d825a91f`
Release PR: #42 — `release/v1.3.0-resilience`
Runtime merge: `094401b649954656e27e4a92d027e9532e84ccbf`
Immediate recovery predecessor: `1.2.0-r2`

The candidate preserved the proven r2 DOM and changed release identity coherently to application `1.3.0` / runtime `1.3.0-r1`. The Service Worker predecessor is deliberately `1.2.0-r2`, not `1.2.0-r1`.

## Shipped resilience hardening

- Candidate A fails closed when canonical reads cannot be trusted.
- Candidate B remains strictly read-only analysis.
- Candidate C destructive Apply requires strict exact raw snapshot authority from `captureCareerModeRawRestoreSnapshot()` before mutation; unavailable authority returns `snapshot-unavailable` without entering the storage transaction.
- Candidate C preserves immutable confirmed intent, exact preconditions, freshness checks, transaction-owned mutation and rollback, anti-clobber ownership, exact post-write verification, byte-for-byte rollback verification and critical recovery on uncertainty.
- Service Worker update activation verifies the whole candidate shell, awaits successful `skipWaiting()`, and only then emits `CMS_ACTIVATION_ACCEPTED`.
- Existing Service Worker registration is reused and updated rather than redundantly registered.
- PWA reconnect rendering preserves the true pre-offline external-media state.
- Whole-shell current/previous recovery remains coherent across cache corruption and browser restart without turning Cache Storage into user-data authority.
- Settings-only install/update presentation and the protected r2 iOS installed-app loading composition remain unchanged.

## Pre-merge proof

The exact candidate head passed all 13 normal PR workflow families together on PR #40 and then passed an independent fresh 13/13 release generation on PR #42. There are 14 permanent workflow families total; Release Integration Burn-In is main/manual release-only. Performance ceilings were not raised.

## Post-merge public proof

- GitHub Pages deployment: `31755135819` — success.
- Stability: `31755136265` — success.
- deployed-site-smoke job: `94629478166` — success.
- Release Integration Burn-In: `31755136240` — 2/2 independent complete stateful journeys passed.

The deployed-site smoke passed every public-boundary gate against GitHub Pages: exact deployed runtime bytes, runtime-error provenance, Home visual audit, crop-safe football photography, Candidate A export, Candidate B import analysis, Candidate C atomic restore/recovery, install/offline boundary and the complete public journey.

## Protected invariants

Exactly two managers, Showdown lengths 1/3/5/10, same selected league, different permanent clubs, existing scoring and the 11-point maximum remain unchanged. Only 0–0 uses league-position then league-points tiebreakers.

`js/screens.js` remains navigation/history/Smart Back authority. `js/storage.js` remains canonical persistence/destructive mutation authority. `js/storageTransaction.js` remains the raw transaction engine. `js/scoring.js` and `js/analytics.js` remain their existing authorities.

Exactly three canonical localStorage keys remain legal:

1. `careerModeShowdown.activeShowdown`
2. `careerModeShowdown.legacyShowdowns`
3. `careerModeShowdown.preferences`

The Service Worker and Cache Storage contain application bytes only and never canonical user data.

## Visual acceptance boundary

Automated technical production proof is complete. It does not fabricate owner visual acceptance. Any later owner screenshot, visual complaint or acceptance statement is separate evidence and must be handled on its own merits.

## Historical branch warning

PR #37 / `agent/v13-hardening` remains untrusted historical work and must not be used as a baseline. PR #40 remains the detailed salvage/audit record; PR #42 is the actual release PR.
